---
phase: 01-afternoon-win-point-agent-at-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent-config.test.ts
autonomous: false
requirements: [WS-01, WS-02, WS-03]
must_haves:
  truths:
    - "An agent configured with project_dir at the agentic-os repo runs Claude Code with that dir as the SDK cwd"
    - "resolveAgentRuntime returns the project_dir as cwd when the dir exists, and falls back to the agent's own dir (with a warning) when it does not"
    - "Pointed at the workspace, the agent answers from agentic-os CLAUDE.md/AGENTS.md without being told (live chat)"
    - "The mkt-copywriting text skill loads brand_context/voice-profile.md and returns on-brand output over chat (live)"
  artifacts:
    - path: "src/agent-config.test.ts"
      provides: "Both project_dir branch assertions for resolveAgentRuntime (existing dir -> cwd; missing dir -> fallback)"
      contains: "resolveAgentRuntime"
    - path: "~/.claudeclaw/agents/aos/agent.yaml"
      provides: "Workspace agent config with project_dir pointed at agentic-os"
      contains: "project_dir"
  key_links:
    - from: "~/.claudeclaw/agents/aos/agent.yaml"
      to: "resolveAgentRuntime"
      via: "loadAgentConfig reads project_dir; resolveAgentRuntime sets cwd"
      pattern: "project_dir"
    - from: "SDK cwd (agentic-os)"
      to: "agentic-os CLAUDE.md + .claude/skills"
      via: "settingSources:['project','user']"
      pattern: "settingSources"
---

<objective>
Stand up a NEW dedicated workspace agent (id `aos`) whose `project_dir` points at the agentic-os repo, close the one real automated test gap (both `project_dir` branches of `resolveAgentRuntime`), then prove the slice end to end over chat: the agent answers from agentic-os context (WS-02) and a text-only brand skill returns on-brand output (WS-03).

This is the thin end-to-end vertical slice for the phase: configure -> verify cwd resolution in code -> verify the running agent behaves correctly over a live transport. No new feature code: WS-01/WS-02 are already implemented in `agent-config.ts`/`agent.ts`; the only code change is extending an existing test.

Purpose: De-risk pointing an agent at an external workspace before any bridge work, with zero regression to the existing fleet (Bertha/forge/samantha/sentinel/skylar).
Output: extended `src/agent-config.test.ts`; a new `~/.claudeclaw/agents/aos/agent.yaml`; captured live chat transcripts proving WS-02 and WS-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-afternoon-win-point-agent-at-workspace/01-RESEARCH.md
@.planning/phases/01-afternoon-win-point-agent-at-workspace/01-VALIDATION.md
@src/agent-config.ts
@src/agent-config.test.ts
</context>

## Phase Goal

**As a** ClaudeClaw operator, **I want to** point a dedicated agent at the agentic-os workspace over chat, **so that** it auto-loads that workspace's context and skills and produces on-brand output without any divergence from a terminal session.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the WS-01 test gap — assert both project_dir branches of resolveAgentRuntime</name>
  <read_first>
    - src/agent-config.test.ts (existing fixture harness: writeAgent helper, FIXTURE_ROOT, the `projdir` fixture at line 56, the existing "uses project_dir as cwd when it exists" case at line 110)
    - src/agent-config.ts (resolveAgentRuntime lines 208-226: cwd = projectDir if fs.existsSync else resolveAgentDir; loadAgentConfig project_dir parse + warn at lines 166-171)
  </read_first>
  <behavior>
    - Existing-dir branch (already partially covered): resolveAgentRuntime for an agent whose project_dir exists returns cwd === that project_dir. Keep/strengthen the existing `projdir` assertion.
    - Missing-dir branch (the gap): add a fixture agent (e.g. `projdirmissing`) whose project_dir points at a path that does NOT exist; assert resolveAgentRuntime(...).cwd falls back to the agent's own dir (path.join(FIXTURE_ROOT, 'agents', 'projdirmissing')), NOT the missing path.
    - The missing-dir branch must also confirm the resolver does not throw (fallback is non-fatal).
  </behavior>
  <action>
    Extend src/agent-config.test.ts using the existing writeAgent fixture helper and FIXTURE_ROOT pattern. Add a fixture agent `projdirmissing` with `project_dir` set to a path under FIXTURE_ROOT that is never created (e.g. FIXTURE_ROOT + '/does-not-exist-xyz'). Add a `describe('resolveAgentRuntime project_dir fallback')` (or extend the existing `resolveAgentRuntime` describe block) with one case asserting the existing-dir branch (cwd === project_dir for `projdir`) and one case asserting the missing-dir branch (cwd === the agent's own dir for `projdirmissing`). Do NOT mock or assert on console.warn output — the warning is emitted by loadAgentConfig and is non-load-bearing for this test; assert only the resolved cwd. Follow the file's existing ESM import style (`./agent-config.js`) and vitest idioms already in the file. No production code changes — agent-config.ts already implements both branches.
  </action>
  <verify>
    <automated>npx vitest run src/agent-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/agent-config.test.ts` exits 0 with all cases green.
    - A test case asserts `resolveAgentRuntime('projdirmissing').cwd === path.join(FIXTURE_ROOT, 'agents', 'projdirmissing')` (fallback branch).
    - A test case asserts the existing-dir branch resolves cwd to the project_dir.
    - No production source file is modified (only src/agent-config.test.ts changes).
  </acceptance_criteria>
  <done>Both project_dir branches of resolveAgentRuntime are covered by passing automated tests; WS-01 has automated proof for cwd resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Create the `aos` workspace agent config pointed at agentic-os</name>
  <read_first>
    - src/agent-config.ts (loadAgentConfig field parsing lines 101-187: name required; project_dir, slack_channel, model, mcp_servers all optional; AGENT_ID_RE at line 20)
    - .planning/codebase/CONVENTIONS.md ("Agent YAML Schema Conventions" section — canonical agent.yaml fields)
    - ~/.claudeclaw/agents/skylar/agent.yaml (a real fleet agent.yaml for shape reference)
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-RESEARCH.md (Pattern 1 minimal agent.yaml; Pitfalls 4/5/6: path with spaces, restart-on-edit, MCP from settings.json)
  </read_first>
  <action>
    Create the directory `~/.claudeclaw/agents/aos/` and write `~/.claudeclaw/agents/aos/agent.yaml`. Required key: `name: AOS` (or `name: Workspace`). Set `description` to one line describing it as the agentic-os workspace agent. Set `project_dir: "/Users/shannongueringer/App Repo/agentic-os"` (quote the value — the path contains a space; per Pitfall 4 cwd handles spaces fine, the quotes are for YAML/doc safety). Do NOT set `telegram_bot_token_env` (run delegation/routed only — no standalone launchd service, avoiding the path-with-spaces launchd bug per Pitfall 4). Do NOT set `mcp_servers` (the text brand skill needs none, and the allowlist only filters servers defined in settings.json per Pitfall 6). Optionally set `slack_channel` only if the operator supplies a real channel ID; otherwise omit it and reach the agent via `@aos:` delegation. Do NOT create a `CLAUDE.md` for this agent (per locked decision 3: keep the workspace agent's own role prompt minimal/absent so it does not fight the agentic-os CLAUDE.md the SDK loads; full persona reconciliation is Phase 8). Keep agent id `aos` (matches AGENT_ID_RE `/^[a-z0-9_-]+$/i`). Do NOT touch any existing fleet agent dir.
  </action>
  <verify>
    <automated>test -f ~/.claudeclaw/agents/aos/agent.yaml && node -e "const y=require('js-yaml');const fs=require('fs');const os=require('os');const c=y.load(fs.readFileSync(os.homedir()+'/.claudeclaw/agents/aos/agent.yaml','utf-8'));if(!c.name)process.exit(1);if(c.project_dir!=='/Users/shannongueringer/App Repo/agentic-os')process.exit(2);if(fs.existsSync(os.homedir()+'/.claudeclaw/agents/aos/CLAUDE.md'))process.exit(3);console.log('aos agent.yaml valid; project_dir set; no CLAUDE.md')"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.claudeclaw/agents/aos/agent.yaml` exists, parses as YAML, has a non-empty `name`.
    - `project_dir` is exactly `/Users/shannongueringer/App Repo/agentic-os`.
    - No `CLAUDE.md` exists under `~/.claudeclaw/agents/aos/`.
    - `npx vitest run` (full suite) stays green — no fleet regression (COMPAT-02/03).
  </acceptance_criteria>
  <done>A new `aos` agent exists in the external config dir, points at the agentic-os workspace via project_dir, has no competing CLAUDE.md, and leaves the existing fleet untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Restart the bot, then verify WS-02 (workspace context) and WS-03 (on-brand skill) live</name>
  <what-built>
    A new `aos` agent whose SDK cwd is the agentic-os repo. The SDK loads agentic-os CLAUDE.md/AGENTS.md and its .claude/skills via `settingSources:['project','user']` (WS-02), and the text-only `mkt-copywriting` skill lazy-loads `brand_context/voice-profile.md` to produce on-brand output (WS-03). Both verifications are inherently manual (live chat round-trip + brand-voice judgment) per 01-VALIDATION.md.
  </what-built>
  <read_first>
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-RESEARCH.md (Pitfall 5: restart required after agent.yaml edit; WS-02/WS-03 verification targets; Open Question 3 objective on-brand check)
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <how-to-verify>
    1. RESTART the bot/agent process. Editing agent.yaml while running has NO effect — the Slack channel map and runtimeCache are built once at startup (Pitfall 5). Use the project's normal restart path (launchctl kickstart of the running ClaudeClaw service, or restart `dist/index.js`).
    2. WS-02 — reach the `aos` agent (via its `slack_channel` if one was set, otherwise `@aos: <message>` delegation through the main bot) and ask a question whose answer lives ONLY in the agentic-os AGENTS.md / workspace instructions (e.g. ask it to state its workspace's silent-startup ritual or name a skill from the agentic-os skill registry). Expected: it answers correctly from workspace context WITHOUT being told that context in the message. Capture the transcript.
    3. WS-03 — trigger the text-only `mkt-copywriting` skill over the same channel with a real short brief (e.g. "draft a 2-line headline + subhead for X"). Expected: (a) the skill announces it loaded `brand_context/voice-profile.md` (it shows a status line); (b) the output respects the voice profile — no em dashes, no AI clichés, matches the sample tone. Capture the transcript.
    4. Confirm no destructive side effect fired: check `git -C "/Users/shannongueringer/App Repo/agentic-os" status` for unexpected commits and watch SDK stderr for hook stalls (agentic-os hooks fire under headless runs per Pitfall 1; a text skill should not trip a destructive one).
    5. Do NOT use an API/Downloads skill to prove WS-03 (it would hit the env-scrub + deny-list and produce a false negative — those are Phase 2/3 concerns per locked decision 2).
    Save both transcripts into the phase dir (e.g. `.planning/phases/01-afternoon-win-point-agent-at-workspace/ws02-ws03-transcripts.md`) as verification evidence for `/gsd-verify-work`.
  </how-to-verify>
  <acceptance_criteria>
    - WS-02 transcript shows the agent answering from agentic-os AGENTS.md/CLAUDE.md content it was never told in the message.
    - WS-03 transcript shows `mkt-copywriting` loading `brand_context/voice-profile.md` and producing output with no em dashes and no AI clichés, matching the brand voice.
    - `git -C "/Users/shannongueringer/App Repo/agentic-os" status` shows no unexpected/destructive changes from the verification turns.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the two transcripts captured, or describe what failed (e.g. agent not routed = restart missed; skill silently failed = wrong skill choice).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat -> SDK subprocess | Untrusted user message crosses into the agent run |
| SDK cwd -> agentic-os workspace files | Pointing the agent's cwd at an external repo auto-loads that repo's CLAUDE.md/AGENTS.md, skills, .claude/settings.json (hooks + deny rules) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / EoP | agentic-os CLAUDE.md/AGENTS.md/skill content auto-loaded into the agent | accept | Trusted local user-owned repo. Inherited agentic-os deny list (Bash curl/wget/rm/ssh, Read .env/*.key) still applies even under bypassPermissions ("deny always wins"), and getScrubbedSdkEnv strips *_API_KEY/*_TOKEN. No new external untrusted input. Documented as a constraint (only point at trusted repos) in plan 02. |
| T-01-02 | EoP | agentic-os .claude hooks (SessionStart/Stop/PostToolUse etc.) run under the headless bot turn | accept | Hooks are from the user's own repo and are fire-and-forget (each parses stdin in try/catch, returns silently). Verification step 4 checks for unexpected git commits / hook stalls. Disabling/scoping hooks is deferred to Phase 2/3 (SK-04). |
| T-01-03 | Information Disclosure | workspace skill attempting to read secrets (cat .env / env) | mitigate | Already mitigated and left UNCHANGED this phase: SDK env scrubbed (security.ts getScrubbedSdkEnv) + `Read(.env)` denied by agentic-os settings. Do not relax either in Phase 1. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (config + test-extension + docs only); RESEARCH.md Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
- `npx vitest run src/agent-config.test.ts` green (WS-01, both branches).
- `npx vitest run` (full suite) green — no fleet regression (COMPAT-03).
- `~/.claudeclaw/agents/aos/agent.yaml` exists with project_dir at agentic-os and no CLAUDE.md.
- WS-02 and WS-03 transcripts captured after a bot restart, showing workspace-context awareness and on-brand text output.
- agentic-os repo has no unexpected git changes from verification turns.
</verification>

<success_criteria>
- WS-01: project_dir resolves to SDK cwd with both branches under automated test.
- WS-02: the `aos` agent reflects agentic-os CLAUDE.md/AGENTS.md context over live chat (transcript).
- WS-03: a text-only brand skill produces on-brand output over live chat (transcript).
- Existing fleet (Bertha/forge/samantha/sentinel/skylar) behavior unchanged; full test suite green.
</success_criteria>

<output>
Create `.planning/phases/01-afternoon-win-point-agent-at-workspace/01-01-SUMMARY.md` when done. Include the captured WS-02/WS-03 transcript file path.
</output>
