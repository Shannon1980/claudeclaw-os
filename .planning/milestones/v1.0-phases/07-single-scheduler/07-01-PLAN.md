---
phase: 07-single-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/<version>/add-aos-cron-scheduled-task-columns.ts
  - migrations/version.json
  - package.json
  - src/db.ts
autonomous: true
requirements: [SCH-02, SCH-03, SCH-04]
must_haves:
  truths:
    - "scheduled_tasks has source, job_path, model, timeout, notify, retry columns after migration"
    - "An atomic claim only lets one process win a given due task (changes === 1 for the winner)"
    - "aos rows are scoped and read separately from main rows via agent_id"
  artifacts:
    - path: "migrations/<version>/add-aos-cron-scheduled-task-columns.ts"
      provides: "Versioned migration adding aos-cron columns to scheduled_tasks"
      contains: "export async function run"
    - path: "src/db.ts"
      provides: "claimDueTask atomic claim + ScheduledTask type with new columns + deactivate-orphan helper"
      contains: "claimDueTask"
  key_links:
    - from: "migrations/version.json"
      to: "migrations/<version>/add-aos-cron-scheduled-task-columns.ts"
      via: "registry entry under version key"
      pattern: "add-aos-cron-scheduled-task-columns"
    - from: "src/db.ts claimDueTask"
      to: "scheduled_tasks"
      via: "UPDATE ... WHERE id=? AND status='active' + changes check"
      pattern: "status\\s*=\\s*'active'"
---

<objective>
Lay the data foundation for the single scheduler: ship a versioned migration adding the aos-cron columns to `scheduled_tasks`, and add the db.ts primitives the firing loop will depend on — an atomic cross-process claim (`claimDueTask`), an extended `ScheduledTask` type, an aos-row sync helper set (upsert + deactivate-orphan), all scoped by `agent_id`.

Purpose: SCH-04 exactly-once requires an atomic claim with a status predicate. SCH-02/SCH-03 require new columns (`source`, `job_path`, `model`, `timeout`, `notify`, `retry`) to hold job-file metadata and the derived projection. Per D-09 these columns MUST ship as a versioned migration, never inline ALTER in `createSchema`.

Output: One registered migration (applied via `npm run migrate`), updated `src/db.ts` (type + claim + helpers), `migrations/version.json` + `package.json` bumped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-scheduler/07-CONTEXT.md
@.planning/phases/07-single-scheduler/07-PATTERNS.md
@.claude/skills/add-migration/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Versioned migration adding aos-cron columns to scheduled_tasks</name>
  <files>migrations/&lt;version&gt;/add-aos-cron-scheduled-task-columns.ts, migrations/version.json, package.json</files>
  <read_first>
    - .claude/skills/add-migration/SKILL.md (steps 1-7: version compute, file shape, version.json + package.json updates)
    - src/db.ts lines 503-525 (idempotent `PRAGMA table_info(...)` + conditional `ALTER TABLE ... ADD COLUMN` pattern to mirror)
    - migrations/version.json (currently `{ "migrations": {} }` — first registered migration)
    - scripts/migrate.ts lines 240-267 (runner contract: imports each pending `<ver>/<file>.ts`, calls `mod.run()`)
    - package.json (read `version` field as the base per SKILL step 1)
  </read_first>
  <behavior>
    - After `npm run migrate`: `PRAGMA table_info(scheduled_tasks)` includes columns `source` (TEXT DEFAULT 'user'), `job_path` (TEXT), `model` (TEXT), `timeout` (TEXT), `notify` (TEXT), `retry` (INTEGER NOT NULL DEFAULT 0).
    - Re-running the migration is idempotent: each ADD COLUMN is guarded by a `PRAGMA table_info` existence check, so a second `npm run migrate` makes no changes and does not throw.
    - Existing rows are preserved (no data loss): `source` defaults to 'user' so pre-existing user tasks remain non-aos-cron.
  </behavior>
  <action>
    Follow the add-migration skill to scaffold `migrations/&lt;version&gt;/add-aos-cron-scheduled-task-columns.ts`. Compute the version from `package.json` (version.json is empty → first key); use a patch bump. The file exports `const description` and `async function run(): Promise&lt;void&gt;`. The `run()` body opens `store/claudeclaw.db` via the same `better-sqlite3` import `db.ts` uses (the migration receives no `database` handle — open it itself). Add these columns to `scheduled_tasks`, each guarded by a `PRAGMA table_info(scheduled_tasks)` existence check (mirror src/db.ts 503-525): `source TEXT NOT NULL DEFAULT 'user'`, `job_path TEXT`, `model TEXT`, `timeout TEXT`, `notify TEXT`, `retry INTEGER NOT NULL DEFAULT 0`. Append the filename to `migrations/version.json` under the new version key and bump `package.json` `version` to match (no `v` prefix) per SKILL steps 6-7. Do NOT add these columns via inline `addColumnIfMissing` in `db.ts createSchema` (D-09). The `source` default 'user' keeps existing user-created tasks distinct from `'aos-cron'` rows.
  </action>
  <verify>
    <automated>npm run migrate &amp;&amp; node -e "const d=require('better-sqlite3')('store/claudeclaw.db');const c=d.prepare('PRAGMA table_info(scheduled_tasks)').all().map(x=>x.name);['source','job_path','model','timeout','notify','retry'].forEach(n=>{if(!c.includes(n))throw new Error('missing '+n)});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/version.json` has one version key whose array contains `add-aos-cron-scheduled-task-columns.ts`.
    - `package.json` `version` matches the new migration version key (no `v` prefix).
    - After `npm run migrate`, the six new columns exist on `scheduled_tasks`; a second `npm run migrate` is a no-op (idempotent).
    - No inline ALTER for these columns appears in `db.ts createSchema`.
  </acceptance_criteria>
  <done>Migration registered + applied; six aos-cron columns present and idempotent; version registry + package.json bumped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: db.ts atomic claim, extended type, and aos-row helpers</name>
  <files>src/db.ts</files>
  <read_first>
    - src/db.ts lines 1247-1359 (ScheduledTask type 1247-1259, createScheduledTask 1261-1273, getDueTasks 1275-1282, markTaskRunning 1301-1312, updateTaskAfterRun 1314-1324, updateScheduledTask 1342-1355, pauseScheduledTask 1357-1359)
    - src/db.ts lines 2269-2286 (claimNextMissionTask — the SELECT-then-claim-in-one-transaction precedent)
    - .planning/phases/07-single-scheduler/07-PATTERNS.md "src/db.ts — generalized atomic claim" section (D-06 atomic-claim contract)
  </read_first>
  <behavior>
    - `claimDueTask(id, tentativeNextRun)` returns `true` only when its `UPDATE scheduled_tasks SET status='running', started_at=?, next_run=? WHERE id=? AND status='active'` reports `changes === 1`; a second concurrent call on the same active row returns `false` (the row is already 'running'). This is the SCH-04 cross-process backstop.
    - `ScheduledTask` type exposes the new columns: `source`, `job_path`, `model`, `timeout`, `notify`, `retry`.
    - An aos upsert helper writes one row per job scoped `agent_id='aos'`, `source='aos-cron'`; re-running it updates schedule/active/model/timeout/notify/retry metadata for an existing job id without creating a duplicate row.
    - A deactivate-orphan helper sets a row's status to 'paused' (mirrors `pauseScheduledTask`), never deletes — `last_result` history is preserved.
  </behavior>
  <action>
    In `src/db.ts`: (1) Extend the `ScheduledTask` interface (1247-1259) with `source: string; job_path: string | null; model: string | null; timeout: string | null; notify: string | null; retry: number;`. (2) Add `claimDueTask(id: string, tentativeNextRun: number): boolean` mirroring `markTaskRunning` (1301) but with the status predicate `WHERE id = ? AND status = 'active'` and returning `result.changes === 1` — this is the atomic cross-process claim from D-06/PATTERNS. Keep `markTaskRunning` intact for existing callers. (3) Add `upsertAosCronTask({ id, prompt, schedule, nextRun, jobPath, model, timeout, notify, retry, active })` that INSERTs with `agent_id='aos'`, `source='aos-cron'`, status `'active'` or `'paused'` per `active`, and on conflict updates the metadata columns + schedule + next_run (use `INSERT ... ON CONFLICT(id) DO UPDATE` or a select-then-branch); dormant `active:false` jobs get status `'paused'`. (4) Add `deactivateAosCronTask(id: string)` that sets status `'paused'` (reuse the `pauseScheduledTask` shape, 1357-1359) — never DELETE (D-07). (5) Add `getAosCronTaskIds(): string[]` returning all `agent_id='aos' AND source='aos-cron'` ids so the sync can compute the orphan set. Keep `getDueTasks(agentId)` (1275) unchanged — it already scopes by agent_id, so `getDueTasks('aos')` is the aos read path.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run src/db.test.ts 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `claimDueTask` returns true exactly once for a fresh active row and false on a second call (proven by a unit test or a node REPL transcript exercising two sequential claims on the same id).
    - `ScheduledTask` type compiles with the six new fields; `npx tsc --noEmit` is clean.
    - `upsertAosCronTask` produces exactly one row per job id (re-run does not duplicate); `deactivateAosCronTask` sets status 'paused' and leaves `last_result` intact.
    - No `DELETE FROM scheduled_tasks` is used in any aos-cron helper.
  </acceptance_criteria>
  <done>Atomic `claimDueTask` with status predicate + changes check; extended type; aos upsert/deactivate/id-list helpers all scoped to agent_id='aos', source='aos-cron'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | a schema change runs against `store/claudeclaw.db`; a bad ALTER could lose data |
| cross-process → scheduled_tasks | two scheduler processes (main, aos) racing to claim one due row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | migration `run()` ALTER on scheduled_tasks | mitigate | Each ADD COLUMN guarded by `PRAGMA table_info` existence check (idempotent); columns are additive with safe defaults — no DROP/rename, no data rewrite. Verified by re-run no-op. |
| T-07-02 | Denial of Service / integrity | cross-process claim race | mitigate | `claimDueTask` uses `WHERE id=? AND status='active'` + `changes===1`; SQLite write serialization guarantees exactly one winner. This is the SCH-04 backstop. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (uses existing `better-sqlite3`, `cron-parser`). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run migrate` applies cleanly and is idempotent on re-run.
- `npx tsc --noEmit` clean; existing tests still pass.
- `claimDueTask` proven single-winner via the cross-process claim test.
</verification>

<success_criteria>
- Six aos-cron columns present on `scheduled_tasks` via a registered, applied versioned migration (D-09, success criterion 5).
- Atomic `claimDueTask` available as the SCH-04 cross-process backstop.
- aos-scoped upsert/deactivate/id-list helpers ready for the sync service.
</success_criteria>

<output>
Create `.planning/phases/07-single-scheduler/07-01-SUMMARY.md` when done.
</output>
