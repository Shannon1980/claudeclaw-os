---
phase: 04-memory-source-of-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator.ts
  - src/orchestrator.test.ts
  - src/memory.test.ts
  - src/db.test.ts
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A delegated (@aos:) recall is scoped to the workspace agent — it surfaces only the workspace agent's memories, not other agents' memories sharing the same chat_id"
    - "The main (non-delegated) recall path is unchanged — the default fleet still recalls memories the same way it did before (COMPAT-02)"
    - "The memory store path resolves from PROJECT_ROOT, is absolute, and never points under agentic-os — proving the single source of record (MEM-01)"
    - "A memory written under agent X is not returned for agent aos when strict scoping is in effect (no cross-agent leakage)"
  artifacts:
    - path: "src/orchestrator.ts"
      provides: "Delegated recall call passes strictAgentId so the workspace agent's recall is per-agent scoped"
      contains: "strictAgentId"
    - path: "src/memory.test.ts"
      provides: "Unit test asserting buildMemoryContext threads strictAgentId into searchMemories and getRecentHighImportanceMemories"
      contains: "strictAgentId"
    - path: "src/db.test.ts"
      provides: "Real-DB cross-agent leakage test + MEM-01 single-store path assertion"
      contains: "agent_id"
    - path: "src/orchestrator.test.ts"
      provides: "Test asserting delegateToAgent calls buildMemoryContext with { strictAgentId: agentId }"
      contains: "strictAgentId"
  key_links:
    - from: "src/orchestrator.ts (delegateToAgent)"
      to: "buildMemoryContext"
      via: "strictAgentId option on the delegated recall call"
      pattern: "buildMemoryContext\\([^)]*strictAgentId"
    - from: "buildMemoryContext"
      to: "searchMemories / getRecentHighImportanceMemories"
      via: "strictAgentId forwarded as the agentId scope argument"
      pattern: "strictAgentId"
---

<objective>
Close the only correctness gap in Phase 4: the delegated (workspace-agent) recall path does not scope memory recall to the agent, so the workspace agent (`aos`) can surface other agents' memories that share the same `chat_id`. Pass `strictAgentId: agentId` on the single delegated `buildMemoryContext` call in `delegateToAgent` (orchestrator.ts:196). Leave the main/non-delegated recall path untouched to protect the default fleet (COMPAT-02).

This plan also lands the Wave 0 unit tests that prove the behavior: strict per-agent recall (no cross-agent leakage), the delegated call actually passes the flag, and the MEM-01 single-store path assertion. No schema change is needed — `agent_id` already exists on `memories` and `conversation_log`, and the `strictAgentId` plumbing already exists in `buildMemoryContext`, `searchMemories`, and `getRecentHighImportanceMemories`.

Purpose: MEM-02's "no cross-agent leakage into the workspace agent's recall" success criterion, plus the MEM-01 single-store proof, both satisfied with a one-argument code change guarded by tests.
Output: A scoped delegated recall in orchestrator.ts and three test additions (memory.test.ts, orchestrator.test.ts, db.test.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memory-source-of-record/04-RESEARCH.md
@.planning/phases/04-memory-source-of-record/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — strict per-agent recall + single-store unit tests (RED)</name>
  <read_first>
    - src/memory.ts (buildMemoryContext signature lines 62-67; strictAgentId opt lines 53-56; forwarded into searchMemories at line 93 and getRecentHighImportanceMemories at line 103)
    - src/memory.test.ts (existing mock setup: db.js boundary is mocked at lines 3-19; embeddings mocked; buildMemoryContext imported)
    - src/db.test.ts (uses a REAL in-memory DB via _initTestDatabase() in beforeEach at line 27-29; saveStructuredMemory + searchMemories + getRecentHighImportanceMemories imported and exercised against the real schema)
    - src/db.ts (searchMemories signature line 899, agentId is the 5th param; getRecentHighImportanceMemories signature line 1029, agentId is the 3rd param; saveStructuredMemory signature line 830, agentId is the 8th param default 'main')
    - src/config.ts (PROJECT_ROOT line 143, STORE_DIR line 144)
  </read_first>
  <behavior>
    - Test A (memory.test.ts, mocked db boundary): calling buildMemoryContext('chat1', 'q', 'aos', { strictAgentId: 'aos' }) forwards 'aos' as the agentId argument to BOTH searchMemories (5th positional arg) and getRecentHighImportanceMemories (3rd positional arg). Assert via vi.mocked(searchMemories).mock.calls and vi.mocked(getRecentHighImportanceMemories).mock.calls.
    - Test B (memory.test.ts): calling buildMemoryContext('chat1', 'q', 'main') WITHOUT opts forwards undefined as the agentId scope arg to searchMemories and getRecentHighImportanceMemories (proves the legacy/default-fleet recall is unscoped — COMPAT-02 guard).
    - Test C (db.test.ts, real in-memory DB): save one memory under agent_id='aos' and one under agent_id='main' in the same chat_id, both with importance >= 0.5 and overlapping summary text. Assert getRecentHighImportanceMemories('chat1', 10, 'aos') returns ONLY the aos memory; getRecentHighImportanceMemories('chat1', 10, 'main') returns ONLY the main memory; getRecentHighImportanceMemories('chat1', 10) (no agentId) returns BOTH.
    - Test D (db.test.ts, real in-memory DB): same two-agent setup; searchMemories('chat1', '<shared keyword>', 5, undefined, 'aos') returns only the aos row (FTS5 path, no embedding).
    - Test E (db.test.ts, MEM-01 single-store path assertion): import STORE_DIR and PROJECT_ROOT from config; assert path.isAbsolute(STORE_DIR), STORE_DIR.startsWith(PROJECT_ROOT), and !STORE_DIR.includes('agentic-os'). This proves the store path is cwd-independent and cannot resolve to a second store under the workspace.
  </behavior>
  <action>
    Add the five test cases described in the behavior block. Put Test A and Test B in src/memory.test.ts inside the existing describe block for buildMemoryContext, reusing the existing vi.mock('./db.js') and vi.mock('./embeddings.js') setup (embedText already returns []); set the searchMemories and getRecentHighImportanceMemories mocks to return [] so the assertion is purely on the forwarded arguments. Put Test C, Test D, and Test E in src/db.test.ts, which already initializes a real in-memory DB in beforeEach; pass the agent id as the trailing 'agentId' argument to saveStructuredMemory (8th positional arg) when seeding the aos and main rows. For Test E, import STORE_DIR and PROJECT_ROOT from './config.js'. Use the literal agent id 'aos' (confirmed in ~/.claudeclaw/agents/aos/agent.yaml). These tests assert against the ALREADY-EXISTING strictAgentId/agentId plumbing — do NOT modify production code in this task. Tests A and C-E will pass on current code (the plumbing exists); they exist as regression guards. Test for the delegated caller wiring lives in Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/memory.test.ts src/db.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - getRecentHighImportanceMemories('chat1', 10, 'aos') returns only the aos-attributed memory; the 'main' variant returns only the main memory; the no-agentId variant returns both (Test C green)
    - searchMemories scoped to 'aos' excludes the 'main' row for the same chat_id (Test D green)
    - buildMemoryContext forwards 'aos' to searchMemories and getRecentHighImportanceMemories when strictAgentId is set, and forwards undefined when it is not (Tests A and B green)
    - STORE_DIR is absolute, under PROJECT_ROOT, and does not contain 'agentic-os' (Test E green)
  </acceptance_criteria>
  <done>All five new cases pass; no production code changed in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope the delegated recall call + assert the wiring (GREEN)</name>
  <read_first>
    - src/orchestrator.ts (delegateToAgent; the delegated recall call is at line 196: `const { contextText: memCtx } = await buildMemoryContext(chatId, prompt, agentId);` — note it passes NO opts, leaving strictAgentId undefined)
    - src/memory.ts (buildMemoryContext opts: BuildMemoryContextOpts.strictAgentId lines 53-56)
    - src/orchestrator.test.ts (delegateToAgent suite; buildMemoryContext is already mocked at line 33-35 returning { contextText: '' }; tests assert against vi.mocked(runAgent).mock.calls; agent id under test is 'aos')
    - src/db.ts (no change here; for context only — searchMemories/getRecentHighImportanceMemories agentId scoping)
  </read_first>
  <action>
    In src/orchestrator.ts, change ONLY the delegated recall call at line 196 to pass the strict scope option: call buildMemoryContext with the existing positional args (chatId, prompt, agentId) plus a fourth options argument { strictAgentId: agentId }. Resulting call shape: `await buildMemoryContext(chatId, prompt, agentId, { strictAgentId: agentId })`. Do NOT touch any recall call on the main/non-delegated path (message-core.ts) — that is deliberately left unscoped to protect COMPAT-02. Do NOT add a second ingestion call anywhere (saveConversationTurn already ingests; a second call double-writes). Then add a test in src/orchestrator.test.ts asserting that delegateToAgent('aos', 'do a thing', 'chat1', 'main') calls the mocked buildMemoryContext with a 4th argument equal to { strictAgentId: 'aos' } — assert against vi.mocked(buildMemoryContext).mock.calls[0][3].
  </action>
  <verify>
    <automated>npx vitest run src/orchestrator.test.ts src/memory.test.ts src/db.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The delegated recall call in orchestrator.ts passes { strictAgentId: agentId } (grep: `buildMemoryContext(chatId, prompt, agentId, { strictAgentId: agentId })` present in src/orchestrator.ts)
    - The new orchestrator test asserts buildMemoryContext was called with a 4th arg of { strictAgentId: 'aos' } and passes
    - No main-path recall call was modified (message-core.ts unchanged in this plan; files_modified does not include it)
    - No second ingestConversationTurn / saveConversationTurn call was added
  </acceptance_criteria>
  <done>Delegated recall is per-agent scoped; the wiring test is green; main path untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite at baseline (regression gate)</name>
  <read_first>
    - .planning/phases/04-memory-source-of-record/04-VALIDATION.md (baseline: 519 pass, 4 pre-existing failures — 3 environmental schedule-cli, 1 known dashboard chatId bug)
    - .planning/phases/03-skill-hardening/03-01-SUMMARY.md (test baseline confirmation)
  </read_first>
  <action>
    Run the full vitest suite. Confirm the only failures are the 4 known pre-existing ones (3 schedule-cli environmental, 1 dashboard chatId). The scoping change must not introduce any new failure, and in particular must not change default-fleet (main/comms/content/ops/research) recall behavior (COMPAT-02). If any NEW failure appears, fix it before completing. Do not "fix" the 4 pre-existing failures here — they are out of scope and tracked separately.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Suite result is the established baseline: all pass except the 4 known pre-existing failures
    - No new failure attributable to the scoping change
    - No default-fleet recall test regressed
  </acceptance_criteria>
  <done>Full suite green at baseline; no new failures introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → shared chat memory store | Multiple agents (main, aos, default fleet) share a chat_id in one SQLite store; one agent's recall must not surface another agent's private memories |
| user message → FTS5 query | Untrusted user text is turned into an FTS5 query at recall time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | delegateToAgent recall (orchestrator.ts:196) | mitigate | Pass `{ strictAgentId: agentId }` so the workspace agent's recall is constrained to `agent_id = aos` via existing `AND memories.agent_id = ?` SQL; guarded by db.test.ts cross-agent leakage test and orchestrator.test.ts wiring test |
| T-04-02 | Tampering | searchMemories FTS5 query build | accept | Existing quote-stripping + parameterized better-sqlite3 prepared statements (db.ts) already mitigate operator injection; no new query construction added this phase |
| T-04-03 | Information Disclosure | default-fleet (main path) recall | accept | Main path intentionally left unscoped to preserve legacy shared-chat behavior (COMPAT-02); the no-leakage requirement is specific to the workspace agent, which is now scoped |
</threat_model>

<verification>
- `npx vitest run src/memory.test.ts src/orchestrator.test.ts src/db.test.ts` green (per-task quick run)
- `npx vitest run` at baseline (519 pass, 4 known pre-existing failures)
- grep confirms `strictAgentId` on the delegated call in orchestrator.ts and absence of any new recall-scope change in message-core.ts
</verification>

<success_criteria>
- The delegated (@aos:) recall is per-agent scoped (no cross-agent leakage into the workspace agent's recall) — MEM-02 scoping criterion
- The main path recall is unchanged — COMPAT-02 protected
- MEM-01 single-store path assertion passes (store resolves from PROJECT_ROOT, never under agentic-os)
- No schema change introduced (none required; agent_id already present)
- Full suite at baseline
</success_criteria>

<output>
Create `.planning/phases/04-memory-source-of-record/04-01-SUMMARY.md` when done.
</output>
