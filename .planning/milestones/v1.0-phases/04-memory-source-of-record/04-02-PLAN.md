---
phase: 04-memory-source-of-record
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [MEM-02]
must_haves:
  truths:
    - "A standing preference stated via @aos: in one chat session is durably written to the single ClaudeClaw store and recalled as [Memory context] in a later, separate session (after /newchat) with the same chat + aos agent"
    - "The recalled context reflects only the workspace agent's own memory (no other agent's memory leaks into the aos recall)"
    - "The whole write→recall round-trip runs against store/claudeclaw.db with no second store created"
  artifacts:
    - path: ".planning/phases/04-memory-source-of-record/04-02-SUMMARY.md"
      provides: "Captured two-session transcript proving MEM-02 cross-session recall live"
  key_links:
    - from: "session A @aos: exchange"
      to: "memories table (agent_id='aos')"
      via: "saveConversationTurn → ingestConversationTurn in the bot process"
      pattern: "agent_id.*aos"
    - from: "session B @aos: query"
      to: "[Memory context] in the agent's prompt"
      via: "delegated buildMemoryContext recall scoped to aos"
      pattern: "Memory context"
---

<objective>
Prove MEM-02 live and end-to-end: a chat exchange handled by the bot is written to ClaudeClaw memory and retrieved as relevant context in a later, separate session. This is the one verification that cannot be automated — it requires two real chat sessions across a `/newchat` session boundary against the running bot, with the scoping fix from 04-01 deployed.

Everything automatable was covered in 04-01 (strict per-agent recall, the delegated wiring, the single-store path assertion, full suite at baseline). This plan deploys the 04-01 change, restarts the bot, and captures the two-session transcript.

Purpose: Close MEM-02's live success criterion ("retrieved as relevant context in a later, separate session") with a captured transcript.
Output: A two-session transcript recorded in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memory-source-of-record/04-RESEARCH.md
@.planning/phases/04-memory-source-of-record/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and restart the bot with the 04-01 scoping change deployed</name>
  <files>dist/ (build output), launchd bot service (no source files changed)</files>
  <read_first>
    - .planning/phases/04-memory-source-of-record/04-01-SUMMARY.md (confirm the scoping fix landed and the suite is at baseline)
    - CLAUDE.md (launchd rules: log paths must not contain spaces; diagnose via launchctl print; project root via git rev-parse --show-toplevel)
    - .planning/phases/03-skill-hardening/03-01-SUMMARY.md (prior deploy + restart pattern used in this project)
  </read_first>
  <action>
    Run the project build (npm run build / tsc to dist) so the bot process runs the compiled 04-01 change. Restart the running ClaudeClaw bot service so the delegated recall scoping is live. Use the project's existing restart path (launchd agent for the bot; do NOT use log paths with spaces). Confirm GOOGLE_API_KEY is present in the bot process env so recall is semantic, not keyword-only (per RESEARCH Pitfall 4). After restart, confirm the bot is up and the aos agent is reachable (a trivial @aos: ping). Do not modify source in this task — build + restart only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (dist updated)
    - Bot service restarted and running (launchctl print shows running state, no exit code 78)
    - GOOGLE_API_KEY present in the bot env (semantic recall available)
    - A trivial @aos: ping returns a response
  </acceptance_criteria>
  <done>Bot is running the 04-01 change with semantic recall available; aos reachable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live two-session MEM-02 recall proof</name>
  <files>.planning/phases/04-memory-source-of-record/04-02-SUMMARY.md</files>
  <action>
    Human-verify checkpoint. The delegated recall path is now scoped to the workspace agent (04-01), built and deployed (Task 1). Capture the live two-session MEM-02 proof: a standing preference stated via @aos: in session A is recalled in a later, separate session B after /newchat. Follow the steps in how-to-verify and paste both transcripts into the summary.
  </action>
  <what-built>
    The delegated recall path is now scoped to the workspace agent (04-01), built and deployed to the running bot (Task 1). This checkpoint captures the live two-session MEM-02 proof: a standing preference stated via @aos: in session A is recalled in a later, separate session B after /newchat.
  </what-built>
  <how-to-verify>
    Session A (current chat):
    1. Send: `@aos: remember that I prefer taglines under 5 words`
       (Use a standing PREFERENCE, not "ok thanks" or a one-off task — the extractor only keeps preferences/decisions/standing rules at importance >= 0.5, per RESEARCH Pitfall 5.)
    2. Confirm the agent acknowledges.

    Session B (new session, same chat + aos agent):
    3. Start a fresh session: send `/newchat` (new session_id, same chat_id).
    4. Send: `@aos: draft a tagline for a coffee brand`
    5. Confirm the response reflects the prior preference (tagline is under 5 words, or the agent references the under-5-words rule). The prior fact should surface via the [Memory context] block.

    No-leakage spot check (optional but recommended):
    6. Confirm the aos recall did NOT surface an unrelated memory belonging to another agent for the same chat.

    Capture both sessions' transcripts (the @aos: messages and the agent responses) for the summary.
  </how-to-verify>
  <verify>
    <human-check>Both session transcripts pasted; session B response reflects the session A preference.</human-check>
  </verify>
  <acceptance_criteria>
    - The session B response observably reflects the under-5-words preference stated in session A (recall spanned the /newchat session boundary)
    - The recall surfaced the workspace agent's own memory (no obvious cross-agent leak)
    - Both transcripts captured
  </acceptance_criteria>
  <done>Two-session transcript captured proving cross-session MEM-02 recall; no cross-agent leak observed.</done>
  <resume-signal>Type "approved" with the two transcripts pasted, or describe what failed (e.g., the preference was not recalled).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → shared chat memory store | The live proof must confirm the deployed scoping holds in production: aos recall surfaces only aos memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | live delegated recall after deploy | mitigate | The 04-01 strictAgentId scoping is verified live by the no-leakage spot check in the human-verify checkpoint |
</threat_model>

<verification>
- Build succeeds and bot restarts cleanly (no launchd exit 78)
- Two-session transcript shows recall across the /newchat boundary
- No-leakage spot check passes
</verification>

<success_criteria>
- MEM-02 live: a bot exchange written in session A is recalled as relevant context in a later session B (transcript captured)
- Recall is scoped to the workspace agent (no cross-agent leak observed live)
- Round-trip ran against the single store/claudeclaw.db
</success_criteria>

<output>
Create `.planning/phases/04-memory-source-of-record/04-02-SUMMARY.md` with both session transcripts when done.
</output>
