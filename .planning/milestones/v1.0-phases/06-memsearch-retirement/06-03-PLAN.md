---
phase: 06-memsearch-retirement
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [MEM-05, MEM-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The full ClaudeClaw test suite passes (modulo the documented baseline failures) with the recall-CLI + scoping changes in place (COMPAT-03, success criterion 4)"
    - "Recall works in BOTH modes: a known fact is recalled via the terminal recall-CLI AND recalled by the bot for an @aos: turn (success criteria 2 + 3)"
    - "Terminal-session work written to the shared workspace files (context/MEMORY.md + daily logs) is recalled by a later bot @aos: turn (MEM-04, rescoped: files are the shared layer, no SQLite Stop-hook capture)"
    - "The nightly memsearch index does not fire and no memsearch index process runs (success criterion 1)"
  artifacts: []
  key_links:
    - from: "terminal recall-cli.js"
      to: "bot @aos: recall"
      via: "both hit the same searchMemories(ws:aos, q, k, embed, aos) path"
      pattern: "recall-cli\\.js"
    - from: "terminal session writes to context/MEMORY.md"
      to: "later bot @aos: recall"
      via: "bot reads the shared workspace MEMORY.md (rescoped MEM-04, proven live in 05-02)"
      pattern: "MEMORY\\.md"
---

<rescope_note>
MEM-04 was rescoped on 2026-06-15 (commit 7f62a81, "drop Stop-hook SQLite capture; workspace files are the shared layer"). The Stop-hook SQLite capture (`src/capture-cli.ts`, `dist/capture-cli.js`) was REMOVED as fragile and redundant. The shared layer between terminal and bot is the agentic-os workspace files (`context/MEMORY.md` + daily logs): the terminal writes to them and the bot recalls terminal-set work by reading MEMORY.md (no SQLite capture; proven live in 05-02). The bot→terminal direction stays via the SQLite→`{date}.claudeclaw.md` projection (load-memory-snapshot.js).

This plan therefore PROVES MEM-04 via the workspace-file path, NOT via a capture-cli Stop hook. Any reference below to "capture-cli", "Stop hook capture", or "dist/capture-cli.js" is obsolete and intentionally dropped. The recall-CLI proof (terminal + bot, single SQLite index) and the index-off proof are unchanged.

Paths: this worktree and `main` are the source of record; the live deploy target is `$HOME/.claudeclaw-app` (symlink to the main repo). Run tests in the deploy checkout on `main` (not the stale `sharp-easley-ba9c43` worktree path baked into the original commands below).
</rescope_note>

<objective>
Prove MEM-05 and the rescoped MEM-04 with the FINAL, gated steps (D-05, D-06): run the full suite to confirm no regression, then a live bidirectional round-trip — recall in both modes (terminal recall-CLI + bot @aos:) against the single ClaudeClaw SQLite index, and confirm terminal-set work in the shared workspace files is recalled by the bot — with the memsearch index already disabled.

Purpose: Close the phase with the automated single-index/regression proof plus the human-verified live proof that recall works in both surfaces against the single ClaudeClaw index and the workspace files are the shared capture layer, and the second index is dead.

Output: Full-suite green run + a human-verified bidirectional round-trip transcript captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-memsearch-retirement/06-CONTEXT.md
@.planning/phases/06-memsearch-retirement/06-VALIDATION.md
@.planning/phases/06-memsearch-retirement/06-01-PLAN.md
@.planning/phases/06-memsearch-retirement/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression gate + deploy/restart for the live proof</name>
  <files>dist/recall-cli.js</files>
  <read_first>
    - .planning/phases/06-memsearch-retirement/06-VALIDATION.md (sampling rate; the documented baseline failures to tolerate: dashboard.contract chatId, chat-task-tracker no-key)
    - .planning/phases/04-memory-source-of-record/04-01-SUMMARY.md "Baseline note" (the known unrelated failures so the executor does not mistake them for regressions)
    - Quick sanity check before the full run (focused, fast): `npx vitest run src/recall-cli.test.ts src/memory.test.ts src/db.test.ts src/orchestrator.test.ts` in the deploy checkout on `main` — the deterministic core must be fully green. This is a pre-flight only; the binding gate is the full suite in `<verify>`.
  </read_first>
  <action>
    Deploy: in the live target (`$HOME/.claudeclaw-app`, the main repo), check out `main` (which carries the merged Plan 01 recall-cli + the rescope) and run `npm run build` (vite build && tsc) so `dist/recall-cli.js` exists at the symlink target the agentic-os AGENTS.md Tier-1 recall command points at. Run the full suite with `npx vitest run` and confirm green modulo only the documented baseline failures (dashboard.contract "rejects missing chatId", chat-task-tracker "returns null when classifier fails", and the environmental file-send.integration Telegram test). If any NEW failure touching memory, recall, db, orchestrator, or message-core appears, stop and report it as a regression rather than proceeding. Confirm `dist/recall-cli.js` exists (capture-cli.js is intentionally gone post-rescope — do NOT check for it) and that the `~/.claudeclaw-app` symlink resolves to the repo root so the agentic-os AGENTS.md recall path is reachable. Restart the running bot (the `com.claudeclaw.*` launchd agents) so the recall scoping + recall-CLI changes are live for the round-trip. Before the live proof, confirm no `memsearch index` process is currently running (`pgrep -fl 'memsearch index' || echo none`) and that nightly-memsearch-index.md is `active: 'false'`.
  </action>
  <verify>
    <automated>cd "$HOME/.claudeclaw-app" && npx vitest run && test -f dist/recall-cli.js && echo SUITE_AND_ARTIFACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full suite) shows no NEW failure beyond the documented baseline (dashboard.contract chatId, chat-task-tracker no-key, file-send.integration Telegram); the run is otherwise green
    - The deterministic core (recall-cli, memory, db, orchestrator) is fully green
    - dist/recall-cli.js exists at the ~/.claudeclaw-app symlink target (capture-cli.js intentionally absent post-rescope)
    - `pgrep -fl 'memsearch index'` returns no running index process; nightly cron is active:'false'
    - The bot is restarted so the recall changes are live
  </acceptance_criteria>
  <done>Full suite green (modulo the documented baseline failures), dist/recall-cli.js built and reachable via the symlink, no memsearch index running, bot deployed for the live proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live bidirectional round-trip proof (recall both modes + workspace-file capture), FINAL gate</name>
  <what-built>
    The ClaudeClaw recall-CLI now serves terminal Tier-1 recall against the single SQLite embedding index (Plan 01); AGENTS.md points at it and the nightly memsearch index cron is disabled (Plan 02). Per the MEM-04 rescope, the terminal-to-bot capture path is the shared agentic-os workspace files (context/MEMORY.md + daily logs) — there is no SQLite Stop-hook capture to prove. This is the D-05 live proof, sequenced LAST per D-06: the index is already off and the replacement is in place.
  </what-built>
  <read_first>
    - .planning/phases/06-memsearch-retirement/06-VALIDATION.md "Manual-Only Verifications" table (terminal recall-CLI parity, recall round-trip, nightly index does not fire)
    - .planning/phases/06-memsearch-retirement/06-CONTEXT.md D-05 (both directions required)
    - Rescope commit 7f62a81 (workspace files are the shared layer; the terminal->bot MEMORY.md recall was proven live in 05-02)
  </read_first>
  <action>
    Present the live verification steps below to the user and pause for their observed results. This is a human-verify gate, not autonomous work; the executor does not fabricate the outcome. After the user reports each step, record the observed outputs in the SUMMARY and only mark the phase verified if every acceptance criterion is met.
  </action>
  <how-to-verify>
    Recall direction (both modes, single SQLite index):
    1. Pick a known fact already in ClaudeClaw memory under (ws:aos, aos) (e.g. the standing tagline preference or the Q3-launch fact verified in 04-02/05-02). If unsure, first write a fresh fact via an `@aos:` bot turn (e.g. "remember: the demo ship date is Aug 9").
    2. In a terminal in the agentic-os workspace, run: `node "$HOME/.claudeclaw-app/dist/recall-cli.js" "<query for that fact>"` and confirm the fact comes back in the printed results (terminal recall via the single index).
    3. In the bot, ask the same question with an `@aos:` turn and confirm it recalls the same fact (bot recall via the same index). Both surfaces, one store.

    Capture direction (rescoped MEM-04, shared workspace files):
    4. In a terminal Claude Code session in the agentic-os workspace, record a new fact into the shared workspace memory (e.g. add a line to `context/MEMORY.md` or today's daily log, the way a terminal session normally persists work).
    5. In a later bot `@aos:` turn, ask about that fact and confirm it is recalled (the bot reads the shared workspace MEMORY.md — files are the shared layer; no SQLite Stop-hook capture). This re-confirms the path proven live in 05-02 now that the memsearch index is off.

    Index-off check:
    6. Confirm `cron/jobs/nightly-memsearch-index.md` is `active: 'false'` and that no `memsearch index` process runs (`pgrep -fl 'memsearch index'` returns nothing).

    Report each step's observed output.
  </how-to-verify>
  <acceptance_criteria>
    - Terminal recall-CLI returns the known/seeded fact (success criterion 3)
    - The bot recalls the same fact for an @aos: turn (success criterion 2)
    - Terminal-session work recorded in the shared workspace files (context/MEMORY.md / daily log) is recalled by a later bot @aos: turn (rescoped MEM-04)
    - The nightly memsearch index cron is active:'false' and no memsearch index process is running (success criterion 1)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed outputs, or describe which direction failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal session to bot | Both read/write the same single SQLite store under (ws:aos, aos); the proof confirms one store, two surfaces with no cross-agent leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | live recall surfacing another agent's memory | mitigate | The live proof confirms recall is scoped to (ws:aos, aos); the automated invariant test (Plan 01) already guards the args; this is the runtime confirmation |
| T-06-10 | Tampering | stale dist / symlink drift breaking the live recall | mitigate | Task 1 confirms dist/recall-cli.js exists and the symlink resolves before the human-verify gate; AGENTS.md uses the symlink path |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs in this phase (Package Legitimacy Audit: zero registry interaction); memsearch remains dormant, not removed |
</threat_model>

<verification>
- `npx vitest run` (full suite) green modulo the 2 documented baseline failures
- Live terminal recall-CLI returns a known fact; bot recalls the same fact (@aos:)
- Terminal capture via committed Stop hook recalled by a later bot @aos: turn
- nightly-memsearch-index.md active:'false'; no memsearch index process running
</verification>

<success_criteria>
- All 4 ROADMAP Phase-6 success criteria are observably true (index off, both-mode recall, terminal equivalent recall, suite green/no regression)
- MEM-04 is durably closed (committed capture hook proven by round-trip)
- MEM-05 is closed: one semantic index, two surfaces, second index dead
</success_criteria>

<output>
Create `.planning/phases/06-memsearch-retirement/06-03-SUMMARY.md` when done
</output>
