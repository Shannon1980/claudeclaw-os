---
phase: 02-routines
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - web/src/pages/Routines.tsx
  - web/src/components/RoutineRow.tsx
  - web/src/components/RoutineDetail.tsx
  - web/src/components/StepList.tsx
  - web/src/components/StepRow.tsx
  - web/src/components/TeammateTag.tsx
  - web/src/components/RunHistoryItem.tsx
  - web/src/components/RoutineBuilderPanel.tsx
  - web/src/components/AutonomySelector.tsx
  - web/src/components/RunOutcomeBadge.tsx
  - web/src/lib/routes.ts
autonomous: false
requirements: [RTN-01, RTN-02, RTN-03, RTN-04, RTN-05]
user_setup: []
must_haves:
  truths:
    - "An operator opens a Routines page showing each routine's plain-language schedule, step count, latest outcome, and an on/off toggle (RTN-02, RTN-04)"
    - "An operator describes a routine in an embedded conversational builder, reviews an editable draft, and saves — nothing persists until Save (RTN-01, D-04, D-05)"
    - "An operator reviews/edits ordered steps, each with a named teammate tag (RTN-03)"
    - "An operator turns a routine on/off and runs it now from the UI (RTN-04)"
    - "Run history shows ok/degraded/failed honestly with View output; an at-creation autonomy selector is visible and stored (RTN-05, D-07)"
    - "No raw cron appears anywhere in the operator path (RTN-02, D-06)"
  artifacts:
    - path: "web/src/pages/Routines.tsx"
      provides: "Routines list page + embedded builder, registered route"
      min_lines: 60
    - path: "web/src/components/RoutineBuilderPanel.tsx"
      provides: "draft-first conversational builder with autonomy selector"
    - path: "web/src/components/RunOutcomeBadge.tsx"
      provides: "ok/degraded/failed Pill+icon per the color contract"
  key_links:
    - from: "web/src/pages/Routines.tsx"
      to: "/api/routines"
      via: "useFetch + apiPost/apiDelete"
      pattern: "/api/routines"
    - from: "web/src/components/RoutineRow.tsx"
      to: "web/src/lib/cron.ts"
      via: "describeCron for plain-language schedule"
      pattern: "describeCron"
    - from: "web/src/components/TeammateTag.tsx"
      to: "web/src/pages/Team.tsx"
      via: "teammateColor()"
      pattern: "teammateColor"
    - from: "web/src/lib/routes.ts"
      to: "web/src/pages/Routines.tsx"
      via: "route entry for the Routines surface"
      pattern: "Routines|/routines|/scheduled"
---

<objective>
Slice C — the operator-facing Routines surface. Build web/src/pages/Routines.tsx plus its components (RoutineRow, RoutineDetail, StepList/StepRow, TeammateTag, RunHistoryItem, RoutineBuilderPanel, AutonomySelector, RunOutcomeBadge) against the live /api/routines* endpoints, and register the route. After this plan an operator can stand up a routine by describing it, review/edit the draft step list with teammate tags, save it, toggle it on/off, run it now, and see honest ok/degraded/failed run history — the full Phase 2 user story, end to end.

Purpose: This is the visible product. It must reuse the existing hand-rolled token system and components (no shadcn) per the UI-SPEC, render plain-language schedules via describeCron (never raw cron, D-06), present the at-creation autonomy selector deliberately (D-07), and keep the draft unsaved until confirm (D-05).
Output: The Routines page + 9 components + the route entry, plus a human-verify checkpoint for the non-deterministic builder UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routines/02-UI-SPEC.md
@.planning/phases/02-routines/02-RESEARCH.md
@.planning/phases/02-routines/02-PATTERNS.md
@.planning/phases/02-routines/02-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routines page, list/detail/run components, and route entry</name>
  <files>web/src/pages/Routines.tsx, web/src/components/RoutineRow.tsx, web/src/components/RoutineDetail.tsx, web/src/components/StepList.tsx, web/src/components/StepRow.tsx, web/src/components/TeammateTag.tsx, web/src/components/RunHistoryItem.tsx, web/src/components/RunOutcomeBadge.tsx, web/src/lib/routes.ts</files>
  <read_first>
    - web/src/pages/Scheduled.tsx (whole page — the exact skeleton to model: imports PageHeader/Pill/PageState/ConfirmModal/useFetch/apiPost/apiDelete/formatRelativeTime/term/pushToast/describeCron ~:1-15; useFetch('/api/...', 30_000) ~:53; formatCountdown ~:33-40; card layout)
    - web/src/pages/Team.tsx (teammateColor() ~:58-65 — reuse verbatim for TeammateTag; AgentAvatar usage)
    - web/src/components/ScheduleBuilder.tsx (props { cron, onChange, externalError } ~:20-25; the "Advanced (cron)" toggle ~:226-233 — the D-06 escape hatch, never default-shown)
    - web/src/lib/cron.ts (describeCron(cron).text ~:195 — plain-language for every row/detail)
    - web/src/lib/routes.ts (ROUTES table ~:27-42 — nav.routines vocabKey currently on /scheduled at :30)
    - web/src/lib/vocabulary.ts (term(); the nav.routines / page.routines keys)
    - .planning/phases/02-routines/02-UI-SPEC.md (§1 List view, §2 Expanded detail, §5 run-outcome color/icon/copy contract, Color table, Copywriting Contract)
    - .planning/phases/02-routines/02-PATTERNS.md (§web/src/pages/Routines.tsx + components)
  </read_first>
  <action>ROUTE SLOT DECISION (explicit, per planning landmine): nav.routines currently sits on /scheduled (routes.ts:30). Replace that slot — change the path to /routines, label to 'Routines', icon to a routine-appropriate lucide icon (keep ListTodo or use Repeat/CalendarClock), keeping vocabKey 'nav.routines'; point it at the new Routines.tsx page. (Do not leave a dangling /scheduled.) Create web/src/pages/Routines.tsx modeled on Scheduled.tsx: load via useFetch('/api/routines', 30_000); render PageHeader title term('page.routines') with the count line "{onCount} on, {offCount} off" (tabular-nums, on/off split not a raw total) and an accent "New routine" button that opens the RoutineBuilderPanel (Task 2) inline — it must NOT navigate to Chat (D-04); PageState for loading/error/empty (empty copy from the Copywriting Contract). Build the components: RoutineRow (icon+name 12.5px medium, plain-language schedule via describeCron(...).text with a Clock icon — NEVER raw cron, step-count+last/next meta with accent countdown when on, RunOutcomeBadge for the latest run, a size='sm' Toggle as the primary non-destructive on/off control calling POST /api/routines/:id/pause|resume with NO confirm modal, chevron to expand RoutineDetail; off routines get opacity-50 on content but the toggle stays full-opacity); RoutineDetail (four .section-label blocks — When: describeCron text + a "Change" action opening ScheduleBuilder whose Advanced toggle is the only raw-cron surface; Steps: StepList; Recent runs: RunHistoryItem list; Actions: "Run now" → POST /api/routines/:id/run with pushToast "Run started" on success / "already running" on 409, and "Turn off"); StepList + StepRow (step number + action text + TeammateTag, add/reorder affordances; a step whose teammate is paused shows a muted "teammate paused" note); TeammateTag (reuse teammateColor(id) from Team.tsx + AgentAvatar, soft chip color-mix background); RunHistoryItem (RunOutcomeBadge + formatRelativeTime timestamp + "View output" opening the stored run output verbatim — failures shown honestly, never collapsed to a generic error); RunOutcomeBadge (Pill tone done/medium/failed + lucide Check/AlertTriangle/XCircle per the color table: ok=done/green, degraded=medium/amber, failed=failed/red). All labels through term(); all colors via var(--color-*) tokens; no shadcn; no em dashes in shipped copy (CLAUDE.md). Delete-routine uses ConfirmModal (destructive). DeleteRoutine via apiDelete('/api/routines/:id').</action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -rEl 'describeCron' web/src/components/RoutineRow.tsx && echo DESCRIBECRON_USED && { grep -REn '[0-9*]+ [0-9*]+ [0-9*]+ [0-9*]+ [0-9*]+' web/src/pages/Routines.tsx web/src/components/RoutineRow.tsx web/src/components/RoutineDetail.tsx | grep -v 'describeCron\|ScheduleBuilder\|//' && echo RAW_CRON_LEAK_FOUND && exit 1 || echo NO_RAW_CRON_LEAK; }</automated>
  </verify>
  <acceptance_criteria>
    - web/src/lib/routes.ts has a route entry pointing at the Routines page with vocabKey 'nav.routines' and no remaining /scheduled path (grep confirms /routines present, /scheduled absent)
    - web/src/components/RoutineRow.tsx imports and calls describeCron (grep confirms) and does not render the raw task.schedule cron string in operator-facing text
    - web/src/components/TeammateTag.tsx references teammateColor (grep confirms)
    - web/src/components/RunOutcomeBadge.tsx maps ok/degraded/failed to Pill tones done/medium/failed (grep confirms all three)
    - `npm run build` (vite) succeeds with the new page/components compiled
    - No em dash character (—) appears in shipped string literals in the new files (grep -L confirms)
  </acceptance_criteria>
  <done>The Routines list/detail surface renders against /api/routines*, shows plain-language schedules + honest outcomes, on/off + run-now work, the route is registered, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Conversational builder panel + at-creation autonomy selector</name>
  <files>web/src/components/RoutineBuilderPanel.tsx, web/src/components/AutonomySelector.tsx, web/src/pages/Routines.tsx</files>
  <read_first>
    - web/src/pages/Routines.tsx (Task 1 — wire the builder panel open/close + refresh-on-save here)
    - web/src/components/ScheduleBuilder.tsx (reuse for draft schedule edit + the Advanced raw-cron hatch)
    - web/src/components/StepList.tsx (Task 1 — reuse for the editable draft step list)
    - web/src/pages/Scheduled.tsx (apiPost usage pattern, pushToast, ConfirmModal)
    - .planning/phases/02-routines/02-UI-SPEC.md (§3 builder D-04/D-05, §4 AutonomySelector D-07, Copywriting Contract: seed prompt, draft pill, discard-draft confirm, autonomy explainers)
    - .planning/phases/02-routines/02-CONTEXT.md (D-04 embedded panel not Chat; D-05 draft-first nothing persists until confirm; D-07 visible autonomy; D-06 no raw cron default)
    - .planning/phases/02-routines/02-RESEARCH.md (Pattern 5 draft-first; the /api/routines/draft → /api/routines confirm flow)
  </read_first>
  <action>Create web/src/components/RoutineBuilderPanel.tsx: an embedded panel that expands above the list ON the Routines page (NOT a hand-off to Chat — D-04). It POSTs the operator's plain-language description to /api/routines/draft (apiPost) and renders the returned { schedule_text, cron, steps[] } as an EDITABLE draft using the SAME StepList and ScheduleBuilder surfaces as detail. Mark the draft visually unsaved (a "Draft, not saved yet" Pill tone accent or dashed border) so nothing reads as live (D-05). The operator can edit step text, reassign teammates, reorder, add/remove steps, and adjust the schedule before anything saves. "Save routine" (accent) POSTs to /api/routines (description→persist) then refreshes the list and pushToast "Routine saved"; Cancel/discard a dirty draft routes through ConfirmModal (destructive: the discard-draft copy). NOTHING is written until Save — the draft endpoint persisted nothing (D-05). Create web/src/components/AutonomySelector.tsx: a two-option segmented control (active = bg-[var(--color-accent)] text-white) "Run unattended" vs "Queue for my approval", each with its one-line plain explainer (Copywriting Contract autonomy strings), rendered VISIBLY inside the builder above "Save routine" (D-07 — deliberate, not buried). The selected machine value (unattended | queue_approval, NOT the display label — Pitfall 5 forward-compat) is included in the POST /api/routines body and surfaced read-only as a small Pill in RoutineDetail. Make clear in copy that the routine "won't send, pay, or commit on its own" for unattended — but do NOT imply enforcement exists this phase (D-08 honesty; enforcement is Phase 3). All copy plain operator language, no cron, no em dashes.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "/api/routines/draft" web/src/components/RoutineBuilderPanel.tsx && grep -Eq "unattended|queue_approval" web/src/components/AutonomySelector.tsx && echo BUILDER_OK</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/RoutineBuilderPanel.tsx POSTs to /api/routines/draft and, only on Save, POSTs to /api/routines (grep confirms both endpoints, with the draft call separate from the persist call)
    - The builder reuses StepList and ScheduleBuilder (grep confirms imports) — it does not reimplement step editing or the schedule picker
    - web/src/components/AutonomySelector.tsx emits the machine values unattended / queue_approval (not display labels) (grep confirms)
    - The AutonomySelector is rendered inside RoutineBuilderPanel above the Save action (visible at creation, D-07)
    - `npm run build` succeeds; no em dash in shipped strings
  </acceptance_criteria>
  <done>The embedded draft-first builder assembles a routine from a description, lets the operator edit the draft (persisting nothing until Save), captures the at-creation autonomy choice as a forward-compatible machine value, and saves via /api/routines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the end-to-end Routines experience</name>
  <what-built>The full Routines surface: list page with plain-language schedules + on/off toggles + run-outcome badges, expandable detail with ordered steps/teammate tags/recent runs/run-now, the embedded conversational draft-first builder, and the at-creation autonomy selector — all wired to /api/routines* and the multi-step runner.</what-built>
  <how-to-verify>
    1. Start the dev dashboard (npm run dev or the app window) and open the Routines page (sidebar item, route /routines).
    2. Click "New routine" and describe: "every weekday at 8, check my calendar and inbox and send me a brief, then chase overdue invoices". Confirm a draft appears with a plain-language schedule ("Every weekday at 8:00am") and an ordered step list, each step showing a teammate tag. Confirm the draft is marked unsaved and that NO routine appears in the list yet.
    3. Edit a step's text, reassign a teammate, and reorder a step in the draft. Confirm edits hold.
    4. Pick an autonomy option (Run unattended / Queue for my approval) — confirm it is clearly visible above Save. Click "Save routine". Confirm the routine now appears in the list and the count line updates.
    5. Confirm the list row and detail show ONLY plain-language schedule — no raw cron (0 8 * * 1-5) anywhere. The only place cron may appear is behind the ScheduleBuilder "Advanced (cron)" toggle.
    6. Toggle the routine off (row should dim, no confirm modal), then back on.
    7. Click "Run now" — confirm a "Run started" toast. After it runs, expand detail and confirm Recent runs shows an ok/degraded/failed badge with a timestamp and a working "View output". If a step's teammate is paused, confirm the run reads degraded with the honest reason, not a silent success.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (raw cron leak, draft persisting early, missing autonomy selector, dishonest run history, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → /api/routines* | The page is a thin client over the authenticated API; all writes go through the validated server routes (02-03) |
| rendered run output → operator screen | Stored run/step output text is displayed in "View output" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | Raw cron leaking into the operator path (D-06/RTN-02 violation) | mitigate | Every row/detail renders describeCron(...).text; raw cron only inside ScheduleBuilder's Advanced toggle; checkpoint step 5 verifies no leak |
| T-02-13 | Integrity | Draft persisting before the operator confirms (D-05 violation) | mitigate | Builder calls /api/routines/draft (no persist) and only POSTs /api/routines on Save; checkpoint step 2 verifies nothing appears pre-Save |
| T-02-14 | Tampering (XSS) | Run output / step text rendered as raw HTML | mitigate | Preact escapes text bindings by default; render output as text, never dangerouslySetInnerHTML |
| T-02-04c | Access Control | Autonomy selector implying enforced protection | accept (this phase) | Copy states behavior intent only; D-08 enforcement is Phase 3 — UI must not claim a guarantee it cannot keep |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Gate: N/A); lucide-preact + Preact already present |
</threat_model>

<verification>
- `npm run build` (vite) green with the new page + 9 components + route entry
- No raw cron string in operator-facing render paths (describeCron only); raw cron only behind ScheduleBuilder Advanced
- No em dash in shipped copy (CLAUDE.md)
- Human-verify checkpoint approved: builder draft-first, autonomy visible, honest run history, on/off + run-now functional
</verification>

<success_criteria>
- Operator can create a routine by describing it, review/edit the draft (nothing persists until Save), and save with an at-creation autonomy choice
- List + detail show plain-language schedules, teammate-tagged ordered steps, and honest ok/degraded/failed history with View output
- On/off and run-now work from the UI; no raw cron in the operator path
- Full Phase 2 user story verified end to end by the operator
</success_criteria>

<output>
Create `.planning/phases/02-routines/02-04-SUMMARY.md` when done
</output>
