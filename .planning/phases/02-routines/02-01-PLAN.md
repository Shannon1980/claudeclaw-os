---
phase: 02-routines
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/routine-runner.test.ts
  - src/routine-draft.test.ts
  - src/db.test.ts
  - src/dashboard.contract.test.ts
  - src/scheduler.test.ts
autonomous: true
requirements: [RTN-01, RTN-02, RTN-03, RTN-04, RTN-05]
must_haves:
  truths:
    - "Every Phase 2 behavior has a failing test that pins its contract before implementation"
    - "deriveOutcome ok/degraded/failed (incl. all-continue-fail → failed) is asserted"
    - "A routine claims exactly once per run (no double-fire) is asserted"
    - "State-change notify fires once on ok→broken, never on subsequent breaks (D-10) is asserted"
    - "Draft assembly parses NL→{cron,steps[]} and persists nothing until confirm is asserted"
  artifacts:
    - path: "src/routine-runner.test.ts"
      provides: "deriveOutcome, step threading, paused-teammate skip, one-claim, notify-transition tests"
      min_lines: 80
    - path: "src/routine-draft.test.ts"
      provides: "parseJsonLoose, agent_id validation, cron validity tests"
      min_lines: 40
  key_links:
    - from: "src/routine-runner.test.ts"
      to: "src/routine-runner.ts"
      via: "import deriveOutcome, runRoutineOnce"
      pattern: "from '.*routine-runner'"
    - from: "src/db.test.ts"
      to: "src/db.ts"
      via: "_initTestDatabase + routine CRUD"
      pattern: "routine_steps|routine_runs|autonomy"
---

## Phase Goal

**As a** business operator, **I want to** stand up multi-step work that runs on its own by describing it in plain language and then review, control, and trust its run history, **so that** standing work (the morning brief, the invoice chase) happens reliably without me wiring cron jobs or babysitting it.

> MVP note: the ROADMAP `**Goal:**` line for Phase 2 carries this role/capability/outcome in prose, not in strict user-story form. The story above is derived directly from that Goal line; if the operator wants different framing, re-run `/gsd mvp-phase 2`. The phase is planned as vertical slices — Wave 0 (this plan) lays the failing-test safety net, then each subsequent slice (engine → API → UI) leaves a real, demonstrable capability behind.

<objective>
Wave 0 test scaffolding for Phase 2 Routines. Author the failing tests that pin every requirement contract (RTN-01..RTN-05) before any implementation lands, per the VALIDATION.md Wave 0 list. These tests are the MVP happy-path safety net: the engine, API, and UI plans (02-02, 02-03, 02-04) exist to turn these red tests green.

Purpose: Nyquist compliance — no behavior ships without an automated check. The two load-bearing invariants (claim-once anti-double-fire, notify-on-state-change-only) and the D-02 outcome derivation are encoded here so the implementer satisfies a spec, not a vibe.
Output: Five test files/extensions, all RED (they import symbols and tables that 02-02/02-03 create).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routines/02-VALIDATION.md
@.planning/phases/02-routines/02-RESEARCH.md
@.planning/phases/02-routines/02-PATTERNS.md
@.planning/phases/02-routines/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author routine-runner + routine-draft engine tests (RED)</name>
  <files>src/routine-runner.test.ts, src/routine-draft.test.ts</files>
  <read_first>
    - .planning/phases/02-routines/02-RESEARCH.md (Pattern 1, 2, 6; Validation Architecture → Test Map; deriveOutcome contract D-02; notify-transition D-10)
    - .planning/phases/02-routines/02-PATTERNS.md (§routine-runner.ts analogs A/B; §routine-draft.ts parseJsonLoose; Tests (Wave 0) → AosFireDeps injection pattern)
    - src/scheduler.test.ts (the AosFireDeps dependency-injection test pattern + parseTimeout-style pure-fn tests — model the new tests on this file's structure)
    - .planning/phases/02-routines/02-CONTEXT.md (D-01 on_error flag, D-02 outcome derivation, D-03 step threading, D-10 state-change-only alert)
  </read_first>
  <behavior>
    routine-runner.test.ts:
    - deriveOutcome: every step ok → 'ok'
    - deriveOutcome: a continue-on-error step failed but another step produced useful output AND run completed → 'degraded'
    - deriveOutcome: a stop-on-error step failed (halted=true) → 'failed'
    - deriveOutcome edge: ALL steps continue-on-error AND all fail (no useful output) → 'failed' (NOT degraded)
    - runRoutineOnce: steps execute in step_order; later step's prompt contains earlier step output (D-03 threading) — assert via a mocked delegateToAgent that records the prompts it received
    - runRoutineOnce: a step whose teammate isAgentPaused is recorded as a skip and contributes to a degraded outcome (A4)
    - runRoutineOnce: delegateToAgent (and any claim) is invoked exactly once per routine run, never once-per-step (claim-once invariant) — assert the injected claim/delegate call counts
    - notify-transition: sender fires once when prior outcome is 'ok' (or null) and new outcome is 'degraded'/'failed'; sender does NOT fire when prior is already 'failed'/'degraded' and new is failing (D-10)
    routine-draft.test.ts:
    - parseJsonLoose strips ```json fences and parses; regex-fallback extracts the first {...} block from prose; returns null/throws friendly on no JSON (Pitfall 4)
    - assembleRoutineDraft validates each step agent_id against a mocked listAgentIds roster; unknown id falls back to 'main'
    - assembleRoutineDraft (or its validator) rejects an assembled cron that computeNextRun cannot parse (cron-valid case)
    - runAgent is mocked in all draft tests (no live SDK subprocess)
  </behavior>
  <action>Create src/routine-runner.test.ts and src/routine-draft.test.ts as vitest suites covering the behaviors above. Import the not-yet-existing symbols deriveOutcome, runRoutineOnce, StepResult from '../src/routine-runner' and assembleRoutineDraft, parseJsonLoose from '../src/routine-draft' (relative path matching the repo's existing test imports). Use vitest's vi.fn() to inject deps (sender, delegateToAgent, claim, isAgentPaused, runAgent, listAgentIds) exactly as scheduler.test.ts injects AosFireDeps — do not hit real subprocesses or the real DB. Tests MUST be RED now (the modules do not exist yet); name the tests so the VALIDATION.md -t filters match: include the substrings 'outcome', 'teammate', 'notify-transition', 'cron-valid' in the relevant test titles. Do not write production code in this plan.</action>
  <verify>
    <automated>npx vitest run src/routine-runner.test.ts src/routine-draft.test.ts 2>&1 | grep -Eiq 'cannot find module|failed|no test' && echo RED_AS_EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - src/routine-runner.test.ts contains test titles matching /outcome/, /teammate/, /notify-transition/ (grep -lE confirms)
    - src/routine-draft.test.ts contains a test title matching /cron-valid/
    - Both files import from '../src/routine-runner' and '../src/routine-draft' respectively
    - Running the suite reports failures or missing-module errors (RED), proving no implementation is being graded as green
    - No production source file (src/routine-runner.ts, src/routine-draft.ts) is created by this task
  </acceptance_criteria>
  <done>Both engine test files exist, are RED, and encode D-02/D-03/D-10 + draft parsing contracts with injected mocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend db, dashboard-contract, and scheduler tests (RED)</name>
  <files>src/db.test.ts, src/dashboard.contract.test.ts, src/scheduler.test.ts</files>
  <read_first>
    - src/db.test.ts (the _initTestDatabase() real-in-memory-SQLite pattern; existing scheduled_tasks round-trip tests to mirror)
    - src/dashboard.contract.test.ts (the GET /api/tasks block ~:276-298, the get() helper ~:37, TOKEN/Q constants ~:24-25, _initTestDatabase() in beforeEach ~:33)
    - src/scheduler.test.ts (existing source-branch firing tests + AosFireDeps; the CONCERNS.md double-claim gap this closes)
    - .planning/phases/02-routines/02-PATTERNS.md (§Tests (Wave 0): exact analog citations for each extension)
    - .planning/phases/02-routines/02-RESEARCH.md (Companion-table schema; /api/routines* route shapes; claim-once branch)
  </read_first>
  <behavior>
    db.test.ts (extend, using _initTestDatabase()):
    - routine_steps round-trips: insert ordered steps, read back ordered by step_order, with agent_id + on_error fields
    - routine_runs round-trips: saveRoutineRun then read outcome + step_results (JSON) + output + ran_at
    - scheduled_tasks.autonomy column round-trips a stored value (default 'unattended')
    - getLastRoutineOutcome returns the most recent run's outcome ordered by ran_at DESC, null when none
    dashboard.contract.test.ts (extend, mirror GET /api/tasks block):
    - GET /api/routines returns { routines: [...] } shape, auth-gated by the existing token
    - POST /api/routines/draft returns a JSON draft and writes NO rows (assert routine count unchanged after the call) — the draft-does-not-persist assertion (D-05)
    - POST /api/routines/:id/run returns 409 when the routine is already claimed/running
    scheduler.test.ts (extend):
    - a source='routine' due task drives the routine runner branch (not the single-prompt path)
    - the routine is claimed exactly once (claim-once); a paused owner agent skips the routine
  </behavior>
  <action>Extend the three existing test files with the behaviors above. For db.test.ts, use the existing _initTestDatabase() helper and add a describe block 'routine' (so the VALIDATION.md `-t routine` filter matches) exercising getRoutineSteps/saveRoutineRun/getLastRoutineOutcome and the autonomy column — these CRUD symbols do not exist yet, so the block is RED. For dashboard.contract.test.ts, copy the GET /api/tasks helpers/setup and add a describe 'routines' block asserting the route shapes, auth gate, and the draft-does-not-persist count assertion. For scheduler.test.ts, add a 'routine' test that injects deps and asserts the source==='routine' branch claims once and respects a paused owner. All new blocks MUST be RED. Do not write production code in this plan.</action>
  <verify>
    <automated>npx vitest run src/db.test.ts src/dashboard.contract.test.ts src/scheduler.test.ts 2>&1 | grep -Eiq 'fail|not a function|undefined|no such (table|column)' && echo RED_AS_EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - src/db.test.ts has a describe/test block whose title matches /routine/ exercising routine_steps, routine_runs, and the autonomy column
    - src/dashboard.contract.test.ts has a describe/test block whose title matches /routines/ including an assertion that the routine count is unchanged after POST /api/routines/draft
    - src/scheduler.test.ts has a test whose title matches /routine/ asserting claim-once and paused-owner skip
    - The combined run reports failures (RED) attributable to missing CRUD fns / tables / routes, not to test-harness errors
    - No production source is modified by this task
  </acceptance_criteria>
  <done>All three test files are extended, RED, and lock the DB CRUD, API contract (incl. draft-no-persist), and scheduler claim-once behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → none | Wave 0 authors tests only; no runtime trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Test fixtures masking real behavior | mitigate | Tests inject mocks via the established AosFireDeps pattern and assert call counts/shapes, not internal state; the draft-does-not-persist test asserts an observable row count, not a stub |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Gate: N/A); vitest already present |
</threat_model>

<verification>
- `npx vitest run src/routine-runner.test.ts src/routine-draft.test.ts src/db.test.ts src/dashboard.contract.test.ts src/scheduler.test.ts` runs and reports RED (expected pre-implementation)
- VALIDATION.md `-t` filter substrings (outcome, teammate, notify-transition, cron-valid, routine, routines) all resolve to authored test titles
</verification>

<success_criteria>
- All five Wave 0 test files exist or are extended and are RED
- Every RTN-01..RTN-05 behavior from the VALIDATION.md Test Map has a corresponding authored test
- No production code written; downstream plans 02-02/02-03/02-04 turn these green
</success_criteria>

<output>
Create `.planning/phases/02-routines/02-01-SUMMARY.md` when done
</output>
