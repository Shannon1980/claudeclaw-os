---
phase: 02-skills-over-chat
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-skills-over-chat/sk01-sk02-sk03-transcripts.md
autonomous: false
requirements: [SK-01, SK-02, SK-03]
user_setup:
  - service: slack
    why: "Belt-and-suspenders delivery route for the aos agent (D-1b) — give aos a routed slack_channel so file turns can also flow through the main marker-extracting path. The channel ID can only come from a human (create/pick a Slack channel, read its ID)."
    dashboard_config:
      - task: "Create or pick a Slack channel for the aos agent, invite the ClaudeClaw Slack app to it, and copy the channel ID (Cxxxxxxxx)"
        location: "Slack → channel → View channel details → bottom → Channel ID; ensure the bot is a member"
must_haves:
  truths:
    - "The aos agent lists/invokes agentic-os workspace methodology skills (mkt-*/str-*/viz-*/meta-*) over chat (SK-01)"
    - "A representative brand/marketing skill (mkt-copywriting or mkt-ugc-scripts) returns an on-brand result over chat, judged against agentic-os brand_context/voice-profile.md (SK-02)"
    - "viz-excalidraw-diagram renders a real PNG and it arrives as a Slack/Telegram attachment (not a raw [SEND_PHOTO:] string) via the aos agent (SK-03)"
    - "aos has a slack_channel in its agent.yaml and the bot was restarted (D-1b belt-and-suspenders)"
    - "The default fleet (agents not pointed at the workspace) is unaffected"
  artifacts:
    - path: ".planning/phases/02-skills-over-chat/sk01-sk02-sk03-transcripts.md"
      provides: "Live chat transcripts proving SK-01, SK-02, SK-03 end to end"
      min_lines: 30
    - path: "~/.claudeclaw/agents/aos/agent.yaml"
      provides: "slack_channel field set to the user-supplied channel ID"
      contains: "slack_channel"
  key_links:
    - from: "aos agent (cwd = agentic-os)"
      to: "viz-excalidraw-diagram/references/render_excalidraw.py"
      via: "uv run (offline after venv pre-warm)"
      pattern: "render_excalidraw"
    - from: "delegated/channel-routed aos reply"
      to: "Slack/Telegram attachment"
      via: "bracketed [SEND_PHOTO:<abs path>] marker → extractFileMarkers → cb.sendPhoto"
      pattern: "SEND_PHOTO"
---

<objective>
Prove the phase goal live: the aos workspace agent discovers and invokes agentic-os methodology skills (SK-01), returns an on-brand brand/marketing result (SK-02), and delivers a real file (a PNG from `viz-excalidraw-diagram`) as a chat attachment (SK-03) — all over Slack/Telegram. Also apply the belt-and-suspenders config from locked decision D-1b: give `aos` a `slack_channel` (the channel ID must come from the user).

This plan depends on 02-01 (the delegation marker-extraction fix) because the `aos` agent is reached via `@aos:` delegation today; SK-03 over that route is only deliverable once the delegation branch extracts markers. The `slack_channel` from D-1b is the secondary/redundant route (the main path already extracts markers).

Per locked decisions: SK-03 proof skill is `viz-excalidraw-diagram` (local Playwright PNG, no API key, no network at render — D-2); the agent emits the BRACKETED marker form because the workspace path contains a space in "App Repo" and the bare form truncates (D-3); pre-warm the skill `.venv` once (D-2); one fresh SK-02 transcript suffices (D-4).

Purpose: This is the observable, user-facing proof that skills work over chat including file delivery — the whole point of Phase 2.
Output: A transcripts artifact capturing SK-01/SK-02/SK-03 evidence; `aos` agent.yaml updated with a `slack_channel`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skills-over-chat/02-RESEARCH.md
@.planning/phases/02-skills-over-chat/02-01-SUMMARY.md
@.planning/phases/01-afternoon-win-point-agent-at-workspace/ws02-ws03-transcripts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-warm the excalidraw skill venv (offline render thereafter)</name>
  <files>(no repo files — creates the skill's references/.venv under agentic-os)</files>
  <read_first>
    - .planning/phases/02-skills-over-chat/02-RESEARCH.md "Pitfall 4" and "Environment Availability" (the `.venv` is absent; first run triggers `uv sync` which hits PyPI; `uv` is NOT on the deny list; chromium is already cached)
    - .planning/phases/02-skills-over-chat/02-RESEARCH.md "Pitfall 5" (the SKILL.md has an inconsistent venv path — the skill lives under the WORKSPACE `.claude/skills/`, not `~/.claude/`; use the workspace-relative path)
  </read_first>
  <action>
    Pre-warm the `viz-excalidraw-diagram` skill venv once so the live SK-03 render does no in-turn network. Run `uv sync` then `uv run playwright install chromium` (chromium is already cached, so the second is a fast no-op) from the skill's `references` directory at `/Users/shannongueringer/App Repo/agentic-os/.claude/skills/viz-excalidraw-diagram/references`. PyPI is not deny-listed (D-2), so this is allowed. After this, `render_excalidraw.py` runs fully offline. Do NOT edit the agentic-os skill files (host-agnostic; Pitfall 5 is a workspace doc gap to flag, not fix here).
  </action>
  <verify>
    <automated>test -d "/Users/shannongueringer/App Repo/agentic-os/.claude/skills/viz-excalidraw-diagram/references/.venv" && echo VENV_OK</automated>
  </verify>
  <done>`references/.venv` exists for the excalidraw skill; a subsequent render does not require network. SUMMARY notes the pre-warm command output and confirms chromium was already cached.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User supplies aos Slack channel ID + restart (D-1b)</name>
  <files>~/.claudeclaw/agents/aos/agent.yaml</files>
  <what-built>The delegation marker fix (plan 02-01) already makes `@aos:` file delivery work. This task adds the redundant channel route from locked decision D-1b: a `slack_channel` on the aos agent.</what-built>
  <action>
    Human-action checkpoint (no code Claude can do unaided — the channel ID lives only in the user's Slack workspace). The user must:
    1. In Slack, create or pick a channel for the aos agent.
    2. Invite the ClaudeClaw Slack app to that channel (it must be a member to route).
    3. Copy the channel ID (Slack → channel → View channel details → bottom → Channel ID, format `Cxxxxxxxx`).
    4. Provide the channel ID. Claude then adds `slack_channel: <ID>` to `~/.claudeclaw/agents/aos/agent.yaml` and restarts the bot (the channel map + runtimeCache are built once at startup — a restart is required per 02-RESEARCH Runtime State Inventory). Per D-1b this is belt-and-suspenders; the 02-01 delegation fix already covers SK-03, so "skip channel" is an acceptable resume.
  </action>
  <human-action>
    1. In Slack, create or pick a channel for the aos agent.
    2. Invite the ClaudeClaw Slack app to that channel (it must be a member to route).
    3. Copy the channel ID (Slack → channel → View channel details → bottom → Channel ID, format `Cxxxxxxxx`).
    4. Provide the channel ID. Claude will then add `slack_channel: <ID>` to `~/.claudeclaw/agents/aos/agent.yaml` and restart the bot.
  </human-action>
  <how-to-verify>
    After Claude writes the channel ID and restarts: send a plain message in that Slack channel and confirm the aos agent responds there (proving the channel routes to the workspace agent). The agent.yaml now contains `slack_channel:` with the supplied ID.
  </how-to-verify>
  <verify>
    <human-check>agent.yaml contains `slack_channel:` with the supplied ID and the aos agent replies in that Slack channel after restart — OR the user chose "skip channel" and the @aos: delegation route is relied upon.</human-check>
  </verify>
  <done>Either `~/.claudeclaw/agents/aos/agent.yaml` has `slack_channel:` set to the user-supplied ID and the bot was restarted and replies in that channel, or the user explicitly chose to skip the channel.</done>
  <resume-signal>Reply with the channel ID (Cxxxxxxxx), or "skip channel" to rely solely on the `@aos:` delegation route (the 02-01 fix already covers SK-03 either way).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live SK-01 / SK-02 / SK-03 proof + capture transcripts</name>
  <files>.planning/phases/02-skills-over-chat/sk01-sk02-sk03-transcripts.md</files>
  <what-built>
    The delegation file-marker fix (02-01) plus the pre-warmed excalidraw venv (Task 1) and optional aos channel (Task 2) make a full skills-over-chat round trip possible: discover a skill (SK-01), run a brand skill on-brand (SK-02), and deliver a rendered PNG as an attachment (SK-03).
  </what-built>
  <action>
    Run the SK-01/SK-02/SK-03 + default-fleet exchanges below over the live transport and capture them into `.planning/phases/02-skills-over-chat/sk01-sk02-sk03-transcripts.md`. This is a human-verify checkpoint: Claude prepares the prompts and writes the transcript artifact; the user confirms the PNG arrived as a real attachment and the brand output reads on-brand. See `<how-to-verify>` for the exact prompts and expectations.
  </action>
  <how-to-verify>
    Run these over chat (Slack or Telegram, whichever transport is live) against the aos agent and capture each exchange into `.planning/phases/02-skills-over-chat/sk01-sk02-sk03-transcripts.md`:

    1. SK-01 (discovery/invocation): Send `@aos: list your available mkt-/str-/viz-/meta- skills`, then invoke one by trigger phrase. Confirm workspace skills appear and one runs. Expected: the agent enumerates agentic-os methodology skills and executes one.

    2. SK-02 (on-brand brand skill): Send `@aos:` invoking a representative brand skill (mkt-copywriting OR mkt-ugc-scripts) for a short brief. Confirm the output is on-brand JUDGED AGAINST agentic-os `brand_context/voice-profile.md` — NOT against ClaudeClaw's no-em-dash house style (the workspace voice deliberately uses em dashes; per 02-RESEARCH "State of the Art"). One fresh transcript suffices (D-4).

    3. SK-03 (file delivery — the crux): Send `@aos: draw an excalidraw diagram of <a simple flow>`. The agent must, after the skill saves the PNG, emit the BRACKETED marker `[SEND_PHOTO:<absolute path>]` with the full path (the path contains a space in "App Repo"; the bare form would truncate — D-3). Expected: a PNG arrives as a Slack/Telegram attachment, NOT a raw `[SEND_PHOTO:...]` string in the message text. If the agent forgets the bracketed marker or uses the bare form, re-prompt it to emit the bracketed `[SEND_PHOTO:<absolute path>]`; record the working exchange.

    4. Default-fleet sanity (COMPAT-02): send a normal message to a non-workspace agent and confirm it behaves as before (no marker leakage, normal reply).

    Then create `sk01-sk02-sk03-transcripts.md` with the four exchanges, a one-line on-brand verdict for SK-02 referencing voice-profile.md, and a note confirming the SK-03 PNG arrived as an attachment.
  </how-to-verify>
  <verify>
    <human-check>The SK-03 exchange delivered a PNG as a real chat attachment (not a raw `[SEND_PHOTO:]` string); SK-01 shows skill discovery/invocation; SK-02 reads on-brand vs voice-profile.md; the default-fleet reply is normal. The transcripts file exists with all four exchanges.</human-check>
  </verify>
  <done>`sk01-sk02-sk03-transcripts.md` exists containing the SK-01, SK-02, SK-03, and default-fleet exchanges; the SK-03 PNG arrived as an attachment; the SK-02 on-brand verdict references voice-profile.md.</done>
  <resume-signal>Type "approved" once the PNG arrived as an attachment and the transcripts are captured, or describe what failed (e.g. raw marker leaked, wrong path, off-brand) so it can be corrected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agentic-os skill execution (SDK subprocess) → local filesystem / chat | The skill renders a PNG locally and the agent names a file path ClaudeClaw uploads. Workspace is a trusted local repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | first-run `uv sync` pulls a PyPI `playwright` | accept | Pre-warm from a trusted network using the skill's own pinned `uv.lock` (present). Workspace-owned dependency, not added to ClaudeClaw's tree (02-RESEARCH Security Domain). |
| T-02-04 | Elevation of Privilege | agentic-os hook runs a script during a file turn | accept | Trusted local repo (Phase 1 mitigation); the excalidraw run writes to `projects/`, not a SKILL file, so the auto-commit hook should not fire on it. Do NOT disable hooks here (that is SK-04/Phase 3). Verify the demo turn triggers no destructive hook. |
| T-02-05 | Information Disclosure | agent emits a marker pointing at an arbitrary local file | accept | Inherited from the main path and the 02-01 delegation fix; the `fs.existsSync` guard remains; single-user personal machine. Constraining uploads to the workspace `projects/` dir is deferred to SK-04. |
| T-02-SC | Tampering | npm/pip installs | accept | The only install is the workspace skill's own `uv sync` (T-02-03). No package added to ClaudeClaw's npm tree; no slopcheck checkpoint required (02-RESEARCH Package Legitimacy Audit — N/A). |
</threat_model>

<verification>
- `references/.venv` exists for the excalidraw skill (offline render confirmed)
- aos `agent.yaml` contains `slack_channel:` with the user-supplied ID (unless user chose "skip channel")
- Transcripts artifact exists with SK-01, SK-02, SK-03, and a default-fleet sanity exchange
- The SK-03 exchange shows a PNG ATTACHMENT, not a raw `[SEND_PHOTO:]` string
</verification>

<success_criteria>
- SK-01: aos lists and invokes workspace methodology skills over chat (transcript).
- SK-02: a brand skill returns an on-brand result judged against agentic-os voice-profile.md (fresh transcript).
- SK-03: viz-excalidraw-diagram's PNG is delivered as a chat attachment via the bracketed marker (transcript + confirmed attachment).
- aos has a slack_channel (D-1b) and the bot was restarted, OR the user explicitly skipped the channel and relies on the @aos: delegation route.
- No default-fleet regression.
</success_criteria>

<output>
Create `.planning/phases/02-skills-over-chat/02-02-SUMMARY.md` when done. Record: the pre-warm result, the aos channel ID outcome (set or skipped) and restart, links/excerpts from the three transcripts, the SK-02 on-brand verdict vs voice-profile.md, and explicit confirmation the SK-03 PNG arrived as an attachment (not raw marker text).
</output>
