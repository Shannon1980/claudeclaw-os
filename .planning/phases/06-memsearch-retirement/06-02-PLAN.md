---
phase: 06-memsearch-retirement
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - /Users/shannongueringer/App Repo/agentic-os/AGENTS.md
  - /Users/shannongueringer/App Repo/agentic-os/cron/jobs/nightly-memsearch-index.md
  - /Users/shannongueringer/App Repo/agentic-os/.claude/settings.json
autonomous: true
requirements: [MEM-05, MEM-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The agentic-os AGENTS.md Tier-1 recall instruction points at the ClaudeClaw recall CLI over the symlink, not memsearch/reranker (D-02)"
    - "The nightly-memsearch-index cron is active:'false' so the second index never re-indexes (D-03)"
    - "The agentic-os Stop hook runs the committed capture-cli.js alongside session-sync-stop.js so terminal work persists to the single store (D-07/MEM-04)"
    - "The capture-hook wiring is committed in the agentic-os repo so it persists across checkouts"
  artifacts:
    - path: "/Users/shannongueringer/App Repo/agentic-os/AGENTS.md"
      provides: "Rewritten Tier-1 recall instruction invoking the recall CLI"
      contains: "recall-cli.js"
    - path: "/Users/shannongueringer/App Repo/agentic-os/cron/jobs/nightly-memsearch-index.md"
      provides: "Disabled nightly index cron"
      contains: "active: 'false'"
    - path: "/Users/shannongueringer/App Repo/agentic-os/.claude/settings.json"
      provides: "Stop hook with committed capture-cli.js entry"
      contains: "capture-cli.js"
  key_links:
    - from: "agentic-os AGENTS.md Tier 1"
      to: "dist/recall-cli.js"
      via: "node \"$HOME/.claudeclaw-app/dist/recall-cli.js\" \"query\" --top-k 10"
      pattern: "recall-cli\\.js"
    - from: "agentic-os .claude/settings.json Stop array"
      to: "dist/capture-cli.js"
      via: "node \"$HOME/.claudeclaw-app/dist/capture-cli.js\""
      pattern: "capture-cli\\.js"
---

<objective>
Repoint the agentic-os workspace at the ClaudeClaw single index and durably restore capture: rewrite AGENTS.md Tier-1 recall to call the new recall CLI (D-02), disable the nightly memsearch index cron (D-03), and wire+commit the capture-cli.js Stop hook so MEM-04 persists (D-07). All edits use the `$HOME/.claudeclaw-app` symlink, never the spaced raw repo path.

Purpose: Cut the workspace over to the surviving index and close the re-opened MEM-04. This wave runs ONLY after Plan 01 has shipped and built the recall CLI (D-06: never disable the index before its replacement exists).

Output: Edited AGENTS.md, disabled cron frontmatter, capture-hook entry in settings.json, all committed in the agentic-os repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-memsearch-retirement/06-CONTEXT.md
@.planning/phases/06-memsearch-retirement/06-RESEARCH.md
@.planning/phases/06-memsearch-retirement/06-PATTERNS.md
@.planning/phases/05-memory-projection-capture/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AGENTS.md Tier-1 recall to the recall CLI (D-02)</name>
  <files>/Users/shannongueringer/App Repo/agentic-os/AGENTS.md</files>
  <read_first>
    - /Users/shannongueringer/App Repo/agentic-os/AGENTS.md "Memory Retrieval" section (current Tier-1 text at lines ~216-219 with the /memory-recall plugin + `memsearch search "query" --top-k 10 --json-output | python3 scripts/lib/reranker.py "query"` instruction and the "Searches context/memory/, .memsearch/memory/, ..." line; the absent-response phrasing "ran semantic search across all indexed sources" at line ~226; the `bash scripts/lib/memory-meta.sh "[topic]"` coverage line at ~228)
    - .planning/phases/06-memsearch-retirement/06-RESEARCH.md lines 253-267 (exact before/after for the Tier-1 edit) and PATTERNS.md lines 165-180 (target state + planner follow-ups)
  </read_first>
  <action>
    In agentic-os AGENTS.md "Memory Retrieval", replace the Tier-1 block (the two sub-bullets invoking `/memory-recall` and the `memsearch search ... | reranker.py` pipe, plus the "Searches context/memory/, .memsearch/memory/, ..." line) with a single instruction that runs ClaudeClaw semantic recall against the single store: invoke `node "$HOME/.claudeclaw-app/dist/recall-cli.js" "query" --top-k 10`, state that it queries ClaudeClaw's SQLite embeddings (the source of record shared with the bot) scoped to this workspace, and to summarise the top results and cite them per the rules below. Use the `$HOME/.claudeclaw-app` symlink path verbatim, NOT the raw `/Users/shannongueringer/App Repo/claudeclaw` path (it has a space; CLAUDE.md launchd rule). No em dashes in the new prose (use -- or commas). Also reword the absent-response phrase at line ~226 from "ran semantic search across all indexed sources" to "ran ClaudeClaw semantic recall" for consistency. Leave Tier 0 (line ~215), the Cite-sources block, and Tiers 2-3 (line ~230) unchanged. Keep the `bash scripts/lib/memory-meta.sh "[topic]"` coverage line at ~228 as-is: memory-meta.sh reads the `.memsearch/memory/` directory as frozen markdown file paths and does NOT invoke any memsearch command (verified), so it stays valid against the frozen archive (resolves RESEARCH Open Question 2).
  </action>
  <verify>
    <automated>cd "/Users/shannongueringer/App Repo/agentic-os" && grep -q 'recall-cli.js' AGENTS.md && ! grep -q 'memsearch search' AGENTS.md && ! grep -q 'reranker.py' AGENTS.md && ! grep -q '—' AGENTS.md && echo TIER1_OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md Tier 1 contains the exact command substring `node "$HOME/.claudeclaw-app/dist/recall-cli.js"`
    - AGENTS.md no longer contains `memsearch search` or `reranker.py`
    - AGENTS.md no longer contains the `/memory-recall` plugin invocation in Tier 1
    - The absent-response phrasing reads "ran ClaudeClaw semantic recall" (not "ran semantic search across all indexed sources")
    - AGENTS.md contains no em dash character
    - Tier 0 and Tiers 2-3 text are byte-unchanged from before the edit
  </acceptance_criteria>
  <done>AGENTS.md Tier-1 recall invokes the ClaudeClaw recall CLI over the symlink; the memsearch/reranker recall path is gone; Tier 0 and 2-3 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Disable the nightly memsearch index cron + wire and commit the capture Stop hook (D-03, D-07)</name>
  <files>/Users/shannongueringer/App Repo/agentic-os/cron/jobs/nightly-memsearch-index.md, /Users/shannongueringer/App Repo/agentic-os/.claude/settings.json</files>
  <read_first>
    - /Users/shannongueringer/App Repo/agentic-os/cron/jobs/nightly-memsearch-index.md frontmatter (current `active: 'true'`; this is the ONLY cron invoking `memsearch index`, verified)
    - /Users/shannongueringer/App Repo/agentic-os/.claude/settings.json "Stop" array (current state runs only `node "$CLAUDE_PROJECT_DIR/.claude/hooks/session-sync-stop.js"`; capture-cli appears in NO committed revision)
    - .planning/phases/06-memsearch-retirement/06-PATTERNS.md lines 183-203 (cron flip + Stop-hook additive entry, both target states) and 06-CONTEXT.md D-07
    - .planning/phases/05-memory-projection-capture/05-02-SUMMARY.md CORRECTION block (capture-cli Stop hook was never committed; this task fixes that durably)
  </read_first>
  <action>
    Edit cron/jobs/nightly-memsearch-index.md: change the frontmatter key `active: 'true'` to `active: 'false'`. Leave the entire job body intact (dormant + reversible per D-03). Do NOT touch any other cron file, the memsearch CLI binary, scripts/setup-memsearch.*, scripts/stop-memsearch-watchers.ps1, or the Bash(memsearch *) / setup-script permissions in settings.json, those stay dormant, not deleted (D-03). Do NOT migrate cron ownership (Phase 7 owns the scheduler bridge); this is a single-flag flip.

    Edit .claude/settings.json: add `node "$HOME/.claudeclaw-app/dist/capture-cli.js"` as an ADDITIONAL command entry in the existing "Stop" hook array, alongside (not replacing) the existing `session-sync-stop.js` entry, following the existing Stop-array entry shape (a hooks object with a command of type "command"). Use the `$HOME/.claudeclaw-app` symlink path verbatim (NOT the spaced raw path; CLAUDE.md launchd rule). This is additive: session-sync-stop.js keeps running; capture-cli.js writes terminal work into the single SQLite store under (ws:aos, aos). Keep the JSON valid (parseable). No em dashes anywhere in the file.

    Commit BOTH edits in the agentic-os repo so the capture wiring persists across checkouts (D-07 durability requirement; the prior uncommitted edit is what re-opened MEM-04). Per CLAUDE.md GSD git workflow, commit on a branch off main in the agentic-os repo (do not push unless asked); use a message describing the cron disable + capture-hook wire and end it with the Co-Authored-By trailer. Confirm `git -C "/Users/shannongueringer/App Repo/agentic-os" grep -l 'capture-cli' .claude/settings.json` resolves against the committed tree.
  </action>
  <verify>
    <automated>cd "/Users/shannongueringer/App Repo/agentic-os" && grep -q "active: 'false'" cron/jobs/nightly-memsearch-index.md && node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'))" && grep -q 'capture-cli.js' .claude/settings.json && grep -q 'session-sync-stop.js' .claude/settings.json && git ls-files --error-unmatch .claude/settings.json >/dev/null && git diff --quiet HEAD -- .claude/settings.json cron/jobs/nightly-memsearch-index.md && echo CRON_HOOK_COMMITTED_OK</automated>
  </verify>
  <acceptance_criteria>
    - nightly-memsearch-index.md frontmatter reads `active: 'false'`; its job body is otherwise unchanged
    - No other cron job file is modified
    - .claude/settings.json is valid JSON and its Stop array contains BOTH `capture-cli.js` and `session-sync-stop.js` command entries
    - The capture-cli.js Stop entry uses `$HOME/.claudeclaw-app/dist/capture-cli.js` (symlink), not the spaced raw path
    - Both files are committed in the agentic-os repo (working tree clean for these two paths after commit)
    - No memsearch binary, setup script, or settings.json permission was deleted (D-03 dormancy preserved)
  </acceptance_criteria>
  <done>The nightly index cron is disabled, the capture-cli.js Stop hook is wired additively alongside session-sync-stop.js, and both edits are committed in the agentic-os repo so MEM-04 capture persists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agentic-os config → terminal Claude Code | AGENTS.md/settings.json instruct a terminal session what command to run for recall and capture |
| terminal Stop hook → SQLite store | capture-cli.js writes terminal-session work across the process boundary into the shared store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | AGENTS.md pointing at a non-existent or wrong-path artifact | mitigate | Reference the verified `$HOME/.claudeclaw-app/dist/recall-cli.js` symlink built in Plan 01 (dependency 06-01); never the spaced raw path; verify recall-cli.js exists before the live proof (Plan 03) |
| T-06-07 | Information Disclosure | capture Stop hook attribution | mitigate | capture-cli.js fixes agentId='aos' server-side (CAPTURE_AGENT_ID); the Stop hook entry passes no agent override, so terminal work lands only in (ws:aos, aos) |
| T-06-08 | Repudiation | uncommitted capture wiring (root cause of MEM-04 re-open) | mitigate | Commit the settings.json edit in the agentic-os repo (D-07); verify with git ls-files + clean diff so the wiring is durable, not a transient local edit |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; the Package Legitimacy Audit (RESEARCH) confirms zero registry interaction; memsearch stays installed-but-dormant, not added |
</threat_model>

<verification>
- AGENTS.md Tier-1 points at recall-cli.js; no memsearch/reranker; Tier 0/2-3 unchanged
- nightly-memsearch-index.md `active: 'false'`; body intact
- settings.json valid JSON, Stop array has both capture-cli.js and session-sync-stop.js
- Both agentic-os files committed (clean diff post-commit)
- No em dashes in any edited file
</verification>

<success_criteria>
- Terminal recall is documented to run through the ClaudeClaw single index, not memsearch (MEM-05)
- The nightly second-index re-index no longer fires (MEM-05 criterion 1)
- Terminal capture is durably wired and committed so the bot sees terminal work (MEM-04)
- memsearch CLI/scripts/perms remain dormant (D-03 reversibility)
</success_criteria>

<output>
Create `.planning/phases/06-memsearch-retirement/06-02-SUMMARY.md` when done
</output>
