---
phase: 01-desktop-shell-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/config.ts
  - src/env.ts
  - src/config.test.ts
  - electron/main.cjs
  - electron/config.cjs
  - electron/boot.html
  - package.json
autonomous: true
requirements: [PKG-02]

must_haves:
  truths:
    - "In a packaged read-only .app, the service writes .env, store/, and logs to a writable per-user dir (PKG-02)"
    - "Pending migrations are applied before the service forks, so the migration gate can never silently kill the service"
    - "The build rebuilds better-sqlite3 for Electron's ABI via prebuilt binaries (no source compile)"
  artifacts:
    - path: "src/config.ts"
      provides: "STORE_DIR/.env base honors CLAUDECLAW_DATA_DIR when set"
      contains: "CLAUDECLAW_DATA_DIR"
    - path: "src/env.ts"
      provides: "readEnvFile reads .env from the data dir, not just process.cwd()"
      contains: "CLAUDECLAW_DATA_DIR"
    - path: "electron/main.cjs"
      provides: "Migrations run before fork; child cwd/env points at the writable data dir; migration-failed boot state"
      contains: "CLAUDECLAW_DATA_DIR"
    - path: "package.json"
      provides: "electron:build rebuilds native deps; postinstall keeps dev ABI matched"
      contains: "install-app-deps"
  key_links:
    - from: "electron/main.cjs"
      to: "src/migrations.ts"
      via: "run migrate before startService()"
      pattern: "migrat"
    - from: "electron/main.cjs"
      to: "src/config.ts"
      via: "CLAUDECLAW_DATA_DIR env passed to the forked child"
      pattern: "CLAUDECLAW_DATA_DIR"
---

<objective>
Fix the three packaging-correctness bugs that make a signed build fail before the user ever sees the dashboard, in the order they break the boot: (1) writable-state redirect — route .env/store/logs to app.getPath('userData') because process.resourcesPath/app is read-only under Gatekeeper; (2) migration gate — run migrations before forking the service so checkPendingMigrations()'s process.exit(1) can never silently kill it; (3) native ABI — rebuild better-sqlite3 for Electron's Node ABI via prebuilt binaries (the dev-machine toolchain cannot source-compile).

These are prerequisites for the first real end-to-end launch test (plan 04). PKG-02 (boot the service internally and open the dashboard) cannot be true until all three are fixed.

Purpose: make the packaged service actually boot and bind its dashboard on a clean machine.
Output: data-dir-aware config, a migration-before-fork boot path, and a build that ships an ABI-correct native module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-desktop-shell-onboarding/01-RESEARCH.md
@src/migrations.ts
@scripts/migrate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route writable state to a data dir the packaged app can write</name>
  <files>src/config.ts, src/env.ts, src/config.test.ts</files>
  <read_first>
    - src/config.ts lines 137-166 (PROJECT_ROOT, STORE_DIR, CLAUDECLAW_CONFIG, expandHome — the path resolution to extend)
    - src/env.ts (readEnvFile — reads process.cwd()/.env; must also honor the data dir)
    - src/db.ts lines 429-450 (STORE_DIR usage, db open, chmod 0700 — the consumer that must land in the writable dir)
    - electron/main.cjs lines 23-47 (APP_ROOT = resourcesPath/app, ENV_PATH — the read-only path being fixed)
    - .planning/phases/01-desktop-shell-onboarding/01-RESEARCH.md Pitfall 2 + Runtime State Inventory (Stored data / Secrets rows) + Assumption A3
  </read_first>
  <behavior>
    - When CLAUDECLAW_DATA_DIR is set: STORE_DIR resolves under it and readEnvFile reads <data-dir>/.env
    - When CLAUDECLAW_DATA_DIR is unset: behavior is unchanged (STORE_DIR under PROJECT_ROOT, .env from process.cwd()) so the terminal/dev path is untouched
    - The data dir is created (recursive) if missing before STORE_DIR is used
    - DB_ENCRYPTION_KEY and DASHBOARD_TOKEN read from the data-dir .env survive across app updates (they live outside the replaceable bundle)
  </behavior>
  <action>
    In src/config.ts introduce a CLAUDECLAW_DATA_DIR resolution mirroring the existing expandHome/CLAUDECLAW_CONFIG pattern: read process.env.CLAUDECLAW_DATA_DIR (no .env fallback — it must come from the shell env so it is known before .env is located), and when present resolve STORE_DIR to path.join(dataDir, 'store') instead of PROJECT_ROOT/store. Leave PROJECT_ROOT (the SDK cwd that loads CLAUDE.md/skills) pointing at the code dir — only writable state moves. In src/env.ts change readEnvFile to read from process.env.CLAUDECLAW_DATA_DIR + '/.env' when that env var is set, else fall back to process.cwd()/.env (unchanged default). Keep the no-process.env-load behavior (secrets stay out of the environment). Add or extend src/config.test.ts asserting STORE_DIR honors CLAUDECLAW_DATA_DIR and is unchanged when unset, mocking the env var. Do NOT hardcode an absolute macOS path — the shell supplies the userData path. No inline code blocks in other files.
  </action>
  <verify>
    <automated>npx vitest run src/config.test.ts src/env.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/config.test.ts src/env.test.ts` passes
    - `grep -n "CLAUDECLAW_DATA_DIR" src/config.ts src/env.ts` shows the override in both files
    - With CLAUDECLAW_DATA_DIR unset, a test asserts STORE_DIR equals the prior PROJECT_ROOT/store value (no regression for the terminal path)
    - `npm run typecheck` clean
  </acceptance_criteria>
  <done>The service resolves all writable state (.env, store/, db) under CLAUDECLAW_DATA_DIR when the shell sets it, and is byte-identical to today when it is unset.</done>
</task>

<task type="auto">
  <name>Task 2: Run migrations before fork; point the child at the data dir; add migration-failed boot state</name>
  <files>electron/main.cjs, electron/config.cjs, electron/boot.html</files>
  <read_first>
    - electron/main.cjs lines 23-98 (APP_ROOT, ENV_PATH, resolveServiceCommand, startService — fork path to modify)
    - electron/main.cjs lines 170-201 (bootDashboard, boot — where migrations must run first)
    - src/migrations.ts (checkPendingMigrations — the process.exit(1) gate)
    - scripts/migrate.ts lines 85-119 (the migrate entry; note the interactive [y/N] prompt the shell must avoid blocking on)
    - electron/boot.html lines 99-120 (the copy map to extend)
    - .planning/phases/01-desktop-shell-onboarding/01-RESEARCH.md Pitfall 3 + "Build script gap to fix"
  </read_first>
  <action>
    In electron/main.cjs compute a writable DATA_DIR via app.getPath('userData') when app.isPackaged (else keep dev behavior using APP_ROOT), set ENV_PATH to DATA_DIR/.env in packaged mode, and pass CLAUDECLAW_DATA_DIR=DATA_DIR in the forked child env (childEnv) so the service from task 1 lands its writable state there. Before startService() in bootDashboard(), run migrations to completion in the main process: invoke the migrate logic non-interactively (run scripts/migrate.ts via the bundled tsx in dev, or the compiled migrate path in packaged mode) with stdin pre-answered or a non-interactive flag so it never blocks on the prompt; show bootUrl('migrating') while it runs. On migration failure load a boot.html migrating-failed state offering Retry that re-runs migrate, not a bare error. Only after migrations succeed call startService() + waitForDashboard(). Because the gate no longer sees pending migrations at service start, checkPendingMigrations() exit(1) cannot fire. In electron/boot.html add migrating and migrating-failed entries to the copy map. In electron/config.cjs point its ENV reads at the same DATA_DIR .env when CLAUDECLAW_DATA_DIR is set so isConfigured/readEnv operate on the live file. Honor the launchd rule: any log path written must be space-free (/tmp or ~/Library/Logs), never the spaced repo path.
  </action>
  <verify>
    <automated>node --check electron/main.cjs && node --check electron/config.cjs && grep -q "CLAUDECLAW_DATA_DIR" electron/main.cjs && grep -q "migrating-failed" electron/boot.html</automated>
  </verify>
  <acceptance_criteria>
    - `node --check electron/main.cjs` and `node --check electron/config.cjs` succeed (valid syntax)
    - `grep -n "getPath('userData')\|getPath(\"userData\")" electron/main.cjs` shows the writable-dir resolution
    - `grep -n "CLAUDECLAW_DATA_DIR" electron/main.cjs` shows it is passed to the child env
    - migrations are invoked before startService() (manual read confirms ordering in bootDashboard)
    - `grep -c "migrating" electron/boot.html` is >= 1 (both new states present)
  </acceptance_criteria>
  <done>The shell applies migrations before forking the service, routes all writable state to userData in packaged mode, and shows a real migrating/retry state instead of a silent death.</done>
</task>

<task type="auto">
  <name>Task 3: Add the native-module ABI rebuild to the build and dev install</name>
  <files>package.json</files>
  <read_first>
    - package.json lines 7-28 (scripts — electron:build, build, postinstall absence)
    - package.json lines 84-114 (build block — files, extraResources, mac target)
    - .planning/phases/01-desktop-shell-onboarding/01-RESEARCH.md Pitfall 1 + "Build script gap to fix" + Assumption A2 (prebuild availability) + Don't Hand-Roll (install-app-deps row)
  </read_first>
  <action>
    In package.json change the electron:build script to `npm run build && electron-builder install-app-deps && electron-builder` so better-sqlite3 is rebuilt/prebuilt-fetched for Electron's ABI between the tsc/vite build and packaging. Add a `postinstall` script `electron-builder install-app-deps` so dev installs match the Electron ABI too. install-app-deps fetches prebuilt binaries (prebuild-install) — do NOT add any node-gyp/source-compile step; this machine's native toolchain is broken (Python 3.12 no-distutils + Node 25). Do NOT bump electron, electron-builder, or better-sqlite3 versions — the pins are load-bearing for the ABI match. Leave the build block's extraResources (dist/agents/warroom/migrations/package.json) intact; confirm migrations/** is copied so the in-app migrate step has its files.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['electron:build'].includes('install-app-deps')) process.exit(1); if(!(p.scripts.postinstall||'').includes('install-app-deps')) process.exit(1); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); console.log(p.scripts['electron:build'])"` shows install-app-deps between build and electron-builder
    - `node -e "const p=require('./package.json'); console.log(p.scripts.postinstall)"` shows install-app-deps
    - electron/electron-builder/better-sqlite3 versions are unchanged from the pre-edit values
    - build.extraResources still includes migrations/**
  </acceptance_criteria>
  <done>The build rebuilds better-sqlite3 for Electron's ABI via prebuilt binaries before packaging, and dev installs stay ABI-matched via postinstall, with no version bumps and no source-compile step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only bundle -> writable userData | Secrets (DB_ENCRYPTION_KEY, tokens) must live in the writable dir, 0600 |
| build pipeline -> native binary | An ABI-wrong or tampered .node breaks or compromises the DB layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | userData .env / db | mitigate | Writes stay 0600 (config.cjs writeEnv) and 0700 store dir (db.ts); only move location, not perms |
| T-02-02 | Denial of Service | migration gate | mitigate | Run migrations before fork; failure shows retry state, never a silent exit |
| T-02-03 | Tampering | better-sqlite3 .node | mitigate | Use install-app-deps prebuilt binaries from the pinned incumbent package; no version bump, no source compile |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new runtime packages; install-app-deps operates on the already-approved incumbent better-sqlite3 (RESEARCH Package Legitimacy Audit: none new) |
</threat_model>

<verification>
- `npx vitest run src/config.test.ts src/env.test.ts` green; `npm run typecheck` clean
- `node --check electron/main.cjs` and `node --check electron/config.cjs` pass
- electron:build and postinstall both include install-app-deps; no version bumps
- migrations run before fork (read-confirm ordering in bootDashboard)
</verification>

<success_criteria>
- Writable state resolves under userData in packaged mode, unchanged in dev/terminal
- Migration gate cannot silently kill the service; a retry state exists
- The build ships an Electron-ABI better-sqlite3 via prebuilt binaries
</success_criteria>

<output>
Create `.planning/phases/01-desktop-shell-onboarding/01-02-SUMMARY.md` when done.
</output>
