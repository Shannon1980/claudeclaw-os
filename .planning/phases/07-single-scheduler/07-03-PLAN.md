---
phase: 07-single-scheduler
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - launchd/com.claudeclaw.aos.plist
autonomous: true
requirements: [SCH-01]
must_haves:
  truths:
    - "A launchd plist exists that runs the aos agent as a standalone service"
    - "Log paths contain no spaces (launchd will not exit 78)"
    - "WorkingDirectory uses the spaces-safe symlink convention"
  artifacts:
    - path: "launchd/com.claudeclaw.aos.plist"
      provides: "Standalone aos service definition with crash recovery"
      contains: "com.claudeclaw.aos"
  key_links:
    - from: "launchd/com.claudeclaw.aos.plist"
      to: "dist/index.js --agent aos"
      via: "ProgramArguments"
      pattern: "aos"
---

<objective>
Create the `launchd/com.claudeclaw.aos.plist` so the `aos` workspace agent — today delegation-only with no process — runs as a standalone service (`--agent aos`), giving it its own `initScheduler(send, 'aos')` loop (D-04/D-05). Strictly follow the project launchd rules: the live project path contains spaces, so log paths use `/tmp/claudeclaw-aos.log` (never a spaced path — launchd exits 78 on spaces) and `WorkingDirectory` uses the `~/.claudeclaw-app` symlink convention.

Purpose: SCH-01 makes ClaudeClaw the single runner; the aos jobs must fire from a real process. The aos agent currently has no service, so it needs its own plist mirroring the per-agent template.

Output: `launchd/com.claudeclaw.aos.plist`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-scheduler/07-CONTEXT.md
@.planning/phases/07-single-scheduler/07-PATTERNS.md
@CLAUDE.md
@launchd/com.claudeclaw.comms.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author com.claudeclaw.aos.plist from the per-agent template</name>
  <files>launchd/com.claudeclaw.aos.plist</files>
  <read_first>
    - launchd/com.claudeclaw.comms.plist (the per-agent standalone-service template — Label, ProgramArguments with --agent flag, WorkingDirectory, RunAtLoad, KeepAlive, ThrottleInterval, StandardOutPath/StandardErrorPath)
    - CLAUDE.md "launchd Rules" section (exit 78 on spaces in log paths; symlink for WorkingDirectory; KeepAlive+ThrottleInterval recovery; diagnosis via launchctl print)
    - .planning/phases/07-single-scheduler/07-PATTERNS.md "launchd/com.claudeclaw.aos.plist" section (the diverging detail: /tmp log path, symlink WorkingDirectory; do NOT copy the comms PROJECT_DIR/logs form)
  </read_first>
  <behavior>
    - The plist Label is com.claudeclaw.aos and ProgramArguments runs the node binary on dist/index.js with the --agent aos flag.
    - StandardOutPath and StandardErrorPath are both /tmp/claudeclaw-aos.log with no spaces anywhere in either path (the spaced-path exit-78 trap is avoided).
    - RunAtLoad, KeepAlive, and ThrottleInterval (30) are present for network-not-ready crash recovery.
    - WorkingDirectory resolves through the spaces-safe convention (the ~/.claudeclaw-app symlink placeholder), not a literal spaced path.
  </behavior>
  <action>
    Copy launchd/com.claudeclaw.comms.plist as the structural template. Change Label to com.claudeclaw.aos and the --agent arg from comms to aos. Carry over RunAtLoad true, KeepAlive true, ThrottleInterval 30. CRITICAL divergence (CLAUDE.md launchd rules + PATTERNS): set BOTH StandardOutPath and StandardErrorPath to /tmp/claudeclaw-aos.log; do NOT use the comms plist PROJECT_DIR/logs/comms.log form, because the live project path /Users/shannongueringer/App Repo/claudeclaw contains spaces and launchd exits 78 (EX_CONFIG) on spaces in those keys. For WorkingDirectory keep the project placeholder/symlink convention the other plists use (the install tooling substitutes the spaces-safe ~/.claudeclaw-app symlink). Keep the NODE_PATH and PROJECT_DIR placeholders intact so existing install tooling substitutes them. Do NOT load the service in this plan; loading/cutover happens in the final proof plan (07-05) after the new path is proven.
  </action>
  <verify>
    <automated>plutil -lint launchd/com.claudeclaw.aos.plist; grep -q 'com.claudeclaw.aos' launchd/com.claudeclaw.aos.plist; grep -q '/tmp/claudeclaw-aos.log' launchd/com.claudeclaw.aos.plist</automated>
  </verify>
  <acceptance_criteria>
    - plutil -lint reports the plist is valid (well-formed XML plist).
    - Label and the --agent argument both resolve to aos.
    - Both StandardOutPath and StandardErrorPath are exactly /tmp/claudeclaw-aos.log; no occurrence of "App Repo" or any space inside either log-path value.
    - RunAtLoad, KeepAlive true, ThrottleInterval 30 are present.
    - The service is NOT loaded by this plan.
  </acceptance_criteria>
  <done>Valid com.claudeclaw.aos.plist runs --agent aos with spaces-safe /tmp log paths and crash-recovery keys, unloaded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launchd → aos process | a service running with the user's privileges in the agentic-os workspace cwd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Elevation of Privilege | aos launchd service runs as the user | accept | Mirrors every other com.claudeclaw.*.plist already running as the user; the aos agent is an existing trusted workspace agent. No new privilege surface vs. the existing fleet services. |
| T-07-07 | Denial of Service | bad log path causing launchd exit 78 crash-loop | mitigate | Log paths forced to /tmp/claudeclaw-aos.log (no spaces) per CLAUDE.md launchd rules; KeepAlive+ThrottleInterval handle network-not-ready recovery. Verified by acceptance criterion (no "App Repo"/space in log-path values). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- plutil -lint passes on the new plist.
- Label/--agent = aos; both log paths = /tmp/claudeclaw-aos.log with no spaces.
- Crash-recovery keys present; service left unloaded.
</verification>

<success_criteria>
- A standalone aos service definition exists (D-05), spaces-safe per CLAUDE.md launchd rules, ready to be loaded at cutover (07-05).
</success_criteria>

<output>
Create `.planning/phases/07-single-scheduler/07-03-SUMMARY.md` when done.
</output>
