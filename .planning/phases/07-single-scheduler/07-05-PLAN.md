---
phase: 07-single-scheduler
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - /Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts
autonomous: false
requirements: [SCH-01, SCH-02, SCH-03, SCH-04]
must_haves:
  truths:
    - "The Command Centre can boot without starting its in-process cron scheduler (CRON_IN_PROCESS gate)"
    - "The migration is applied and the bot restarts without checkPendingMigrations crash-looping"
    - "A real aos-cron job fires from the live aos service and writes its result to the DB row + Slack"
    - "The agentic-os cron engine no longer schedules or fires any job"
    - "A given job fires exactly once across the main and aos processes (no double-fire)"
  artifacts:
    - path: "/Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts"
      provides: "CRON_IN_PROCESS opt-out gate around initCronScheduler()"
      contains: "CRON_IN_PROCESS"
  key_links:
    - from: "instrumentation.ts"
      to: "initCronScheduler()"
      via: "env gate"
      pattern: "CRON_IN_PROCESS"
    - from: "com.claudeclaw.aos service"
      to: "scheduled_tasks aos rows"
      via: "live fire writes last_run/last_status/last_result"
      pattern: "last_status"
---

<objective>
Final cutover and live proof — sequenced LAST per the critical-sequencing rule (never disable the old engine before the new path fires a real job). Add the `CRON_IN_PROCESS` opt-out gate in the agentic-os Command Centre (D-01), apply the migration and restart the bot (D-09 deploy step), load the `com.claudeclaw.aos` service, prove a real aos-cron job fires cross-process and writes DB-row + Slack parity, prove the old engine is stopped, and prove exactly-once (no double-fire) against the cross-process claim path (SCH-04).

Purpose: closes SCH-01 (single runner / old engine disabled), SCH-03 (status/log parity on a real fire), and SCH-04 (no double-fire, verified against the claim path). This is the blocking human-verify gate for the phase.

Output: gated `instrumentation.ts` (agentic-os repo), applied migration, loaded aos service, and a live-proof transcript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-scheduler/07-CONTEXT.md
@.planning/phases/07-single-scheduler/07-PATTERNS.md
@CLAUDE.md
@.planning/phases/07-single-scheduler/07-01-SUMMARY.md
@.planning/phases/07-single-scheduler/07-02-SUMMARY.md
@.planning/phases/07-single-scheduler/07-03-SUMMARY.md
@.planning/phases/07-single-scheduler/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CRON_IN_PROCESS gate in agentic-os Command Centre + apply migration + load aos service</name>
  <files>/Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts</files>
  <read_first>
    - /Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts lines 14-16 (the unconditional initCronScheduler() call to gate)
    - /Users/shannongueringer/App Repo/agentic-os/cron/SCHEDULER-HANDOFF.md lines 22-33 (the CC initCronScheduler warning + CRON_IN_PROCESS=0 suggestion)
    - CLAUDE.md "Scheduling Tasks" + "launchd Rules" + "Deploy" notes (npm run migrate before restart; symlink for spaced paths; launchctl load)
    - .planning/phases/07-single-scheduler/07-PATTERNS.md "command-centre/src/instrumentation.ts" section (the exact env-gate wrap)
  </read_first>
  <behavior>
    - With CRON_IN_PROCESS=0 set, booting the Command Centre Next.js server does NOT call initCronScheduler() (no second scheduler, no cron-runtime-lock.json claim). With the env unset or any other value, current behavior is preserved (the gate is opt-out).
    - npm run migrate is run BEFORE the bot restart so checkPendingMigrations does not process.exit(1) crash-loop on startup (D-09 deploy ordering).
    - After loading com.claudeclaw.aos, launchctl shows the service running (a live PID, no exit 78), and syncAosCronJobs has populated the aos rows.
  </behavior>
  <action>
    In the agentic-os repo file /Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts, wrap the dynamic import + `initCronScheduler()` (14-16) behind `if (process.env.CRON_IN_PROCESS !== "0") { ... }` so setting CRON_IN_PROCESS=0 boots CC without its scheduler (D-01; opt-out semantics per the handoff doc). Then perform the deploy in order: (1) run `npm run migrate` in the claudeclaw project to apply the 07-01 migration (MUST precede restart per D-09 or checkPendingMigrations crash-loops). (2) Restart the main bot service. (3) Ensure the ~/.claudeclaw-app symlink points at the project (per CLAUDE.md launchd rules) and `launchctl load` (or bootstrap) com.claudeclaw.aos; verify via `launchctl print gui/$(id -u)/com.claudeclaw.aos` that state is running and last exit code is 0 (NOT 78). Do NOT run scripts/start-crons.sh and do NOT start the CC server with its scheduler enabled (D-02). This is a cross-repo edit — sequence it here, last.
  </action>
  <verify>
    <automated>grep -q 'CRON_IN_PROCESS' "/Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts"; ls -la "/Users/shannongueringer/App Repo/claudeclaw/migrations/.applied.json" 2>/dev/null; launchctl print gui/$(id -u)/com.claudeclaw.aos 2>/dev/null | grep -E 'state|last exit code'</automated>
  </verify>
  <acceptance_criteria>
    - instrumentation.ts gates initCronScheduler() behind CRON_IN_PROCESS !== "0".
    - migrations/.applied.json records the 07-01 migration as applied; the bot restarted without a checkPendingMigrations exit(1).
    - launchctl print for com.claudeclaw.aos shows state = running and last exit code 0 (not 78); aos rows exist in scheduled_tasks.
    - scripts/start-crons.sh was NOT run; the CC server scheduler is not running.
  </acceptance_criteria>
  <done>CC scheduler gated, migration applied before restart, aos service loaded and healthy with synced rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live cross-process proof — single fire, parity, no double-fire</name>
  <what-built>
    The full single-scheduler path: aos service syncs cron/jobs/*.md, fires a job on schedule via the atomic claim, records the result in its scheduled_tasks row + Slack notify, with the agentic-os cron engine disabled and the CC scheduler gated off.
  </what-built>
  <how-to-verify>
    1. Pick a fast active aos job (or temporarily set one job's time to the next minute, server-local). Confirm its row exists: query scheduled_tasks WHERE source='aos-cron' AND agent_id='aos' — verify one row per job (8 rows), only the 3 active ones status 'active', nightly-memsearch-index 'paused'.
    2. Wait for the scheduled minute. Confirm the job fires exactly once: the row's last_run updates, last_status='success', last_result is populated, and (for notify=on_finish) a Slack message arrives. Confirm NO "Scheduled task running" preamble was sent.
    3. SCH-04 no-double-fire: with BOTH the main bot and the aos service live, confirm the same job did NOT fire twice (one last_run timestamp advance, one Slack message, one log entry). Inspect logs from both processes — only the aos process should report firing the aos row; main should never claim it.
    4. SCH-01 old engine off: confirm no agentic-os cron process is running (no PID daemon from scripts/start-crons.sh; CC server, if booted, was started with CRON_IN_PROCESS=0 and did not claim cron-runtime-lock.json). Confirm ClaudeClaw wrote NO cron/status/*.json or cron/logs/*.log entries for the run (D-03 — DB row + Slack only).
    5. Edit the job's .md prompt body, do NOT restart, wait for the next fire: confirm the new body took effect (prompt re-read at fire time, D-07).
  </how-to-verify>
  <resume-signal>Type "approved" once single-fire + parity + no-double-fire + old-engine-off + prompt-re-read are all confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-repo edit → agentic-os | the CC gate is a write into the agentic-os repo |
| two live processes → one due row | main + aos both running; exactly-once must hold under real concurrency |
| migration → live DB at restart | the applied migration must precede restart or the bot crash-loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering / integrity | live double-fire across main + aos | mitigate | Verified explicitly in Task 2 step 3 against the cross-process claim path (claimDueTask changes===1 + agent-scoped reads). This is the SCH-04 acceptance gate. |
| T-07-12 | Denial of Service | migration registered but unapplied → bot crash-loop | mitigate | Task 1 runs npm run migrate BEFORE restart (D-09 deploy ordering); migrations/.applied.json confirms applied state before the service comes up. |
| T-07-13 | Denial of Service | aos service exit 78 from bad log path | mitigate | 07-03 forced /tmp/claudeclaw-aos.log; Task 1 verifies launchctl last exit code 0 (not 78) before declaring healthy. |
| T-07-14 | Repudiation | losing run history by deleting orphan rows | mitigate | Orphans are paused not deleted (07-02); last_result preserved — confirmed during verification. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- instrumentation.ts gates initCronScheduler behind CRON_IN_PROCESS.
- migration applied before restart; aos service healthy (exit 0, not 78).
- Live: one fire, DB-row + Slack parity, no preamble, no double-fire across processes, old engine off, prompt re-read on edit.
</verification>

<success_criteria>
- SCH-01: ClaudeClaw is the single runner; agentic-os cron engine disabled (CC gated, daemon stopped).
- SCH-02/SCH-03: a real cron/jobs/*.md job fires on schedule as the aos agent with DB-row + Slack parity.
- SCH-04: exactly-once proven against the cross-process claim path with both processes live.
- Success criterion 5: the schema change shipped as an applied versioned migration; bot restarted clean.
</success_criteria>

<output>
Create `.planning/phases/07-single-scheduler/07-05-SUMMARY.md` when done.
</output>
