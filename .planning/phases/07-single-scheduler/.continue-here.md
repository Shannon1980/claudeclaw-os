# Continue Here — Phase 07 (single-scheduler)

**Status:** 4/5 plans complete. Paused at plan 07-05 by user request.

## Done (committed on branch `claude/keen-margulis-7abd74`)
- 07-01 — migration v1.1.1 (aos-cron columns) + `claimDueTask` + aos-cron db helpers
- 07-02 — `src/aos-cron.ts` startup sync (`syncAosCronJobs`, parse, time/days→cron)
- 07-03 — `launchd/com.claudeclaw.aos.plist` standalone aos service (unloaded)
- 07-04 — scheduler firing branch (claim, fire-time re-read, timeout/retry, notify, no preamble) + boot wiring in `src/index.ts`

Build clean, 144/144 tests pass across db/aos-cron/scheduler suites.

## Remaining: 07-05 — live cutover (BLOCKING human-verify gate)
Deliberately NOT run autonomously: it requires the live `.env`/`store` (this worktree has neither) and real-world deploy actions. Must be driven on the **main checkout**.

Steps (from 07-05-PLAN.md, in order):
1. Edit `/Users/shannongueringer/App Repo/agentic-os/command-centre/src/instrumentation.ts` — wrap the dynamic import + `initCronScheduler()` behind `if (process.env.CRON_IN_PROCESS !== "0") { ... }` (D-01, opt-out). Cross-repo edit.
2. `npm run migrate` in claudeclaw (main checkout) BEFORE restart (D-09 — else `checkPendingMigrations` crash-loops).
3. Restart the main bot service.
4. Ensure `~/.claudeclaw-app` symlink points at the project; `launchctl load com.claudeclaw.aos`; verify `launchctl print gui/$(id -u)/com.claudeclaw.aos` shows state=running, last exit code 0 (NOT 78).
5. Do NOT run `scripts/start-crons.sh`; do NOT start CC server with scheduler enabled (D-02).

Live proof (Task 2): pick/temporarily-retime a fast active aos job, confirm single fire → row `last_run`/`last_status='success'`/`last_result` + Slack, no "Scheduled task running" preamble, no double-fire across main+aos (SCH-04), old engine off (SCH-01), and prompt re-read on a body edit without restart (D-07).

When all confirmed: write `07-05-SUMMARY.md`, mark 07-05 complete, then run phase verification (`gsd-verifier`) to close phase 07.
