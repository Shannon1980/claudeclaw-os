---
phase: 05-audit-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/migrations.test.ts
  - src/db.test.ts
  - src/audit-export.test.ts
  - src/dashboard.contract.test.ts
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "A failing test asserts migration v1.2.4 is registered in version.json and applies idempotently"
    - "A failing test asserts insertAuditLog persists the new columns and the filtered reader returns them"
    - "A failing test asserts /api/audit/export returns the full filtered set (not page-capped) with Content-Disposition"
    - "A failing test asserts the CSV serializer is RFC-4180 + formula-injection safe"
    - "A failing test asserts the retention window get/set with default 90 and input validation"
    - "A test asserts no DELETE FROM audit_log exists anywhere in src/ (append-only invariant)"
  artifacts:
    - path: "src/audit-export.test.ts"
      provides: "CSV RFC-4180 + injection unit tests + JSON envelope shape"
      min_lines: 30
    - path: "src/migrations.test.ts"
      provides: "v1.2.4 registration + idempotency assertion"
      contains: "v1.2.4"
    - path: "src/db.test.ts"
      provides: "audit insert/read with new fields + no-DELETE invariant"
      contains: "audit_log"
    - path: "src/dashboard.contract.test.ts"
      provides: "/api/audit enriched + /api/audit/export full-set contract tests"
      contains: "export"
  key_links:
    - from: "src/dashboard.contract.test.ts"
      to: "/api/audit/export"
      via: "app.request with ?token="
      pattern: "audit/export"
    - from: "src/db.test.ts"
      to: "audit_log columns"
      via: "insertAuditLog + filtered reader"
      pattern: "duration_ms|session_id|event_type"
---

<objective>
Wave 0 test scaffolding. Author the failing tests that pin this phase's data-layer and read/export contracts (AUD-01 schema/write/read, AUD-02 export/CSV-safety/retention, append-only invariant) BEFORE any production code exists. These tests define the contracts the implementation plans (02, 03, 04) build against.

Note (scope): the event-emission seams that only exist once their source code is written — auth (`oauth-health.test.ts`), routine (`routine-runner.test.ts`), error (`message-core.test.ts`), and the end-to-end model-capture assertion (`gate.test.ts`) — are authored TDD-style in their implementing plan (04), not here. Wave 0 is not "every automated behavior of the phase"; it is the schema/read/export/retention/CSV/append-only floor that later plans turn GREEN.

Purpose: Establish the Nyquist verification floor per 05-VALIDATION.md so every later task has a runnable `<automated>` command. No production code is written here — only tests that currently fail (RED), which later plans turn GREEN.
Output: Extended/created test files that fail against current source, each red for a missing-but-specified behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audit-log/05-VALIDATION.md
@.planning/phases/05-audit-log/05-RESEARCH.md
@.planning/phases/05-audit-log/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + schema + retention failing tests</name>
  <read_first>
    - src/migrations.test.ts (the file to extend — existing compareSemver/checkPendingMigrations describes)
    - src/db.test.ts (the file to extend — existing audit/db describes)
    - migrations/version.json (current max is v1.2.3 — confirmed; v1.2.4 unclaimed)
    - migrations/v1.2.3/create-approval-queue.ts (the migration skeleton the new v1.2.4 will mirror)
    - src/db.ts:332 (current thin audit_log: action, detail, blocked, agent_id, chat_id, created_at), :3113 insertAuditLog, :3135 getAuditLog, :3618 getDashboardSetting/setDashboardSetting, :861 _initTestDatabase
    - 05-PATTERNS.md § Pattern A (the exact 11 new columns) and § "src/permissions-config.ts retention get/set"
  </read_first>
  <action>
    Extend src/migrations.test.ts with a describe block "audit log migration v1.2.4" asserting: (a) version.json registers key `v1.2.4` mapping to `["enrich-audit-log"]`; (b) applying migrations against a fresh test DB is idempotent (running twice does not throw and leaves audit_log with all 11 new columns present via PRAGMA table_info). Assert the column set exactly: event_type, tool, target, project, decision, decided_by, decided_at, result, duration_ms, model, session_id. cost_usd MUST NOT be a column (resolved read-side per D-11).
    Extend src/db.test.ts with a describe "audit_log enriched columns" asserting: insertAuditLog persists all new fields (call the widened writer with an options object carrying eventType/tool/target/project/decision/decidedBy/decidedAt/result/durationMs/model/sessionId) and the filtered reader returns them; a NULL/omitted field reads back as null (honest absence, never fabricated). Add a separate test "audit_log is append-only" that greps src/ source text for `DELETE FROM audit_log` (filter out comment lines with grep -v '^[[:space:]]*//' style filtering or equivalent) and asserts zero matches.
    Add a retention test (in src/db.test.ts or alongside): getAuditRetentionDays() returns 90 when unset, returns the stored integer when set via setAuditRetentionDays(N), and setAuditRetentionDays rejects/ignores non-positive or non-integer input (returns default 90 on read). Use the dashboard_settings key `audit.retention_days` per 05-PATTERNS.md Pattern E.
    These reference functions do not exist yet — the tests MUST fail at import/assert time (RED). Do NOT stub the production functions.
  </action>
  <verify>
    <automated>npx vitest run src/migrations.test.ts src/db.test.ts 2>&1 | grep -E "audit log migration v1.2.4|audit_log enriched|append-only|retention" || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/migrations.test.ts -t "audit"` runs and the new audit-migration cases are RED (fail because v1.2.4 + new columns do not exist yet).
    - `npx vitest run src/db.test.ts -t "retention"` is RED (getAuditRetentionDays/setAuditRetentionDays not implemented).
    - The "append-only" test is authored and references `DELETE FROM audit_log` with comment-line filtering (no bare `== 0` on an unfiltered file).
    - No production source files (src/db.ts, src/security.ts, etc.) are modified by this task.
  </acceptance_criteria>
  <done>Migration-registration, enriched-column persist/read, no-DELETE invariant, and retention get/set tests exist and fail RED against current source.</done>
</task>

<task type="auto">
  <name>Task 2: Export + CSV-safety + enriched-read failing tests</name>
  <read_first>
    - src/dashboard.contract.test.ts:33-39 (the contract harness: _initTestDatabase() in beforeEach, app.request(path + '?token=' + TOKEN))
    - src/dashboard.ts:3548 GET /api/audit (current limit/offset/agent parsing), :2057 file-download precedent (new Response + Content-Disposition), :345 token middleware, :379 mutations kill-switch (GET exempt)
    - src/audit-export.test.ts (NEW file — does not exist; create it)
    - 05-RESEARCH.md § Pattern 5 (export download), § Pitfall 3 (CSV injection), § Pitfall 6 (export silently page-capped)
    - 05-PATTERNS.md § "src/dashboard.ts" (toCsv RFC-4180 contract: quote `, " \n \r`; escape `"`→`""`; prefix leading `= + - @` with `'`)
  </read_first>
  <action>
    Create src/audit-export.test.ts with unit tests for the (not-yet-existing) `toCsv` serializer exported from src/dashboard.ts (or src/db.ts — the import path the implementation will use; pick src/dashboard.ts and let plan 03 export it there). Cases: a field containing a comma is quoted; a field containing a double-quote has it doubled and the field quoted; a field containing `\n` or `\r` is quoted; a cell whose value starts with `=`, `+`, `-`, or `@` is prefixed with a single quote `'` (formula-injection neutralization) AND still RFC-4180 quoted if needed; a normal field is emitted unquoted. Also assert the JSON envelope shape: an object with keys `exported_at`, `count`, and `rows` (array). Use a representative audit_log.detail value containing comma + quote + newline + leading `=` in one cell.
    Extend src/dashboard.contract.test.ts with: (a) a "/api/audit enriched" test that pins Pitfall 4 (multi-rows-per-session): insert 3 audit rows sharing ONE session_id plus exactly 1 token_usage row for that session (one turn cost), then assert GET /api/audit returns ALL 3 rows each with cost_usd equal to that turn's value (NOT 0, NOT 3x the value — the JOIN must not fan-out or zero-out), and assert uncaptured fields come back null; (b) a "/api/audit/export" test inserting MORE rows than the read endpoint's default page size, calling GET /api/audit/export?format=csv and ?format=json with the token, asserting the response count equals the FULL filtered set (NOT page-capped, Pitfall 6), the Content-Disposition header is `attachment` with an `audit-<ts>.csv`/`.json` filename, the Content-Type is `text/csv`/`application/json`, and an invalid `format` value falls back to csv (or is rejected per the validation choice — assert csv fallback).
    All referenced endpoints/serializer do not exist yet — tests MUST be RED.
  </action>
  <verify>
    <automated>npx vitest run src/audit-export.test.ts src/dashboard.contract.test.ts -t "export" 2>&1 | grep -E "toCsv|export|enriched" || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/audit-export.test.ts` runs and is RED (toCsv not exported yet).
    - `npx vitest run src/dashboard.contract.test.ts -t "export"` is RED (/api/audit/export route absent).
    - The export contract test inserts strictly more rows than the read page size and asserts the FULL count is returned (Pitfall 6 pinned).
    - The enriched-read test inserts 3 audit rows sharing 1 session_id + 1 token_usage row and asserts each of the 3 rows returns cost_usd = that turn's value (not 0, not 3x) — Pitfall 4 (multi-rows-per-session) pinned.
    - The CSV test includes a single cell containing comma AND quote AND newline AND a leading `=` and asserts both RFC-4180 quoting and the `'` injection prefix.
    - No production source files are modified by this task.
  </acceptance_criteria>
  <done>Export full-set, CSV RFC-4180+injection, JSON envelope, and enriched-read cost-JOIN contract tests exist and fail RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author → test files | Tests must not embed real secrets/tokens; use the existing test TOKEN constant from the contract harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | test fixtures | mitigate | CSV/secret tests use synthetic values, never real env vars; the no-secrets test fixture uses a fake sentinel string |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (Vitest already present); no Package Legitimacy Gate applies |
</threat_model>

<verification>
- `npx vitest run src/migrations.test.ts src/db.test.ts src/audit-export.test.ts src/dashboard.contract.test.ts` runs; the newly-authored audit cases are RED, pre-existing cases stay GREEN.
- No file under src/ outside the four named test files is modified.
</verification>

<success_criteria>
Every AUD-01/AUD-02 automated behavior from 05-VALIDATION.md has a failing test that later plans turn green. Append-only invariant has a grep-based guard. Zero production code changed.
</success_criteria>

<output>
Create `.planning/phases/05-audit-log/05-01-SUMMARY.md` when done
</output>
