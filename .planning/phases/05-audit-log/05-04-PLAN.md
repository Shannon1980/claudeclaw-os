---
phase: 05-audit-log
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/agent.ts
  - src/message-core.ts
  - src/routine-runner.ts
  - src/oauth-health.ts
  - src/gate.ts
  - src/routine-runner.test.ts
  - src/message-core.test.ts
  - src/oauth-health.test.ts
  - src/gate.test.ts
autonomous: false
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "Routine runs emit a 'routine' audit event with outcome + duration at their source"
    - "Auth/oauth-health determinations emit an 'auth' audit event with the alert level"
    - "Caught errors emit an 'error' audit event with a capped, scrubbed category/message (no stack frames)"
    - "The agent turn boundary captures session_id, model, and duration and threads them via GateContext (no module globals)"
    - "Honest type chips now light up for the newly-captured event types because real data exists"
  artifacts:
    - path: "src/routine-runner.ts"
      provides: "'routine' event emission at outcome derivation"
      contains: "action: 'routine'"
    - path: "src/oauth-health.ts"
      provides: "'auth' event emission at health/expiry determination"
      contains: "action: 'auth'"
    - path: "src/message-core.ts"
      provides: "'error' event emission in catch + model/session capture for the turn"
      contains: "action: 'error'"
    - path: "src/agent.ts"
      provides: "turn-boundary session_id/model/duration capture threaded via GateContext"
      contains: "GateContext"
    - path: "src/oauth-health.test.ts"
      provides: "auth-emission unit seam (stubs checkOAuthHealth audit callback)"
      contains: "action: 'auth'"
  key_links:
    - from: "src/agent.ts turn boundary"
      to: "GateContext sessionId/model/_startMs"
      via: "per-turn thread, no module globals"
      pattern: "sessionId|model"
    - from: "src/routine-runner.ts outcome"
      to: "audit()"
      via: "routine event emission"
      pattern: "audit\\("
    - from: "src/message-core.ts catch"
      to: "audit()"
      via: "error event emission"
      pattern: "action: 'error'"
    - from: "GateContext model"
      to: "audit_log.model"
      via: "recordDecision → insertAuditLog → /api/audit (end-to-end)"
      pattern: "model"
---

<objective>
Slice C — widen instrumentation to every event type (D-11 full fidelity, D-12 new types). Emit the three missing event types (auth, routine, error) at their source modules, capture session_id/model/duration at the agent turn boundary and thread them through GateContext so non-permission events also carry cost/model/session, and confirm the honest type chips now light up because real backing data exists. Close with the end-of-phase human-verify checkpoint over the live Audit surface.

Purpose: The phase floor is "complete-for-what-it-claims". After plan 03 only permission events were fully instrumented; this plan delivers the full type set so the spec's `Actions · Permissions · Auth · Errors · Routines` chips have real data, honoring D-11/D-12.
Output: auth/routine/error emissions, turn-boundary capture for all event types, extended emission tests GREEN (including a real auth-emission unit seam and an end-to-end model-capture assertion), and an operator sign-off on the live surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audit-log/05-RESEARCH.md
@.planning/phases/05-audit-log/05-PATTERNS.md
@.planning/phases/05-audit-log/05-UI-SPEC.md
@.planning/phases/05-audit-log/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit auth / routine / error events + turn-boundary capture</name>
  <read_first>
    - src/routine-runner.ts:84 runRoutineOnce entry (duration start), :105 chatId precedent (ALLOWED_CHAT_ID || 'routine'), :161 outcome derivation (deriveOutcome)
    - src/oauth-health.ts:43 checkOAuthHealth + lastAlertLevel (none|warning|expired)
    - src/message-core.ts:394 model resolution (effectiveModel), :616 saveTokenUsage, :661 catch(err), :667 AgentError branch with err.category
    - src/agent.ts:287 query() boundary, :418 result event (newSessionId), :80 UsageInfo (no model), :134 AgentResult
    - src/gate.ts:145 GateContext (sessionId/model/_startMs added in plan 02 — thread the turn values in here), :179-191 recordDecision (model already mapped onto the audit entry in plan 02)
    - src/gate.test.ts:146 "audit recorded", :159/:171 audit-shape asserts, :184 no-secrets assert (the anchor for the model end-to-end assertion)
    - src/security.ts AuditAction union (now includes auth|routine|error from plan 02) + AuditEntry
    - src/routine-runner.test.ts / src/message-core.test.ts (the emission tests to extend)
    - src/oauth-health.test.ts (NEW seam — if absent, create it; stub checkOAuthHealth's audit() callback and assert the 'auth' entry shape)
    - 05-RESEARCH.md § "Emitting a new event type" examples + § Pattern 4 (duration boundaries) + Open Q3 (auth refresh observability)
    - 05-PATTERNS.md § "routine-runner / oauth-health / message-core" + § "src/agent.ts" + § Pattern D (no secrets / cap messages / no stack frames)
  </read_first>
  <behavior>
    - Test: routine-runner emits one audit({action:'routine', eventType:'routine', result: outcome, durationMs, blocked: outcome==='failed'}) per run after outcome derivation.
    - Test: message-core catch(err) emits audit({action:'error', eventType:'error', detail JSON {category, message capped 500}, result:'error', blocked:false}) with no stack frames and no secrets.
    - Test: oauth-health emits audit({action:'auth', eventType:'auth', detail {event:'oauth_check', level}, blocked: level==='expired'}). Assert via a unit seam: inject/stub the audit() callback into checkOAuthHealth (or its module-level audit hook) and assert the emitted entry shape for each level (none/warning/expired) — this is the concrete automated seam VALIDATION maps to.
    - Test (end-to-end model capture): fire a permission decision inside a GateContext carrying a known model value (e.g. 'claude-test-model'); assert the resulting audit_log row read back via insertAuditLog/getAuditLog (or the recordDecision → audit() entry) has model === 'claude-test-model' (not null). This proves the agent.ts → GateContext.model → recordDecision → insertAuditLog → /api/audit chain, not a synthetic fixture row.
  </behavior>
  <action>
    Emit 'routine' in src/routine-runner.ts: stamp a start time at runRoutineOnce entry (:84), and after outcome derivation (:161) call audit() with action 'routine', eventType 'routine', detail JSON {routineId, outcome, steps}, result = outcome, durationMs = Date.now() - startedAt, blocked = outcome === 'failed', chatId = ALLOWED_CHAT_ID || 'routine', agentId 'main'.
    Emit 'auth' in src/oauth-health.ts checkOAuthHealth (:43): at each expiry/refresh determination call audit() with action 'auth', eventType 'auth', detail JSON {event:'oauth_check', level: lastAlertLevel}, blocked = lastAlertLevel === 'expired'. durationMs legitimately omitted (point-in-time → null → "not captured"). Per Open Q3, if true SDK-internal refreshes are not observable, only emit at the health-check determinations we control (the coverage banner already states honesty). The audit() call MUST go through an injectable/module-level hook so the unit seam can stub and assert it (see below).
    Emit 'error' in src/message-core.ts catch(err) (:661): alongside the existing logger.error, call audit() with action 'error', eventType 'error', detail JSON {category: err instanceof AgentError ? err.category : 'unknown' (reuse the :667 branch), message: String(err).slice(0,500)}, result 'error', blocked false. NEVER include stack frames or env/secrets (Pattern D); cap the message.
    Turn-boundary capture in src/agent.ts (:287/:418) + src/message-core.ts (:394): capture session_id (result.newSessionId ?? sessionId), model (effectiveModel resolved at :394), and start/stop ms for the turn, and thread them into GateContext (the fields plan 02 added) — per-turn, NO module globals (multi-agent concurrency / Phase 3 D-09 rule). This is what lets permission rows (and any per-turn event) resolve cost/model/session. Define the start boundary at the turn entry and stop at the result event per RESEARCH § Pattern 4.
    Test seams:
      - Auth seam: add (or create) src/oauth-health.test.ts that stubs the audit() callback used by checkOAuthHealth (the injectable hook above) and asserts the emitted entry shape for each alert level — action 'auth', eventType 'auth', detail {event:'oauth_check', level}, blocked === (level==='expired'). This is the real automated coverage for the auth row (prefer this over manual-only).
      - Routine/error seams: extend src/routine-runner.test.ts and src/message-core.test.ts to assert the new emissions (shape + no-secrets + capped message).
      - Model end-to-end seam: extend src/gate.test.ts (anchor at the :184 no-secrets seam) — construct a GateContext with model = a known sentinel, run a permission decision through recordDecision, and assert the audit_log row (read back via getAuditLog after insertAuditLog, or the captured audit() entry) carries model === that sentinel and is non-null. This closes the agent.ts → GateContext.model → recordDecision → insertAuditLog → /api/audit chain end-to-end (no synthetic fixture row).
  </action>
  <verify>
    <automated>npx vitest run src/routine-runner.test.ts src/message-core.test.ts src/oauth-health.test.ts src/gate.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/routine-runner.test.ts src/message-core.test.ts` GREEN: routine + error emissions assert correct shape.
    - `npx vitest run src/oauth-health.test.ts` GREEN: the auth-emission seam stubs checkOAuthHealth's audit() callback and asserts the 'auth' entry shape (action/eventType/detail.level/blocked) for none/warning/expired. This is the automated auth coverage (not manual-only).
    - `npx vitest run src/gate.test.ts` GREEN including the end-to-end model-capture assertion: a permission decision in a GateContext carrying a known model yields an audit_log row whose model equals that value (non-null), proving the GateContext.model → recordDecision → insertAuditLog → /api/audit chain.
    - The error emission contains no stack frames and caps message at 500 chars (source assertion).
    - session_id/model/duration are threaded via GateContext, not a module-level mutable (git diff shows no new module global for turn context).
    - All new emissions go through the single audit() choke point (no second logger added).
    - `npx vitest run` (full suite) is GREEN.
  </acceptance_criteria>
  <done>auth, routine, and error events emit at their sources through the single audit() pipeline; the auth emission has a real unit seam (oauth-health.test.ts); the model-capture chain is proven end-to-end in gate.test.ts; the turn boundary captures session/model/duration via GateContext; emission tests and the full suite are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-of-phase human verification of the live Audit surface</name>
  <what-built>
    The complete Phase 5 Audit surface: enriched append-only audit_log (11 new columns, both DB paths via v1.2.4), full-fidelity instrumentation across permission/routine/auth/error events with read-side cost resolution, the dense technical Audit surface relocated under Settings > Security with honest "not captured" detail and honest type chips, a stated configurable retention window, and complete-filtered-set CSV/JSON export.
  </what-built>
  <read_first>
    - 05-VALIDATION.md § Manual-Only Verifications (the exact checklist below)
    - 05-UI-SPEC.md (the visual contract to judge against)
  </read_first>
  <how-to-verify>
    Precondition: the operator runs `npm run migrate` on the main checkout BEFORE restarting the live service (v1.2.4 must apply, or checkPendingMigrations crash-loops — STATE.md deploy rule). The worktree does not touch the live store.
    1. Open the relocated Audit surface under Settings > Security. Confirm it is dense and monospace and visually unlike the Activity feed (cards). It must not appear in the daily sidebar but must be reachable here and at /audit deep-link.
    2. Expand a pre-migration (old) row. Confirm uncaptured fields show the literal "not captured" in faint text, never a blank cell.
    3. Confirm type chips: only event types with real data are active; spec types with no data are disabled with a "not yet captured" footnote. After triggering a routine/permission/error, confirm the corresponding chips light up.
    4. Confirm the header shows "Retaining 90 days" (the configured value, not a hardcoded literal). [AUD-02 stated-retention manual item]
    5. Apply a filter (search or type or date range), click Export log → Export as CSV, then again → Export as JSON. Confirm each downloads a file whose row count matches the FULL filtered set (not just the loaded page). Open the CSV; confirm columns are aligned even where detail contains commas/quotes/newlines, and no cell executes as a formula. [AUD-02 full-set export manual item]
    6. Confirm there is no delete/edit/clear affordance anywhere on the surface (append-only, read-only is the contract).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caught error object → audit detail | Error strings can carry file paths / secrets / stack frames |
| per-turn identity → audit rows | Turn context (session/model) must not leak across concurrent agents |
| live migration → running service | v1.2.4 must apply before restart or the service crash-loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | message-core error emission | mitigate | Cap message at 500 chars; no stack frames; reuse AgentError.category; no env/secrets (Pattern D) |
| T-05-11 | Spoofing | turn context across agents | mitigate | session_id/model threaded via GateContext per-turn; no module globals (Phase 3 D-09 concurrency rule) |
| T-05-12 | Denial of Service | live v1.2.4 migration | mitigate | Operator runs `npm run migrate` before restart (checkpoint precondition); idempotent migration verified in plan 02 |
| T-05-13 | Tampering | audit_log via new emitters | mitigate | All emissions are INSERT via the single audit() path; zero UPDATE/DELETE on audit_log |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; no Package Legitimacy Gate applies |
</threat_model>

<verification>
- `npx vitest run` full suite GREEN (all Wave 0 tests now green, no regressions).
- The auth emission is covered by an automated unit seam (src/oauth-health.test.ts), and the model-capture chain is covered end-to-end (src/gate.test.ts) — neither relies on the human checkpoint.
- Operator human-verify checkpoint approved (the manual-only items from 05-VALIDATION.md, including the AUD-02 full-set export and stated-retention checks).
- All event types emit via the single audit() choke point; no module globals for turn context.
</verification>

<success_criteria>
Every audited event type (permission, routine, auth, error, plus existing message/command/delegation/kill/blocked/config) carries the new technical fields at full fidelity, cost/model/session resolve per turn, the honest type chips reflect real data, and the operator has signed off on the live, complete-for-what-it-claims Audit surface. AUD-01 is fully met and the AUD-02 manual items (full-set export, stated retention) are confirmed.
</success_criteria>

<output>
Create `.planning/phases/05-audit-log/05-04-SUMMARY.md` when done
</output>
