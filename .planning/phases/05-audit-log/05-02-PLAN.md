---
phase: 05-audit-log
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/db.ts
  - src/security.ts
  - src/index.ts
  - src/gate.ts
  - src/permissions-config.ts
  - migrations/v1.2.4/enrich-audit-log.ts
  - migrations/version.json
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "audit_log carries 11 new nullable columns in BOTH the in-memory test DB and the v1.2.4 migration (P-4 dual-write)"
    - "Every event flows through one widened audit() choke point that persists the new fields"
    - "A permission decision records tool, target (scrubbed), decision, decided_by, decided_at, result, duration_ms, session_id, model — no secrets in detail/target"
    - "The retention window is stored in dashboard_settings and reads back with a default of 90 days"
    - "audit_log has zero UPDATE/DELETE statements (append-only)"
  artifacts:
    - path: "migrations/v1.2.4/enrich-audit-log.ts"
      provides: "Live-store dual-write migration adding the 11 columns idempotently"
      contains: "ALTER TABLE audit_log ADD COLUMN"
    - path: "src/security.ts"
      provides: "AuditAction union + AuditEntry interface extended with optional captured fields"
      contains: "eventType"
    - path: "src/db.ts"
      provides: "audit_log schema enrich (addColumnIfMissing) + widened insertAuditLog + retention get/set"
      contains: "audit.retention_days"
    - path: "src/gate.ts"
      provides: "recordDecision captures enriched permission fields via GateContext"
      contains: "durationMs"
  key_links:
    - from: "src/index.ts"
      to: "insertAuditLog"
      via: "setAuditCallback maps every optional field"
      pattern: "insertAuditLog"
    - from: "migrations/version.json"
      to: "migrations/v1.2.4/enrich-audit-log.ts"
      via: "version registration"
      pattern: "v1.2.4"
    - from: "src/gate.ts recordDecision"
      to: "audit()"
      via: "enriched permission audit entry"
      pattern: "audit\\("
---

<objective>
Slice A — the thin end-to-end data spine. Enrich the audit_log schema with the 11 per-event columns (dual-write: createSchema/runMigrations AND migrations/v1.2.4), widen the single audit() choke point (union, interface, callback, writer) to carry them, fully instrument ONE event type (permission decisions) write-side via GateContext, and add the configurable retention window. This is the MVP floor: after this plan a permission event is captured at full fidelity end-to-end at the data layer, with NULLs honestly stored for everything not yet captured.

Purpose: D-01/D-11 reopen the write path; this lands the schema + choke-point widening + the first fully-instrumented event type so plans 03 (read/export/UI) and 04 (widen to all event types) build on real captured data, not a half-wired schema.
Output: Enriched append-only audit_log (both DB paths), a widened audit pipeline, enriched permission rows, retention config — turning the Task-1 Wave 0 unit tests (migration, db, retention) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audit-log/05-RESEARCH.md
@.planning/phases/05-audit-log/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-write schema migration + widen the audit choke point</name>
  <read_first>
    - src/db.ts:332-342 (inline audit_log CREATE TABLE), :526-540 addColumnIfMissing, :601-615 (the aos-cron addColumnIfMissing precedent to mirror in runMigrations), :3113-3123 insertAuditLog (INSERT-only), :3125 AuditLogEntry interface, :861 _initTestDatabase
    - src/security.ts:87 AuditAction union, :95 AuditEntry interface, :105 setAuditCallback, :109 audit()
    - src/index.ts:161 setAuditCallback → insertAuditLog (the single mapping point)
    - migrations/v1.2.3/create-approval-queue.ts (the exact skeleton to copy: own better-sqlite3 handle at process.cwd()/store/claudeclaw.db, try/finally, description export + run())
    - migrations/version.json (append v1.2.4)
    - 05-PATTERNS.md § Pattern A (the 11 columns + the verbatim v1.2.4 file body) and § Pattern B (extend, never fork)
  </read_first>
  <behavior>
    - Test: migrations.test.ts asserts version.json registers v1.2.4 → ["enrich-audit-log"] and applying it is idempotent (all 11 columns present, second run no-throw).
    - Test: db.test.ts asserts insertAuditLog persists eventType/tool/target/project/decision/decidedBy/decidedAt/result/durationMs/model/sessionId and the reader returns them; omitted fields read back null.
    - Test: db.test.ts "append-only" grep finds zero `DELETE FROM audit_log` in src/.
  </behavior>
  <action>
    In src/db.ts runMigrations(), mirror the :601-615 aos-cron precedent: add `addColumnIfMissing(database, 'audit_log', <col>, <type>)` for all 11 columns — event_type TEXT, tool TEXT, target TEXT, project TEXT, decision TEXT, decided_by TEXT, decided_at INTEGER, result TEXT, duration_ms INTEGER, model TEXT, session_id TEXT. Do NOT add cost_usd (resolved read-side, D-11). All nullable (no NOT NULL DEFAULT) so pre-migration rows read null. Per 05-PATTERNS.md Pattern A, match the existing precedent (addColumnIfMissing only; do not edit the inline CREATE TABLE block — planner's call, taken: addColumnIfMissing for consistency).
    Create migrations/v1.2.4/enrich-audit-log.ts by copying the v1.2.3 skeleton verbatim and replacing the body with PRAGMA-table_info-guarded `ALTER TABLE audit_log ADD COLUMN` for the SAME 11 columns in the SAME order/types (SQLite has no per-column IF NOT EXISTS). Export `description` and async `run()`. The DDL MUST be byte-identical in column names/types to the addColumnIfMissing calls (drift = Pitfall 1 crash-loop). Register `"v1.2.4": ["enrich-audit-log"]` in migrations/version.json.
    In src/security.ts: extend AuditAction with `'auth' | 'routine' | 'error'` (D-12; emissions land in plan 04 but the union ships now). Extend AuditEntry with optional fields: eventType?, tool?, target?, project?, decision?, decidedBy?, decidedAt?, result?, durationMs?, model?, sessionId?. audit() itself needs no change (it forwards the whole entry).
    Widen insertAuditLog in src/db.ts to accept the new fields — convert to an options object once args exceed ~5 — keep `detail.slice(0, 2000)` and keep it INSERT-only (no UPDATE/DELETE ever). Update AuditLogEntry interface (:3125) with the new nullable columns. Update src/index.ts:161 callback to map every optional entry field into the widened insertAuditLog (the single mapping point — do not add a second writer).
  </action>
  <verify>
    <automated>npx vitest run src/migrations.test.ts src/db.test.ts -t "audit"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/migrations.test.ts -t "audit"` GREEN: v1.2.4 registered + idempotent + all 11 columns present.
    - `npx vitest run src/db.test.ts -t "audit"` GREEN: enriched insert/read works; omitted fields null; no-DELETE invariant holds.
    - migrations/v1.2.4/enrich-audit-log.ts column DDL is identical (names + types) to the src/db.ts addColumnIfMissing calls.
    - cost_usd is NOT a column anywhere in audit_log.
    - insertAuditLog contains no UPDATE or DELETE statement.
  </acceptance_criteria>
  <done>audit_log has the 11 new columns in both DB paths; the audit pipeline (union/interface/callback/writer) carries them; migration v1.2.4 registered and idempotent; append-only preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument permission decisions + retention config</name>
  <read_first>
    - src/gate.ts:135 summarize(), :145 GateContext, :169 encodeDecision, :179-191 recordDecision, :203 makeCanUseTool
    - src/gate.test.ts:146 "audit recorded", :159/:171 audit-shape asserts, :184-194 no-secrets assert
    - src/security.ts:139-209 getScrubbedSdkEnv / drop-vars (the secret boundary), :193
    - src/db.ts:3618-3628 getDashboardSetting/setDashboardSetting
    - src/permissions-config.ts:32-50 getMode/setMode (the exact config get/set + audit-on-write template to mirror)
    - 05-RESEARCH.md § "Enriched permission decision (D-01)" + Open Q2 (target extraction) + § Pattern 6 (retention)
    - 05-PATTERNS.md § "src/gate.ts" (recordDecision widening, do NOT touch classifyTier/resolveOutcome) + § Pattern D (no-secrets) + § Pattern E (retention)
  </read_first>
  <behavior>
    - Test: gate.test.ts asserts a permission audit row carries tool, target (whitelisted/scrubbed), decision (=outcome), decided_by (operator for inline, system otherwise), decided_at, duration_ms, session_id, model.
    - Test: gate.test.ts no-secrets assert (extended to cover the new `target` field) confirms no env/secret value lands in detail OR target.
    - Test: retention get/set returns default 90 when unset, the stored value when set, and rejects non-positive/non-integer input.
  </behavior>
  <action>
    Add a `_startMs` (number) and `sessionId`/`model` (string) to GateContext in src/gate.ts (per-turn carrier, NO module globals). Stamp `Date.now()` into `_startMs` at the top of makeCanUseTool (:203). Widen recordDecision (:179) to call audit() with the enriched entry: action 'permission', eventType 'permission', detail = encodeDecision(d) (unchanged), tool = d.tool, target = safeTarget(input) via a small per-tool whitelist extractor (mirror summarize()'s tool→field mapping; default to omitting → "not captured" rather than dumping raw input — Open Q2), decision = d.outcome, decidedBy = d.outcome.includes('inline') ? 'operator' : 'system', decidedAt = Date.now(), durationMs = ctx._startMs ? Date.now() - ctx._startMs : undefined, sessionId = ctx.sessionId, model = ctx.model. Do NOT modify classifyTier or resolveOutcome (D-01 records, it does not re-decide — out of scope). Keep gate.test.ts:159/171/184 passing; extend the no-secrets assertion to the new target field.
    safeTarget MUST only emit whitelisted, model-supplied, non-secret fields (e.g. file_path for Write, to for email-like tools); never the raw input object, never env. Cap any string at a sane length.
    Add retention config to src/permissions-config.ts (or a sibling audit-config.ts — planner's call, taken: permissions-config.ts to reuse the existing module): getAuditRetentionDays() reads dashboard_settings key `audit.retention_days`, parseInt, returns it when a positive integer else default 90. setAuditRetentionDays(days, agentId='main') validates days is a positive integer, calls setDashboardSetting, and emits a config-change audit (action 'permission', detail JSON {event:'retention_change', days}, blocked false) mirroring setMode. NO DELETE anywhere (D-31 — state, never prune this phase).
  </action>
  <verify>
    <automated>npx vitest run src/gate.test.ts && npx vitest run -t "retention"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/gate.test.ts` GREEN including the extended enriched-detail and no-secrets-in-target assertions.
    - `npx vitest run -t "retention"` GREEN: default 90, stored value round-trips, invalid input rejected.
    - classifyTier and resolveOutcome are unchanged (git diff shows no edits to those functions).
    - safeTarget never returns the raw input object and never an env/secret value.
    - GateContext carries _startMs/sessionId/model; no module-level mutable was added for turn context.
    - `npx vitest run src/db.test.ts -t "append-only"` is GREEN after `setAuditRetentionDays` is added (the retention setter introduces no DELETE/UPDATE on audit_log; the append-only invariant is re-checked).
  </acceptance_criteria>
  <done>Permission decisions record enriched, scrubbed technical detail with duration/session/model via GateContext; retention window is configurable, defaults to 90, and is never used to delete rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-supplied tool input → audit detail/target | Untrusted, possibly secret-bearing params cross into the persisted log here |
| live store schema → running service | A schema/migration mismatch crash-loops the service on restart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | gate.ts recordDecision safeTarget / detail | mitigate | safeTarget whitelists known non-secret fields, defaults to omit; gate.test.ts no-secrets assertion extended to cover target; never store raw input or env |
| T-05-03 | Tampering | audit_log writer | mitigate | insertAuditLog stays INSERT-only; zero UPDATE/DELETE on audit_log (db.test.ts grep guard); append-only hard rule |
| T-05-04 | Denial of Service | migration v1.2.4 vs live store | mitigate | Dual-write identical DDL (P-4); idempotent PRAGMA-guarded ADD COLUMN; migrations.test.ts asserts idempotency so checkPendingMigrations does not crash-loop |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; no Package Legitimacy Gate applies |
</threat_model>

<verification>
- `npx vitest run src/migrations.test.ts src/db.test.ts src/gate.test.ts` GREEN.
- `npx vitest run -t "retention"` GREEN.
- Migration DDL parity: the 11 columns in migrations/v1.2.4/enrich-audit-log.ts match src/db.ts addColumnIfMissing exactly (names + types).
- No UPDATE/DELETE on audit_log anywhere in src/ or migrations/.
</verification>

<success_criteria>
The audit_log schema is enriched in both DB paths, the single audit() pipeline carries the new fields, permission events are fully instrumented with scrubbed technical detail, and a stated/configurable retention window exists with no deletion. Wave 0 migration/db/retention/gate tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-audit-log/05-02-SUMMARY.md` when done
</output>
