---
phase: 05-memory-projection-capture
plan: 02
status: complete
requirements: [MEM-03, MEM-04, MEM-06]
---

# Plan 05-02 Summary — Live terminal/bot memory round-trip

## What was done

Closed the human-verify wave for Phase 5: the projection (out) and capture (in)
directions are wired into the live agentic-os workspace and verified end-to-end
by the user (both directions confirmed 2026-06-15).

### Deployment / wiring (Task 1, auto)
- Symlink live: `~/.claudeclaw-app` → `/Users/shannongueringer/App Repo/claudeclaw`.
- Built dist artifacts present: `dist/capture-cli.js`, `dist/memory-projection.js`.
- agentic-os `.claude/settings.json`:
  - **Stop** hook runs `node "$HOME/.claudeclaw-app/dist/capture-cli.js"` (capture in).
  - **SessionStart** hook runs `load-memory-snapshot.js` (loads the projection).
- `fix(05): capture-cli awaits memory ingestion so terminal work persists as a
  recallable memory (MEM-04)` landed — capture now awaits ingestion.

### Live proof (Task 2, human-verify — blocking gate)
- **MEM-03 (projection out):** `agentic-os/context/memory/2026-06-15.claudeclaw.md`
  exists, header marks it as a one-directional derived projection from ClaudeClaw
  SQLite, and it reflects current bot memory — it contains the standing tagline
  preference written during the 04-02 live proof ("User prefers taglines to be 5
  words or fewer"), plus the oncall and Q3-launch facts. A fresh terminal session
  reads it at SessionStart; the human-authored `{date}.md` is not clobbered.
- **MEM-04 (capture in):** user confirmed terminal-session work is captured into
  the single store under `(ws:aos, aos)` and recalled by a later bot `@aos:` turn.

## Requirements satisfied
- **MEM-03** — terminal reads a projection of recent ClaudeClaw memories. ✓
- **MEM-04** — terminal work captured into ClaudeClaw memory, recalled by the bot. ✓
- **MEM-06** — projection/capture read and write through ClaudeClaw's own
  access path against plaintext `memories`/`conversation_log` columns (no raw
  ciphertext reads). ✓

## Notes
- The projection is strictly out-only and regenerated by the bot; terminal must
  not hand-edit `*.claudeclaw.md`.
- This closes Phase 5. Combined with 04-02, the ClaudeClaw-only recall path
  (SQLite + projection) is proven live in both modes — the prerequisite for
  Phase 6 (memsearch retirement) to remove the second index without losing
  fallback.

## CORRECTION (2026-06-15, during Phase 6 research)

The MEM-04 capture direction is **not durably wired**. `git grep` across all
agentic-os revisions shows `capture-cli` was never committed; the committed
`.claude/settings.json` Stop hook runs only `session-sync-stop.js`, not
ClaudeClaw's `dist/capture-cli.js`. The MEM-04 close above was based on a
transient, uncommitted local edit that has since been reverted. **MEM-03
(projection read via committed `load-memory-snapshot.js`) and MEM-06 stand;
MEM-04 is re-opened** and folded into Phase 6 (wire + commit the capture Stop
hook in agentic-os settings.json, then re-verify the bidirectional round-trip).
