---
phase: 05-memory-projection-capture
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - /Users/shannongueringer/App Repo/agentic-os/.claude/settings.json
  - /Users/shannongueringer/App Repo/agentic-os/.claude/hooks/load-memory-snapshot.js
autonomous: false
requirements: [MEM-03, MEM-04, MEM-06]

must_haves:
  truths:
    - "dist/capture-cli.js exists (built) and is reachable from a Stop-hook command via a space-free path (the ~/.claudeclaw-app symlink)"
    - "The agentic-os Stop hook array invokes the capture CLI in addition to the existing run-ccnotify + session-sync-stop entries"
    - "load-memory-snapshot.js loads context/memory/{date}.claudeclaw.md at terminal SessionStart"
    - "A live terminal session in the workspace reads a projection reflecting recent ClaudeClaw memories (MEM-03)"
    - "Work done in a live terminal session is captured into ClaudeClaw memory and recalled by a later bot @aos: turn (MEM-04)"
  artifacts:
    - path: "/Users/shannongueringer/App Repo/agentic-os/.claude/settings.json"
      provides: "Stop array entry invoking the capture CLI"
      contains: "capture-cli"
    - path: "/Users/shannongueringer/App Repo/agentic-os/.claude/hooks/load-memory-snapshot.js"
      provides: "claudeclaw.md added to the SessionStart loader targets"
      contains: "claudeclaw.md"
  key_links:
    - from: "agentic-os/.claude/settings.json Stop"
      to: "dist/capture-cli.js"
      via: "node command on the ~/.claudeclaw-app symlink path"
      pattern: "capture-cli"
    - from: "load-memory-snapshot.js"
      to: "context/memory/{date}.claudeclaw.md"
      via: "SessionStart target list"
      pattern: "claudeclaw.md"
---

<objective>
Make the bridge actually fire end to end: build ClaudeClaw, create a space-free symlink for the Stop-hook command, wire the agentic-os Stop hook to the capture CLI, add the projection file to the terminal's SessionStart loader, then prove both halves live.

Purpose: Plan 01 produced the code with unit proofs; MEM-03/MEM-04 are only satisfied when a real terminal session reads the projection and a real terminal session's work is captured and recalled by the bot. This is the human-verify wave.

Output: Cross-repo edits in the agentic-os workspace (tracked in agentic-os git, OUTSIDE this repo — captured verbatim in the SUMMARY), a built `dist/`, the `~/.claudeclaw-app` symlink, and two live verification transcripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-memory-projection-capture/05-RESEARCH.md
@.planning/phases/05-memory-projection-capture/05-01-PLAN.md
@CLAUDE.md
</context>

<design_notes>
- The agentic-os repo path contains a space, and launchd/Stop-hook command paths break on spaces (CLAUDE.md launchd rule). Use the documented `~/.claudeclaw-app` symlink to the claudeclaw repo and reference `dist/capture-cli.js` through it. Quote the command regardless.
- The Stop hook stdin schema (`session_id`, `last_assistant_message`) is confirmed in `session-sync-stop.js`; `transcript_path`/`cwd` are best-effort (research A4 MEDIUM). The capture CLI only requires `session_id` + `last_assistant_message`, both confirmed present.
- These edits are in the agentic-os repo, OUTSIDE this repo (like prior phases). Capture the exact final JSON/JS diffs verbatim in the SUMMARY so they are reproducible.
- Do NOT wire `src/hooks.ts`. Roadmap success criterion 4 ("hooks not dead code") is satisfied by this Stop hook firing — note that reinterpretation.
</design_notes>

<tasks>

<task type="auto">
  <name>Task 1: Build + space-free symlink + cross-repo hook wiring</name>
  <files>/Users/shannongueringer/App Repo/agentic-os/.claude/settings.json, /Users/shannongueringer/App Repo/agentic-os/.claude/hooks/load-memory-snapshot.js</files>
  <read_first>
    - .planning/phases/05-memory-projection-capture/05-01-PLAN.md (capture-cli stdin contract; projection filename {date}.claudeclaw.md)
    - /Users/shannongueringer/App Repo/agentic-os/.claude/settings.json (Stop array: run-ccnotify.js + session-sync-stop.js)
    - /Users/shannongueringer/App Repo/agentic-os/.claude/hooks/load-memory-snapshot.js (targets list ~67-88; daily-log resolution ~76-88)
    - CLAUDE.md (launchd / space-free path rule; ~/.claudeclaw-app symlink)
  </read_first>
  <action>
    Run `npm run build` in the claudeclaw repo so `dist/capture-cli.js` exists; confirm the file is present.

    Create (or confirm) the space-free symlink `~/.claudeclaw-app` pointing at the claudeclaw repo root (the documented pattern; the repo path has a space). Verify `~/.claudeclaw-app/dist/capture-cli.js` resolves.

    Edit the agentic-os `.claude/settings.json` Stop array: append a third hook command that runs the capture CLI via node on the symlinked, quoted path (e.g. node on `"$HOME/.claudeclaw-app/dist/capture-cli.js"`). Do NOT remove or reorder the existing `run-ccnotify.js` / `session-sync-stop.js` entries. Validate the JSON parses after the edit.

    Edit the agentic-os `load-memory-snapshot.js`: in the daily-log resolution block (~line 76-88), additionally resolve and push `context/memory/{today}.claudeclaw.md` (plus the yesterday fallback variant) into the `targets` list with a clear ClaudeClaw label, mirroring the existing `{today}.md` resolution so the terminal reads the projection at SessionStart. Keep it fire-and-forget (existing try/catch pattern).

    Capture both final file contents verbatim for the SUMMARY (cross-repo, outside this repo). No schema change; do not touch ClaudeClaw migrations.
  </action>
  <verify>
    <automated>test -f "$HOME/.claudeclaw-app/dist/capture-cli.js" && node -e "JSON.parse(require('fs').readFileSync(process.env.HOME + '/App Repo/agentic-os/.claude/settings.json','utf8'))" && grep -q "claudeclaw.md" "$HOME/App Repo/agentic-os/.claude/hooks/load-memory-snapshot.js" && grep -q "capture-cli" "$HOME/App Repo/agentic-os/.claude/settings.json" && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `dist/capture-cli.js` exists and resolves through `~/.claudeclaw-app/dist/capture-cli.js`.
    - agentic-os `settings.json` still parses as JSON, retains the two original Stop entries, and adds the capture-cli entry on a quoted, space-free path.
    - `load-memory-snapshot.js` targets list now resolves `{date}.claudeclaw.md`.
    - Verify command prints `WIRED`.
  </acceptance_criteria>
  <done>ClaudeClaw is built, the symlink resolves, the Stop hook invokes the capture CLI, and the SessionStart loader reads the projection file. Cross-repo file contents captured for the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live MEM-03 + MEM-04 round-trip</name>
  <what-built>
    The full bridge: an inline projection writer that emits `context/memory/{date}.claudeclaw.md` after a delegated `@aos:` turn, a SessionStart loader that reads it, and a Stop hook that captures a terminal session into the shared `(ws:aos, aos)` pool which the bot then recalls.
  </what-built>
  <how-to-verify>
    Restart the bot so it runs the built code with the Plan 01 changes (per the project's deploy/restart path).

    MEM-03 (projection out):
    1. Send a bot message `@aos: remember that the Q3 launch date is October 14` (or any distinctive standing fact).
    2. Confirm the bot replies normally.
    3. In a terminal, open a Claude Code session with cwd = the agentic-os repo. Confirm the SessionStart context includes a "ClaudeClaw memory projection" section, and that the projected memory reflects the fact (or recent aos memories). Also confirm `context/memory/{today}.claudeclaw.md` exists and the agent's own `{today}.md` session blocks are intact (not clobbered).

    MEM-04 (capture in):
    4. In that terminal session, do a small distinctive task (e.g. "note that the deploy runbook lives at docs/deploy.md"). Let the turn finish so the Stop hook fires.
    5. Confirm a row landed: query `memories`/`conversation_log` for `agent_id='aos'` + `chat_id='ws:aos'` containing that content (the convolife/checkpoint sqlite patterns in CLAUDE.md show the DB path).
    6. After `/newchat`, send a bot turn `@aos: where does the deploy runbook live?` and confirm the bot recalls the terminal-captured fact.

    Regression: confirm a non-aos agent and a normal (non-@aos) bot message behave exactly as before (COMPAT-02).
  </how-to-verify>
  <resume-signal>Type "approved" with the two transcripts (projection read + capture recall), or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal Stop hook command -> dist/capture-cli.js | A Claude Code config in the workspace repo invokes a ClaudeClaw binary across the repo boundary via a symlinked path |
| agentic-os workspace files -> terminal SessionStart context | The projection file is injected into a live terminal session's context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | spaced path breaks the Stop hook command (exit 78 class / silent no-op) | mitigate | Use the `~/.claudeclaw-app` space-free symlink and quote the command; verify resolution before the live test (Task 1) |
| T-05-08 | DoS | Stop fires every turn, capture re-runs | mitigate | Idempotency from Plan 01 (session_id + content dedup); Task 2 confirms no duplicate growth |
| T-05-09 | Information Disclosure | projection injects a secret-bearing memory into a workspace file | mitigate | Memory rows are the bot's already-scrubbed responses; projection writes only `summary`; encrypted messaging tables never read (enforced in Plan 01 source-guard) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Task 1 automated check prints `WIRED`.
- Live MEM-03: terminal SessionStart shows the ClaudeClaw projection; `{date}.claudeclaw.md` present; `{date}.md` not clobbered.
- Live MEM-04: terminal work appears in `(ws:aos, aos)` and is recalled by a later bot `@aos:` turn.
- COMPAT-02: default fleet + non-@aos bot path unchanged.
</verification>

<success_criteria>
- The Stop hook fires the capture CLI (success criterion 4 satisfied without wiring `src/hooks.ts`).
- A terminal session reads a projection of recent ClaudeClaw memories (MEM-03).
- Terminal work is captured and later recalled by the bot (MEM-04).
- Cross-repo agentic-os edits captured verbatim in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/05-memory-projection-capture/05-02-SUMMARY.md` when done. Record: the symlink path, the final Stop array JSON and the load-memory-snapshot.js target edit (verbatim, since they live outside this repo), the two live transcripts, and the criterion-4 reinterpretation note (Stop hook satisfies it; hooks.ts left unwired for a later cleanup phase).
</output>
