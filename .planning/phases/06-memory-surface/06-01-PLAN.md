---
phase: 06-memory-surface
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/memory-provenance.test.ts
  - src/memory.test.ts
  - src/memory-projection.test.ts
  - src/memory-ingest.test.ts
  - src/memory-consolidate.test.ts
  - src/migrations.test.ts
  - src/dashboard.contract.test.ts
  - src/db.ts
  - migrations/v1.2.5/add-memory-surface-columns.ts
  - migrations/version.json
autonomous: true
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - "Running npm run migrate adds category + confirmed columns and a memory_tombstones table to the live store without drift"
    - "Existing memory rows are grandfathered to confirmed=1 so the behavior gate never strips the operator's current memory"
    - "A failing test suite pins every Phase 6 behavior (provenance derivation, confirmed gate on both read paths, tombstone suppression, grouped read, Add) before any feature code exists"
  artifacts:
    - path: "migrations/v1.2.5/add-memory-surface-columns.ts"
      provides: "Versioned migration adding category, confirmed, memory_tombstones (byte-identical to db.ts createSchema)"
      exports: ["description", "run"]
    - path: "src/memory-provenance.test.ts"
      provides: "Failing tests for deriveProvenance mapping + D-05 honest email coverage"
    - path: "migrations/version.json"
      provides: "Registers v1.2.5 in the migration ledger"
      contains: "v1.2.5"
  key_links:
    - from: "migrations/v1.2.5/add-memory-surface-columns.ts"
      to: "src/db.ts createSchema"
      via: "byte-identical ADD COLUMN + CREATE TABLE DDL"
      pattern: "ALTER TABLE memories ADD COLUMN (category|confirmed)"
    - from: "migrations/version.json"
      to: "migrations/v1.2.5/add-memory-surface-columns.ts"
      via: "registered filename"
      pattern: "v1\\.2\\.5"
---

<objective>
Lay the schema spine and the failing-test net for Phase 6. This plan adds the three persistent
state changes the entire phase depends on (a nullable `category` column, a `confirmed` flag, and a
`memory_tombstones` table) through the project's strict dual-write migration convention, and writes
the Wave 0 failing tests that pin every behavior the later slices must satisfy.

Purpose: The migration is cross-cutting and drift-sensitive (a mismatch between `db.ts createSchema`
and the versioned migration crash-loops the live service per Pitfall 1), so it is foundation work
that cannot be vertically sliced per feature. The Wave 0 tests satisfy Nyquist Dimension 8 — every
later task verifies against a test that already exists and fails.

Output: `migrations/v1.2.5/add-memory-surface-columns.ts`, the matching dual-write DDL in `src/db.ts`,
a `version.json` bump, and failing test cases across the seven test files in the Test Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-CONTEXT.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-write migration — category, confirmed, memory_tombstones</name>
  <files>migrations/v1.2.5/add-memory-surface-columns.ts, migrations/version.json, src/db.ts</files>
  <read_first>
    - migrations/v1.2.4/enrich-audit-log.ts (read in full — copy structure + the dual-write/Pitfall-1 header comment byte-for-byte)
    - migrations/version.json (current ledger ends at v1.2.4)
    - src/db.ts:728-794 (the PRAGMA-guarded idempotent ADD COLUMN block in createSchema; :791 `pinned` guard is the exact shape to copy)
    - src/db.ts:132 (memories table definition) and the memories_fts triggers region (db.ts:712, :772) so the tombstone table sits cleanly beside it
  </read_first>
  <action>
    Create migrations/v1.2.5/add-memory-surface-columns.ts exporting `description` and `async run()`,
    opening its own better-sqlite3 handle via path.join(process.cwd(),'store','claudeclaw.db') (never a
    hardcoded absolute path), mirroring migrations/v1.2.4/enrich-audit-log.ts exactly. PRAGMA-guard each
    ADD: `category TEXT` (nullable), `confirmed INTEGER NOT NULL DEFAULT 0`. Then UPDATE existing rows to
    `confirmed=1` (grandfather per Open Q1 D-04 — the migration defaults new inserts to 0 but existing rows
    to 1 so the behavior gate does not strip current memory). Create `memory_tombstones` (id INTEGER PK
    AUTOINCREMENT, chat_id TEXT NOT NULL, text_hash TEXT NOT NULL, embedding TEXT, summary TEXT, created_at
    INTEGER NOT NULL) with an index on (chat_id, text_hash). Add the SAME DDL, byte-identical column
    names/types, into src/db.ts createSchema as PRAGMA-guarded idempotent ADD COLUMNs plus a CREATE TABLE IF
    NOT EXISTS memory_tombstones (Pitfall 1 — drift crash-loops the live service). Register
    "v1.2.5": ["add-memory-surface-columns"] in migrations/version.json. Keep the v1.2.4 header comment
    equivalent documenting the dual-write discipline.
  </action>
  <verify>
    <automated>npx vitest run src/migrations.test.ts src/migrate-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ALTER TABLE memories ADD COLUMN category" migrations/v1.2.5/add-memory-surface-columns.ts` and the same string in src/db.ts both match (byte-identical DDL)
    - `grep -c "v1.2.5" migrations/version.json` returns >= 1
    - migrations.test.ts asserts category + confirmed columns and memory_tombstones table exist after run() and that re-running is idempotent (no error on second run)
    - migrations.test.ts asserts a pre-existing row has confirmed=1 after migration (grandfathered)
  </acceptance_criteria>
  <done>Migration file + dual-write db.ts DDL + version.json bump all present and byte-identical; migration tests green; existing rows grandfathered confirmed=1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 failing tests pinning every Phase 6 behavior</name>
  <files>src/memory-provenance.test.ts, src/memory.test.ts, src/memory-projection.test.ts, src/memory-ingest.test.ts, src/memory-consolidate.test.ts, src/dashboard.contract.test.ts</files>
  <read_first>
    - src/memory.test.ts, src/memory-projection.test.ts, src/memory-ingest.test.ts, src/memory-consolidate.test.ts, src/dashboard.contract.test.ts (existing structure + fixtures to mirror)
    - .planning/phases/06-memory-surface/06-RESEARCH.md "Validation Architecture" Test Map (the authoritative requirement-to-test mapping)
    - src/db.ts:1020 searchMemories, :1131 getMemoriesWithEmbeddings, :1150 getRecentHighImportanceMemories (the readers the gate tests target)
  </read_first>
  <behavior>
    - deriveProvenance: source 'you-told-me' or 'checkpoint' -> 'told'; source 'conversation' -> 'work'; source 'email' -> 'email' ONLY if an email row exists, else the email tag is omitted (D-05)
    - confirmed gate: an unconfirmed (confirmed=0) memory does NOT appear in buildMemoryContext output and does NOT appear in renderMemoryProjection output; after confirm it appears in both
    - tombstone: deleting a fact then re-feeding the same/near-paraphrase turn through ingestConversationTurn creates no new memory row; consolidation does not re-save a tombstoned synthesized summary
    - grouped read: the operator-surface reader returns rows grouped/selectable by category, includes BOTH confirmed and unconfirmed, and omits empty categories (D-07)
    - Add: inserts a confirmed=1, source='you-told-me', high-salience row with an operator-chosen category
    - mutation contracts: PATCH updates summary/category; DELETE writes a tombstone then removes the row; POST /confirm flips confirmed=1 and is a no-op on a second call
  </behavior>
  <action>
    Author failing tests for every row in the RESEARCH Test Map. Create src/memory-provenance.test.ts new
    (deriveProvenance mapping + D-05 honest email coverage). Add confirmed-gate cases to src/memory.test.ts
    (buildMemoryContext) and src/memory-projection.test.ts (renderMemoryProjection). Add tombstone
    suppression cases to src/memory-ingest.test.ts and src/memory-consolidate.test.ts. Add Add/Edit/Delete/
    Confirm route-contract cases to src/dashboard.contract.test.ts. Tests reference the not-yet-existing
    helpers (deriveProvenance, getMemoriesForOperatorSurface, isTombstoned, writeTombstoneForMemory,
    deleteMemory, the Add mutator, the four /api/memory routes) so they RED until later plans implement
    them. Do NOT write feature code in this plan — only the failing tests.
  </action>
  <verify>
    <automated>npx vitest run src/memory-provenance.test.ts 2>&1 | grep -qE "(fail|FAIL|No test files|Cannot find|is not (a function|defined))" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/memory-provenance.test.ts` exists
    - Running `npx vitest run src/memory-provenance.test.ts src/dashboard.contract.test.ts` fails (RED) because the referenced helpers/routes do not yet exist — this is the intended Wave 0 state
    - Each RESEARCH Test Map row has at least one corresponding test case (grep the test files for: deriveProvenance, getMemoriesForOperatorSurface, isTombstoned, /api/memory)
  </acceptance_criteria>
  <done>All Wave 0 test cases authored and failing for the right reason (missing implementation, not syntax errors); no feature code added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner -> live SQLite store | DDL applied to the operator's real memory data; drift or a bad backfill is destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | migration dual-write drift between db.ts createSchema and migrations/v1.2.5 | mitigate | Byte-identical column DDL in both places (acceptance grep), PRAGMA-guarded idempotent ADDs, registered in version.json; migrations.test.ts asserts idempotency |
| T-06-02 | Denial of Service | confirmed backfill on existing rows | mitigate | Grandfather existing rows to confirmed=1 (Open Q1) so the gate does not silently strip the operator's whole memory the moment the migration runs; tested |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero external packages); no slopcheck needed |
</threat_model>

<verification>
- `npx vitest run src/migrations.test.ts src/migrate-runner.test.ts` green (migration applies idempotently, no drift)
- `npx vitest run src/memory-provenance.test.ts src/dashboard.contract.test.ts` RED for missing-implementation reasons (Wave 0 net is set)
- `grep -n "ALTER TABLE memories ADD COLUMN confirmed" src/db.ts migrations/v1.2.5/add-memory-surface-columns.ts` shows the byte-identical DDL in both files
</verification>

<success_criteria>
- Schema spine present and dual-written without drift; existing rows grandfathered confirmed=1
- v1.2.5 registered in version.json
- Failing Wave 0 tests pin all phase behaviors, satisfying Nyquist Dimension 8
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-01-SUMMARY.md` when done
</output>
