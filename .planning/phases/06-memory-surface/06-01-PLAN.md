---
phase: 06-memory-surface
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/migrations.test.ts
  - src/db.test.ts
  - src/memory-provenance.test.ts
  - src/memory.test.ts
  - src/memory-projection.test.ts
  - src/memory-ingest.test.ts
  - src/memory-consolidate.test.ts
  - src/dashboard.contract.test.ts
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A red test pins category-grouped operator read (MEM-01) before any reader exists"
    - "A red test pins deriveProvenance source+agent_id -> 3 tags incl D-05 honest email coverage (MEM-02/D-03)"
    - "A red test pins the confirmed=1 behavior gate on BOTH buildMemoryContext and renderMemoryProjection (D-04)"
    - "A red test pins delete-writes-tombstone-then-skips-re-derivation on ingest and consolidation (D-08)"
    - "A red test pins the v1.2.5 dual-write migration columns + tombstone table apply idempotently (no drift)"
  artifacts:
    - path: "src/memory-provenance.test.ts"
      provides: "Failing provenance derivation + honest-email-coverage spec"
      min_lines: 25
    - path: "src/migrations.test.ts"
      provides: "Failing v1.2.5 column/table presence assertions"
  key_links:
    - from: "src/memory.test.ts"
      to: "buildMemoryContext"
      via: "asserts unconfirmed fact absent until confirmed"
      pattern: "confirmed"
    - from: "src/memory-ingest.test.ts"
      to: "isTombstoned"
      via: "asserts re-fed deleted fact creates no new row"
      pattern: "tombstone"
---

<objective>
Wave 0 test scaffolding. Author the failing tests that pin every behavior this phase must deliver BEFORE any implementation exists, so plans 02-04 execute against a RED bar that turns GREEN.

This is the MVP-mode first deliverable: the end-to-end behavioral contract expressed as failing tests. Covers MEM-01 (category-grouped operator read), MEM-02 (provenance derivation + edit/delete), D-04 (confirmed gate on both behavior read paths), D-08 (tombstone no-re-derivation), D-06 (category column), and the v1.2.5 migration dual-write.

Purpose: Lock the contract. Every later task's `<verify>` runs one of these files and must move it RED -> GREEN.
Output: New/extended test cases that FAIL (modules and columns they reference do not exist yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing migration + schema tests (D-06 columns, D-08 table)</name>
  <files>src/migrations.test.ts, src/db.test.ts</files>
  <read_first>
    - src/migrations.test.ts (existing assertions on prior versions are the shape to copy)
    - src/migrate-runner.test.ts (how runMigrations is exercised in-test)
    - migrations/v1.2.4/enrich-audit-log.ts (the dual-write migration analog)
    - migrations/version.json (current top version is v1.2.4; next is v1.2.5)
    - src/db.ts lines 132 and 728-794 (memories schema + PRAGMA-guarded addColumn pattern)
  </read_first>
  <behavior>
    - Test: after createSchema runs, `PRAGMA table_info(memories)` includes a `category` column (type TEXT, nullable) and a `confirmed` column (type INTEGER, NOT NULL, default 0).
    - Test: after createSchema runs, a `memory_tombstones` table exists with columns id, chat_id, text_hash (TEXT NOT NULL), embedding (TEXT), summary (TEXT), created_at, plus an index on (chat_id, text_hash).
    - Test: applying the v1.2.5 migration twice is idempotent (PRAGMA-guarded ADDs do not throw on second run).
    - Test (drift guard): the column names/types added by createSchema match those the migration would add (same names: `category`, `confirmed`; same tombstone table shape).
  </behavior>
  <action>
    Add test cases to src/migrations.test.ts asserting the v1.2.5 migration registers in migrations/version.json and that running it adds `category`, `confirmed`, and `memory_tombstones` to a temp DB idempotently. Add test cases to src/db.test.ts asserting createSchema on an in-memory better-sqlite3 DB produces the same three artifacts (category TEXT nullable, confirmed INTEGER NOT NULL DEFAULT 0, memory_tombstones table + index). These tests reference columns/tables that do NOT exist yet (per D-06, D-08) and MUST fail now. Bind all SQL with `?` placeholders. Do not implement the migration here — only the assertions.
  </action>
  <verify>
    <automated>npx vitest run src/migrations.test.ts src/db.test.ts 2>&1 | grep -Eiq 'fail|category|confirmed|tombstone'</automated>
  </verify>
  <done>New cases in both files reference category/confirmed/memory_tombstones and FAIL because the schema does not yet define them.</done>
  <acceptance_criteria>
    - `npx vitest run src/migrations.test.ts src/db.test.ts` reports failing assertions naming `category`, `confirmed`, and `memory_tombstones`.
    - Tests use parameterized SQL only (no string interpolation of values).
    - No production schema change is made in this task (db.ts unchanged).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing provenance + behavior-gate tests (D-03, D-04, D-05)</name>
  <files>src/memory-provenance.test.ts, src/memory.test.ts, src/memory-projection.test.ts</files>
  <read_first>
    - src/memory.test.ts (existing buildMemoryContext test setup + fixtures)
    - src/memory-projection.test.ts (existing renderMemoryProjection test setup)
    - src/memory.ts lines 63+ (buildMemoryContext read path)
    - src/memory-projection.ts (renderMemoryProjection read path)
    - 06-RESEARCH.md "Provenance Derivation" table and "Confirmed / Unconfirmed Gate" table
  </read_first>
  <behavior>
    - deriveProvenance: source 'you-told-me' -> 'told'; source 'checkpoint' -> 'told'; source 'conversation' -> 'work'; source 'email' -> 'email' (per D-03).
    - Honest email coverage (D-05): provenance label set returned to the surface includes 'email' ONLY when at least one row with source='email' exists; otherwise the 'email' tag is omitted.
    - D-04 gate (buildMemoryContext): an unconfirmed (confirmed=0) memory is ABSENT from buildMemoryContext output; after confirmed=1 it is PRESENT.
    - D-04 gate (renderMemoryProjection): an unconfirmed memory is ABSENT from renderMemoryProjection output (second behavior read path).
  </behavior>
  <action>
    Create src/memory-provenance.test.ts importing the not-yet-existing `deriveProvenance` from `./memory-provenance.js`, asserting the source->tag mapping above and the D-05 honest-coverage rule (email tag only when an email-sourced row exists). Extend src/memory.test.ts with a case that seeds an unconfirmed memory and asserts buildMemoryContext omits it, then flips confirmed=1 and asserts inclusion. Extend src/memory-projection.test.ts with the same omit-when-unconfirmed assertion for renderMemoryProjection. These reference `deriveProvenance` and a `confirmed` column that do not exist yet and MUST fail.
  </action>
  <verify>
    <automated>npx vitest run src/memory-provenance.test.ts src/memory.test.ts src/memory-projection.test.ts 2>&1 | grep -Eiq 'fail|deriveProvenance|confirmed'</automated>
  </verify>
  <done>memory-provenance.test.ts exists and fails on missing import; memory.test.ts and memory-projection.test.ts fail on the confirmed-gate assertions.</done>
  <acceptance_criteria>
    - `npx vitest run src/memory-provenance.test.ts` fails to resolve `./memory-provenance.js` OR fails its mapping assertions.
    - Both behavior-read tests (memory + projection) assert absence-then-presence keyed on `confirmed`.
    - Email tag assertion proves omission when no email row exists (D-05).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Failing tombstone + mutation contract tests (D-08, MEM-02)</name>
  <files>src/memory-ingest.test.ts, src/memory-consolidate.test.ts, src/dashboard.contract.test.ts</files>
  <read_first>
    - src/memory-ingest.test.ts (existing ingestConversationTurn dedupe test setup)
    - src/memory-consolidate.test.ts (existing runConsolidation test setup)
    - src/dashboard.contract.test.ts (existing route contract assertion style)
    - src/memory-ingest.ts lines 205-244 (the dedupe loop the tombstone check sits beside)
    - 06-RESEARCH.md "Tombstone / Suppression" + "Net-New Mutation API Routes"
  </read_first>
  <behavior>
    - Ingest suppression (crit 3): delete a fact (write a tombstone for its normalized-summary sha256), re-feed the SAME turn through ingestConversationTurn -> assert NO new memory row is created.
    - Consolidation suppression: a synthesized summary matching a tombstone is NOT saved by runConsolidation.
    - Mutation contract: POST /api/memory inserts a confirmed, source='you-told-me', operator-authored fact with a category (D-09). PATCH /api/memory/:id updates summary/category. DELETE /api/memory/:id writes a tombstone row THEN deletes the memory (tombstone-first ordering, Pitfall 6). POST /api/memory/:id/confirm sets confirmed=1 and is a no-op on double-call.
  </behavior>
  <action>
    Extend src/memory-ingest.test.ts with a tombstone suppression case calling the not-yet-existing `isTombstoned`/`writeTombstone` helpers and asserting re-fed deleted content yields no new row. Extend src/memory-consolidate.test.ts with the synthesized-fact suppression case. Extend src/dashboard.contract.test.ts asserting the four net-new routes (`POST /api/memory`, `PATCH /api/memory/:id`, `DELETE /api/memory/:id`, `POST /api/memory/:id/confirm`) exist, validate id via Number.isInteger, validate category against the enum {your-business, your-clients, how-you-work}, and that DELETE writes a tombstone before removing the row. These reference routes/helpers that do not exist yet and MUST fail.
  </action>
  <verify>
    <automated>npx vitest run src/memory-ingest.test.ts src/memory-consolidate.test.ts src/dashboard.contract.test.ts 2>&1 | grep -Eiq 'fail|tombstone|/api/memory'</automated>
  </verify>
  <done>All three files contain new failing cases referencing tombstone helpers and the four /api/memory* mutation routes.</done>
  <acceptance_criteria>
    - Ingest + consolidate tests assert zero new rows for tombstoned content.
    - dashboard.contract test asserts all four routes and the tombstone-first delete ordering.
    - Category-enum validation assertion present in the contract test.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> dashboard API | Untrusted form input (fact summary, category) crosses here on later plans; tests pin the validation contract |
| ingest/consolidate engine -> memories table | Machine-derived facts written here; tests pin tombstone suppression at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Test fixtures vs prod schema drift | mitigate | Migration test asserts createSchema and migration add byte-identical columns (Pitfall 1) |
| T-06-02 | Information Disclosure | Unconfirmed fact leaking into behavior reads | mitigate | D-04 gate tests pin absence in BOTH buildMemoryContext and renderMemoryProjection before impl exists |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable); no install task, no checkpoint needed |
</threat_model>

<verification>
- `npx vitest run src/migrations.test.ts src/db.test.ts src/memory-provenance.test.ts src/memory.test.ts src/memory-projection.test.ts src/memory-ingest.test.ts src/memory-consolidate.test.ts src/dashboard.contract.test.ts` runs and reports RED (the new cases fail because implementations are absent).
- No production source files (db.ts, dashboard.ts, memory*.ts) are modified in this plan.
</verification>

<success_criteria>
- Every MEM-01/MEM-02 behavior + D-03/D-04/D-05/D-06/D-08 enforcement has a failing test that later plans turn green.
- No production code changed; only test files.
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-01-SUMMARY.md` when done.
</output>
