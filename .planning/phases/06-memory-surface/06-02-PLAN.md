---
phase: 06-memory-surface
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - migrations/v1.2.5/add-memory-surface-columns.ts
  - migrations/version.json
  - src/db.ts
  - src/memory-provenance.ts
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "The memories table has a nullable category column and a confirmed column (NOT NULL DEFAULT 0) (D-06, D-04)"
    - "A memory_tombstones table exists with a (chat_id, text_hash) index (D-08)"
    - "Behavior-feeding readers exclude unconfirmed facts; the operator-surface reader includes them (D-04)"
    - "deriveProvenance maps source+agent_id to You told me / Learned from your work / Learned from email (D-03)"
    - "Existing rows are grandfathered confirmed=1; new inferred facts default confirmed=0 (RESEARCH Open Q1)"
  artifacts:
    - path: "migrations/v1.2.5/add-memory-surface-columns.ts"
      provides: "Dual-write migration for category + confirmed + memory_tombstones"
      exports: ["description", "run"]
    - path: "src/memory-provenance.ts"
      provides: "deriveProvenance pure function + honest email coverage"
      exports: ["deriveProvenance"]
    - path: "src/db.ts"
      provides: "createSchema dual-write, confirmed gate, operator-surface reader, tombstone helpers"
      contains: "memory_tombstones"
  key_links:
    - from: "src/db.ts getMemoriesWithEmbeddings/searchMemories/getRecentHighImportanceMemories"
      to: "confirmed column"
      via: "AND confirmed = 1 appended beside superseded_by IS NULL"
      pattern: "confirmed = 1"
    - from: "src/db.ts getMemoriesForOperatorSurface"
      to: "category column"
      via: "groups confirmed + unconfirmed rows by category"
      pattern: "category"
---

<objective>
The backend data spine for the Memory surface. One dual-write migration adds the `category` (D-06) and `confirmed` (D-04) columns and the `memory_tombstones` table (D-08). Add the read-side machinery the operator UI (plan 03) consumes: a `deriveProvenance` helper (D-03/D-05), an operator-surface reader that returns rows groupable by category (MEM-01), and the confirmed=1 behavior gate on the three readers that feed assistant behavior (D-04). Add tombstone read/write helpers (`isTombstoned`, `writeTombstoneForMemory`) used by plans 03 (delete) and 04 (ingest/consolidate hooks).

This is the data half of the first real slice: it turns the plan-01 schema/provenance/gate tests GREEN and exposes the contracts plan 03 wires to the UI.

Purpose: All later tasks read these columns, this reader, and these helpers. Defining the contracts here prevents the executor scavenger hunt.
Output: v1.2.5 migration (dual-written into createSchema), src/memory-provenance.ts, new + modified db.ts functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v1.2.5 dual-write migration (category, confirmed, memory_tombstones)</name>
  <files>migrations/v1.2.5/add-memory-surface-columns.ts, migrations/version.json, src/db.ts</files>
  <read_first>
    - migrations/v1.2.4/enrich-audit-log.ts (the exact dual-write migration shape: own better-sqlite3 handle, PRAGMA-guarded add, db.close in finally)
    - migrations/version.json (add "v1.2.5": ["add-memory-surface-columns"])
    - src/db.ts lines 728-794 (createSchema PRAGMA-guarded addColumn pattern) and line 132 (memories schema)
    - 06-RESEARCH.md "Category Column + LLM Backfill" migration convention + Open Q1 (grandfather existing rows)
  </read_first>
  <behavior>
    - createSchema adds `category TEXT` (nullable) and `confirmed INTEGER NOT NULL DEFAULT 0` to memories, PRAGMA-guarded and idempotent.
    - createSchema creates `memory_tombstones` (id INTEGER PK, chat_id TEXT, text_hash TEXT NOT NULL, embedding TEXT, summary TEXT, created_at INTEGER) plus index on (chat_id, text_hash), IF NOT EXISTS.
    - Existing-row grandfather: the migration sets `confirmed=1` for all rows present at migration time (UPDATE memories SET confirmed=1), so the gate does not strip the operator's whole memory on first run. New inserts still default to 0.
    - Migration column names/types are byte-identical to createSchema (drift guard, Pitfall 1).
  </behavior>
  <action>
    Create migrations/v1.2.5/add-memory-surface-columns.ts exporting `description` and an async `run()` modeled byte-for-byte on enrich-audit-log.ts: open better-sqlite3 at path.join(process.cwd(),'store','claudeclaw.db'), build a `have` Set from PRAGMA table_info(memories), ADD COLUMN `category TEXT` and `confirmed INTEGER NOT NULL DEFAULT 0` only if missing, CREATE TABLE IF NOT EXISTS memory_tombstones + CREATE INDEX IF NOT EXISTS on (chat_id, text_hash), then UPDATE memories SET confirmed=1 to grandfather existing rows (RESEARCH Open Q1), db.close() in finally. Register "v1.2.5": ["add-memory-surface-columns"] in migrations/version.json. Mirror the identical ADD COLUMN / CREATE TABLE / CREATE INDEX in src/db.ts createSchema under the PRAGMA-guarded block at ~line 783 (NEW inserts default confirmed=0; the grandfather UPDATE belongs to the migration only, not createSchema). Use parameterized SQL throughout.
  </action>
  <verify>
    <automated>npx vitest run src/migrations.test.ts src/db.test.ts</automated>
  </verify>
  <done>migrations.test.ts and db.test.ts (the category/confirmed/tombstone cases from plan 01) pass; running the migration twice is idempotent; existing rows are confirmed=1.</done>
  <acceptance_criteria>
    - `npx vitest run src/migrations.test.ts src/db.test.ts` is GREEN for the plan-01 schema cases.
    - migrations/version.json contains v1.2.5 with add-memory-surface-columns.
    - createSchema and the migration add identical column names/types (grep both for `confirmed INTEGER NOT NULL DEFAULT 0` and `category TEXT`).
    - memory_tombstones table + (chat_id, text_hash) index present after createSchema.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveProvenance helper + confirmed behavior gate (D-03, D-04, D-05)</name>
  <files>src/memory-provenance.ts, src/db.ts</files>
  <read_first>
    - src/memory-provenance.test.ts (plan-01 spec for the mapping + honest email coverage)
    - src/db.ts lines 1041, 1068, 1093, 1136-1137, 1150-1168 (the three behavior readers carrying superseded_by IS NULL)
    - src/memory.test.ts and src/memory-projection.test.ts (plan-01 confirmed-gate cases)
    - 06-RESEARCH.md "Provenance Derivation" + "Confirmed / Unconfirmed Gate" tables
  </read_first>
  <behavior>
    - deriveProvenance(memory): returns 'told' for source IN ('you-told-me','checkpoint'), 'work' for source='conversation' (default), 'email' for source='email'.
    - Honest email coverage (D-05): the helper exposes a way to omit the 'email' tag unless an email-sourced row exists (e.g. a hasEmailSource(chatId) check the operator reader consults).
    - Behavior gate (D-04): searchMemories, getMemoriesWithEmbeddings, getRecentHighImportanceMemories each append `AND confirmed = 1` everywhere they currently carry `AND superseded_by IS NULL`.
    - These three readers feed buildMemoryContext and renderMemoryProjection; after the gate, an unconfirmed fact is absent from both until confirmed.
  </behavior>
  <action>
    Create src/memory-provenance.ts exporting a pure `deriveProvenance(memory: { source: string }): 'told'|'work'|'email'` implementing the D-03 mapping, plus the D-05 honest-coverage rule (export a helper or accept a hasEmail flag so the surface omits the email tag when no email row exists). Keep it server-side only (no Preact import). In src/db.ts, append `AND confirmed = 1` to getMemoriesWithEmbeddings (1136-1137), searchMemories (1041, 1068, 1093 — every superseded_by clause), and getRecentHighImportanceMemories (1150-1168 both branches), mirroring the existing superseded_by IS NULL pattern. Do NOT touch the operator-surface reader (task 3) which must show unconfirmed rows.
  </action>
  <verify>
    <automated>npx vitest run src/memory-provenance.test.ts src/memory.test.ts src/memory-projection.test.ts</automated>
  </verify>
  <done>Provenance mapping + honest-email tests pass; buildMemoryContext and renderMemoryProjection both exclude unconfirmed facts and include them once confirmed=1.</done>
  <acceptance_criteria>
    - `npx vitest run src/memory-provenance.test.ts src/memory.test.ts src/memory-projection.test.ts` is GREEN.
    - `grep -c 'confirmed = 1' src/db.ts` returns >= 4 (one per superseded_by clause across the three readers).
    - deriveProvenance is exported from src/memory-provenance.ts and imported by db.ts/tests, never by the web/ layer.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Operator-surface reader + tombstone helpers + Add-fact writer (MEM-01, D-08, D-09)</name>
  <files>src/db.ts</files>
  <read_first>
    - src/db.ts lines 951-978 (saveStructuredMemory), 1110-1129 (saveStructuredMemoryAtomic), 2346 (getDashboardMemoriesList row shape)
    - src/db.ts lines 1003, 1054-1059 (extractKeywords normalization + FTS quote-strip to reuse for normalizeSummary)
    - src/embeddings.ts (cosineSimilarity for optional tombstone secondary match)
    - src/dashboard.contract.test.ts (plan-01 tombstone-first delete + Add-fact contract)
    - 06-RESEARCH.md "Tombstone / Suppression" + "Net-New Mutation API Routes" (Add semantics)
  </read_first>
  <behavior>
    - getMemoriesForOperatorSurface(chatId): returns confirmed AND unconfirmed rows scoped to ALLOWED_CHAT_ID, each carrying source, category, confirmed, summary, created_at — groupable by category. Does NOT filter confirmed (distinct from the behavior readers).
    - normalizeSummary(text): lowercase, collapse whitespace, strip punctuation (reuse extractKeywords spirit). sha256 of it is the tombstone primary key.
    - writeTombstoneForMemory(id): reads the memory, writes a memory_tombstones row (text_hash = sha256(normalizeSummary(summary)), embedding if present, summary, chat_id, created_at). Returns false if the memory id does not exist (so DELETE can 404).
    - isTombstoned(chatId, hash, embedding?): true if hash matches a tombstone, OR (when embedding present and GOOGLE_API_KEY available) cosine vs any tombstone embedding > the named TOMBSTONE_COSINE_THRESHOLD (0.88).
    - addOperatorFact(chatId, summary, category): inserts via the saveStructuredMemoryAtomic path with source='you-told-me', confirmed=1, importance ~0.9, category validated against the enum; best-effort embedding (D-09).
    - deleteMemory(id): removes the memory row (called AFTER writeTombstoneForMemory by the route, Pitfall 6).
  </behavior>
  <action>
    In src/db.ts add: getMemoriesForOperatorSurface(chatId) (model row shape on getDashboardMemoriesList:2346 but NO confirmed filter, ordered for category grouping); a normalizeSummary helper + TOMBSTONE_COSINE_THRESHOLD = 0.88 named constant; writeTombstoneForMemory(id) and isTombstoned(chatId, hash, embedding?) using parameterized SQL against memory_tombstones; addOperatorFact(chatId, summary, category) that stamps source='you-told-me', confirmed=1, high importance, validates category against {your-business, your-clients, how-you-work}, and writes best-effort embedding; deleteMemory(id) returning .changes===1. All SQL via `?` placeholders. No raw SQL in dashboard.ts — these functions are the seam plan 03 calls.
  </action>
  <verify>
    <automated>npx vitest run src/memory-ingest.test.ts src/dashboard.contract.test.ts src/db.test.ts</automated>
  </verify>
  <done>Operator-surface reader returns confirmed+unconfirmed grouped-by-category rows; tombstone helpers write+detect (hash floor always, cosine when embeddings present); addOperatorFact stamps you-told-me/confirmed=1; the plan-01 tombstone + Add-fact contract cases pass.</done>
  <acceptance_criteria>
    - `npx vitest run src/memory-ingest.test.ts src/dashboard.contract.test.ts src/db.test.ts` GREEN for tombstone + operator-reader + Add cases.
    - getMemoriesForOperatorSurface returns rows with confirmed=0 present (proves it does NOT filter).
    - TOMBSTONE_COSINE_THRESHOLD is a named constant (grep `0.88` -> the constant, not a literal at the call site).
    - addOperatorFact rejects a category outside the three-value enum.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest/consolidate -> memories | Tombstone helpers gate re-derivation at this write boundary |
| db.ts readers -> assistant behavior | Confirmed gate prevents unconfirmed facts crossing into prompt/projection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Migration vs createSchema drift | mitigate | Task 1 dual-writes byte-identical DDL; migrations.test asserts both (Pitfall 1) |
| T-06-02 | Information Disclosure | Unconfirmed fact influencing behavior | mitigate | Task 2 appends `confirmed = 1` to all three behavior readers (D-04); tests pin both read paths |
| T-06-03 | Tampering | SQL injection via summary/category | mitigate | All new db.ts functions bind via `?` placeholders; category validated against 3-value enum (ASVS V5) |
| T-06-04 | Repudiation | Deleted fact silently re-derived | mitigate | writeTombstoneForMemory + isTombstoned enforce no-re-derivation (D-08); local-only SQLite, no external store |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- `npm test` passes (full suite green) with no migration drift.
- `grep -c 'confirmed = 1' src/db.ts` >= 4.
- memory_tombstones table present; getMemoriesForOperatorSurface returns unconfirmed rows.
- Local-only storage assurance holds: no new external service or secret introduced (RESEARCH Runtime State Inventory).
</verification>

<success_criteria>
- Plan-01 schema, provenance, behavior-gate, tombstone, and Add-fact tests are GREEN.
- The contracts plan 03 needs (getMemoriesForOperatorSurface, deriveProvenance, writeTombstoneForMemory, deleteMemory, addOperatorFact) exist and are exported.
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-02-SUMMARY.md` when done.
</output>
