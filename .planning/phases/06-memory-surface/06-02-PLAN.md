---
phase: 06-memory-surface
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/db.ts
  - src/memory-provenance.ts
  - src/dashboard.ts
  - web/src/pages/Memory.tsx
  - web/src/lib/routes.ts
  - web/src/App.tsx
  - web/src/lib/vocabulary.ts
autonomous: true
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - "An operator can open a new /memory page titled 'What I know about you' and see facts grouped by category (your business / your clients / how you like to work)"
    - "Each fact shows a provenance tag derived server-side (You told me / Learned from your work / Learned from email), with the email tag shown only if an email-source row actually exists (D-05)"
    - "Empty categories are not rendered (D-07)"
    - "Unconfirmed facts appear on the operator surface carrying a 'needs review' marker"
    - "The existing developer view (/memories, BrainGraph) still works but is pulled from the visible operator nav into a hidden Labs-style demotion (D-02)"
  artifacts:
    - path: "src/memory-provenance.ts"
      provides: "Pure server-side deriveProvenance(memory) -> 'told'|'work'|'email' (D-03/D-05)"
      exports: ["deriveProvenance"]
    - path: "web/src/pages/Memory.tsx"
      provides: "New operator Memory page (D-01), grouped-by-category, provenance pill as hero"
      min_lines: 80
    - path: "src/dashboard.ts"
      provides: "GET /api/memory returning category-grouped operator rows with derived provenance"
      contains: "/api/memory"
  key_links:
    - from: "web/src/pages/Memory.tsx"
      to: "/api/memory"
      via: "apiGet in load()"
      pattern: "apiGet.*api/memory"
    - from: "src/dashboard.ts GET /api/memory"
      to: "src/db.ts getMemoriesForOperatorSurface"
      via: "reader call (no confirmed filter)"
      pattern: "getMemoriesForOperatorSurface"
    - from: "src/dashboard.ts GET /api/memory"
      to: "src/memory-provenance.ts deriveProvenance"
      via: "DTO derivation"
      pattern: "deriveProvenance"
    - from: "web/src/App.tsx"
      to: "web/src/pages/Memory.tsx"
      via: "<Route path=/memory>"
      pattern: "path=\"/memory\""
---

<objective>
First shippable vertical slice: the operator can SEE what the assistant knows, grouped by category,
with each fact's provenance as the hero of the row (MEM-01 + the read half of MEM-02). End-to-end:
a new operator-surface reader in db.ts -> a server-side provenance helper -> a GET /api/memory route
-> a NEW /memory operator page modeled on Activity.tsx (D-01: a new clean trust surface, NOT a reframe
of the existing Memories.tsx) -> nav registration + the D-02 Labs demotion of the existing developer view.

Purpose: This is the trust surface. Provenance derivation is server-side so the API and tests agree.
After this plan an operator can open /memory and read their grouped, provenance-tagged knowledge base
even before edit/add (Plan 03) or the behavior gate/categorization engine work (Plan 04) lands.

Output: src/memory-provenance.ts, getMemoriesForOperatorSurface in db.ts, GET /api/memory, Memory.tsx,
and the routes/App/vocabulary changes (new /memory + /memories Labs demotion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-CONTEXT.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provenance helper + operator-surface reader + GET /api/memory</name>
  <files>src/memory-provenance.ts, src/db.ts, src/dashboard.ts</files>
  <read_first>
    - src/memory-provenance.test.ts (the Wave 0 contract this implements — make it GREEN)
    - .planning/phases/06-memory-surface/06-RESEARCH.md "Provenance Derivation (D-03)" mapping table and "Confirmed / Unconfirmed Gate" note that the operator reader must NOT filter confirmed
    - src/db.ts:2346 getDashboardMemoriesList (the read shape to model the operator reader on), :132 memories columns; src/config.ts:88 ALLOWED_CHAT_ID scope
    - src/dashboard.ts:2300 read routes (/api/memories), :432 requireToken gate (inherited, do not re-implement)
  </read_first>
  <action>
    Create src/memory-provenance.ts exporting a pure deriveProvenance(memory): 'told'|'work'|'email'
    helper per the D-03 mapping: source IN ('you-told-me','checkpoint') -> 'told'; source='conversation'
    -> 'work'; source='email' -> 'email'. The email tag is honest-coverage gated (D-05): the route/DTO
    only emits 'email' if an email-source row actually exists; expose a hasEmailSource(chatId) helper (a
    SELECT 1 FROM memories WHERE source='email' LIMIT 1 in db.ts) the route consults. Add
    getMemoriesForOperatorSurface(chatId) in src/db.ts modeled on getDashboardMemoriesList but returning
    BOTH confirmed and unconfirmed rows, scoped to chat_id=ALLOWED_CHAT_ID, selecting id, summary, category,
    source, confirmed, created_at — do NOT add the confirmed=1 clause here (this reader is the opposite of
    the behavior gate). Add GET /api/memory in src/dashboard.ts beside the existing read routes (inheriting
    requireToken) that calls getMemoriesForOperatorSurface, maps each row through deriveProvenance into a
    curated DTO { id, summary, category, provenance, confirmed, created_at } (no secrets, derived fields
    only), and returns { rows }. All SQL lives in db.ts via ? placeholders.
  </action>
  <verify>
    <automated>npx vitest run src/memory-provenance.test.ts src/db.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory-provenance.test.ts is GREEN (deriveProvenance mapping + D-05 email-only-if-exists pass)
    - `grep -n "getMemoriesForOperatorSurface" src/db.ts src/dashboard.ts` shows the reader defined in db.ts and called in dashboard.ts
    - The operator reader body does not contain a `confirmed = 1` clause (operator surface shows unconfirmed)
    - GET /api/memory returns a `rows` array where each row has a `provenance` field in {told,work,email}
  </acceptance_criteria>
  <done>Provenance derived server-side, operator reader returns confirmed+unconfirmed rows, GET /api/memory live and token-gated; provenance test green.</done>
</task>

<task type="auto">
  <name>Task 2: Memory.tsx operator page (D-01) + nav registration + /memories Labs demotion (D-02)</name>
  <files>web/src/pages/Memory.tsx, web/src/lib/routes.ts, web/src/App.tsx, web/src/lib/vocabulary.ts</files>
  <read_first>
    - .planning/phases/06-memory-surface/06-UI-SPEC.md (APPROVED UI contract — the binding source for layout, copy, color, typography, spacing; the analogs below show HOW the in-house components are used, the UI-SPEC says WHAT this surface must render)
    - web/src/pages/Activity.tsx (the exact analog: import block :12-37, toneForTag :42-57, useMemo grouping :172-184, section render :261-285, load() :136-167, PageState :251-259, PageHeader+actions :188-201; ActivityRowCard is the card-anatomy + 12.5px body precedent the UI-SPEC pins to)
    - web/src/components/PageHeader.tsx (PageHeader + Tab — reuse for title bar, action slot, category tabs), web/src/components/PageState.tsx (loading/error/empty — never hand-roll), web/src/components/Pill.tsx (+ StatusDot — provenance + needs-review marker), web/src/components/Modal.tsx + ConfirmModal.tsx (reused in Plan 03)
    - web/src/lib/routes.ts (the intelligence section block :34-37 and the Audit D-13 demotion comment :38-42 — copy that demotion precedent for /memories)
    - web/src/App.tsx (router; :57 /memories route to KEEP, :69 the /memory->/memories redirect to REMOVE/REPOINT — Open Q2)
    - web/src/lib/vocabulary.ts (existing nav.* / page.* TermKey entries to mirror)
  </read_first>
  <action>
    Create web/src/pages/Memory.tsx as a NEW operator page (D-01: clean trust surface, distinct from the
    developer Memories.tsx, mirroring the Activity-vs-Audit precedent) by adapting Activity.tsx and rendering
    STRICTLY to the approved UI-SPEC. Copy the import block and the cancel-token load() hitting apiGet of
    { rows: MemoryRow[] } at '/api/memory' with MemoryRow { id, summary, category,
    provenance:'told'|'work'|'email', confirmed:0|1, created_at }. Group with useMemo by row.category (not
    day); only push a group with >=1 row (D-07 empty categories hidden); default-hide null-category rows.
    REUSE the in-house components — do NOT rebuild any of them: PageHeader (+Tab) for the title bar, action
    slot and the category tabs; PageState for loading/error/empty (never hand-roll); Pill (+StatusDot) for
    provenance and the needs-review marker. Use term / apiGet from the existing libs.
    Provenance is the hero (UI-SPEC Visual Hierarchy): render the provenance as a Pill, right-aligned on the
    fact's top row, via a toneForTag-style map — 'told' ("You told me") -> tone `accent` (accent-soft tint);
    'work' ("Learned from your work") and 'email' ("Learned from email") -> tone `neutral`; the "Needs review"
    unconfirmed marker (confirmed===0) -> tone `medium` (amber). The "Learned from email" pill renders ONLY
    when an email-sourced fact exists in the data (D-05).
    Exact copy (UI-SPEC Copywriting Contract — use literal strings): page title `What I know about you`;
    header assurance subtitle `Stored on this machine. Edit or delete anything.`; category tab labels
    `Your business` / `Your clients` / `How you like to work`; provenance labels `You told me` /
    `Learned from your work` / `Learned from email`; unconfirmed marker `Needs review`; empty-state heading
    `Nothing here yet` with body `As you work together, what the assistant learns about you will show up
    here, grouped by topic. You can also add a fact yourself. Everything stays on this machine.`. Wire copy
    via term() where a TermKey is the project convention, otherwise the literal string.
    Accent discipline (UI-SPEC Color): `--color-accent` solid ONLY for the active category tab (and, in
    Plan 03, the Add CTA / Confirm primary / modal-save). Provenance pills, the needs-review marker, borders
    and hover MUST NOT use solid accent. Use `var(--color-*)` tokens only — never hard-coded hex.
    Card anatomy / spacing (UI-SPEC, mirrors ActivityRowCard): fact row =
    `bg-[var(--color-elevated)] border border-[var(--color-border)] rounded-md p-3`, rows stacked `gap-4`,
    category groups separated `mb-8` with a `mb-2` 10px-uppercase section label. The only permitted sub-4
    spacing is the Pill's `py-0.5`. Typography ladder 14 / 12.5 / 11 / 10: fact body is `text-[12.5px]`
    (matching ActivityRowCard), title 14/600, meta 11/400, section label 10/500 uppercase.
    Affordance rule (UI-SPEC): buttons that cannot act are ABSENT, not disabled — so row actions
    (Add/Edit/Delete/Confirm) are NOT rendered in this plan (wired in Plan 03); render the read-only grouped
    view now with the provenance pill + needs-review marker only.
    In routes.ts add a RouteDef { path:'/memory', label:'Memory', vocabKey:'nav.memory',
    section:'intelligence', icon:<distinct lucide-preact icon> } and REMOVE the /memories RouteDef from the
    visible nav list (D-02 Labs demotion, Audit D-13 precedent). In App.tsx add a Route at path "/memory"
    rendering Memory (import Memory), KEEP the "/memories" Route for deep-link reach, and REMOVE/REPOINT the
    legacy /memory->/memories redirect at :69. Add nav.memory + page.memory terms in vocabulary.ts. CRITICAL:
    edit web/src/App.tsx (tracked), never web/src/app.tsx (untracked stray, Pitfall 2).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without error (Preact/Vite build of the web bundle succeeds)
    - `grep -n "path=\"/memory\"" web/src/App.tsx` matches the new operator Route
    - `grep -n "Redirect to=\"/memories\"" web/src/App.tsx` no longer matches the old /memory redirect (removed/repointed)
    - `grep -n "/memory" web/src/lib/routes.ts` shows the new RouteDef present; the /memories RouteDef is absent from the visible ROUTES array
    - `grep -n "apiGet" web/src/pages/Memory.tsx` shows the fetch to '/api/memory'
    - Exact copy present (UI-SPEC): `grep -F "What I know about you" web/src/pages/Memory.tsx` and `grep -F "Stored on this machine. Edit or delete anything." web/src/pages/Memory.tsx` and `grep -F "Needs review" web/src/pages/Memory.tsx` and `grep -F "Nothing here yet" web/src/pages/Memory.tsx` all match; category labels `Your business`/`Your clients`/`How you like to work` and provenance labels `You told me`/`Learned from your work`/`Learned from email` present
    - Component reuse (no rebuilds): `grep -nE "PageHeader|PageState|Pill" web/src/pages/Memory.tsx` shows the in-house components imported/used; PageState used for loading/error/empty (no hand-rolled spinner/empty markup)
    - Provenance pill is the hero: a Pill is rendered for every row's provenance with tones told->accent, work/email->neutral, needs-review->medium (amber); `grep -nE "accent|neutral|medium" web/src/pages/Memory.tsx` shows the tone mapping
    - Accent discipline: no hard-coded hex in Memory.tsx (`grep -nE "#[0-9a-fA-F]{3,6}" web/src/pages/Memory.tsx` returns nothing); solid `--color-accent` not applied to provenance/needs-review/borders
    - Card anatomy: `grep -F "bg-[var(--color-elevated)]" web/src/pages/Memory.tsx` and `grep -F "text-[12.5px]" web/src/pages/Memory.tsx` match (mirrors ActivityRowCard)
    - Row action buttons (Add/Edit/Delete/Confirm) are ABSENT in this plan (rendered read-only; affordance rule)
    - web/src/app.tsx was NOT modified (`git status --porcelain web/src/app.tsx` empty)
  </acceptance_criteria>
  <done>New /memory operator page (D-01) renders per the approved UI-SPEC: facts grouped by category, provenance Pill as the right-aligned row hero with correct tones, amber "Needs review" marker on unconfirmed rows, exact contract copy, reused PageHeader/PageState/Pill, ActivityRowCard card anatomy + 12.5px body, accent reserved for the active tab only, empty categories hidden; /memories demoted off visible nav but still routable (D-02); build green; App.tsx (tracked) edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> GET /api/memory | operator-authenticated read of stored facts crosses here |
| db reader -> DTO | raw stored columns (source, agent_id) must not leak; only derived fields surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | GET /api/memory DTO | mitigate | Return a curated DTO (id, summary, category, provenance, confirmed, created_at) only; no raw source/agent_id/embedding; provenance derived server-side |
| T-06-04 | Spoofing | GET /api/memory auth | mitigate | Inherit the app-level requireToken query-token gate (dashboard.ts:432); no bespoke auth added |
| T-06-05 | Information Disclosure | "Learned from email" tag | mitigate | D-05 honest coverage: emit email tag only if an email-source row exists (hasEmailSource), never imply uncaptured coverage |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero external packages) |
</threat_model>

<verification>
- `npx vitest run src/memory-provenance.test.ts` green
- `npm run build` green (web bundle compiles with the new page + routes)
- Grep checks in acceptance criteria confirm /memory route added, /memory redirect removed, /memories demoted, App.tsx (not app.tsx) edited
- UI-SPEC copy + component-reuse + accent-discipline grep checks pass on Memory.tsx
</verification>

<success_criteria>
- Operator can view facts grouped by category with provenance as the row hero (MEM-01, success criterion 1 + 2)
- Empty categories hidden (D-07); unconfirmed facts marked "needs review" (D-04 surface side)
- Developer view relocated off visible nav, still routable (D-02)
- Surface matches the approved 06-UI-SPEC.md (copy, provenance pill tones, accent discipline, card anatomy, typography)
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-02-SUMMARY.md` when done
</output>
