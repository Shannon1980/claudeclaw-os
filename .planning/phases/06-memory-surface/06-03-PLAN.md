---
phase: 06-memory-surface
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/db.ts
  - src/dashboard.ts
  - src/memory-ingest.ts
  - src/memory-consolidate.ts
  - web/src/pages/Memory.tsx
autonomous: true
requirements: [MEM-02]
user_setup: []

must_haves:
  truths:
    - "An operator can add a fact from the /memory page; it stores as a confirmed, operator-authored ('You told me') high-salience fact with the chosen category"
    - "The Add affordance carries a prominent local-storage assurance ('Stored on this machine. Edit or delete anything.')"
    - "An operator can edit a fact's summary/category in place and search stays in sync (FTS trigger)"
    - "An operator can delete a fact in place; deletion writes a tombstone FIRST then removes the row"
    - "A deleted fact is NOT silently re-derived: feeding the same/near-paraphrase turn through ingest creates no new row, and consolidation will not re-save a tombstoned synthesized fact (success criterion 3, D-08)"
    - "An operator can confirm an unconfirmed fact in place (Confirm action)"
  artifacts:
    - path: "src/dashboard.ts"
      provides: "POST /api/memory, PATCH /api/memory/:id, DELETE /api/memory/:id, POST /api/memory/:id/confirm"
      contains: "/api/memory/:id"
    - path: "src/db.ts"
      provides: "Add mutator (you-told-me, confirmed=1, high salience), updateMemory, writeTombstoneForMemory, isTombstoned, deleteMemory"
      exports: ["isTombstoned", "writeTombstoneForMemory", "deleteMemory"]
  key_links:
    - from: "src/dashboard.ts DELETE /api/memory/:id"
      to: "src/db.ts writeTombstoneForMemory + deleteMemory"
      via: "tombstone FIRST then delete (fail-safe order, Pitfall 6)"
      pattern: "writeTombstoneForMemory"
    - from: "src/memory-ingest.ts ingestConversationTurn"
      to: "src/db.ts isTombstoned"
      via: "tombstone check at the existing dedupe loop before save"
      pattern: "isTombstoned"
    - from: "src/memory-consolidate.ts runConsolidation"
      to: "src/db.ts isTombstoned"
      via: "tombstone check before saveConsolidationAtomic"
      pattern: "isTombstoned"
    - from: "web/src/pages/Memory.tsx"
      to: "/api/memory/:id"
      via: "apiPost / apiPatch / apiDelete row actions"
      pattern: "api(Post|Patch|Delete)"
---

<objective>
Second vertical slice: the operator can CONTROL their knowledge base — add, edit, delete, confirm —
and a deleted fact provably never comes back (MEM-02 + D-08 + D-09 + the Confirm action of D-04). This
is the one genuinely new mechanism the phase adds: the tombstone, enforced at the exact ingest dedupe
point and again before consolidation save.

Purpose: This delivers the operator's editorial control over memory and the "deleted is not silently
re-derived" guarantee (success criterion 3) with an automated test proving suppression. After this plan
the operator can fully manage facts; the cross-cutting behavior gate + categorization engine work
follows in Plan 04.

Output: four net-new mutation routes in dashboard.ts, the Add/update/tombstone/delete helpers in db.ts,
tombstone hooks in ingest + consolidate, and the row actions (Add form, inline Edit, ConfirmModal delete,
Confirm button) wired into Memory.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-CONTEXT.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tombstone helpers + Add/update/delete mutators + ingest/consolidate hooks</name>
  <files>src/db.ts, src/memory-ingest.ts, src/memory-consolidate.ts</files>
  <read_first>
    - src/memory-ingest.test.ts and src/memory-consolidate.test.ts (the Wave 0 suppression contracts to make GREEN)
    - src/memory-ingest.ts:111 EXTRACTION_PROMPT, :39 extractViaClaude, :214 embedding, :219-232 the existing 0.85 cosine dedupe loop, :234 saveStructuredMemoryAtomic (the exact tombstone hook point)
    - src/memory-consolidate.ts:59 runConsolidation, :142 saveConsolidationAtomic (the second tombstone consult point)
    - src/db.ts:1118 saveStructuredMemoryAtomic (variant for Add), :1003 extractKeywords normalization (reuse spirit for hash), :2346 reader shapes; src/embeddings.ts embedText + cosineSimilarity
  </read_first>
  <action>
    In src/db.ts add: writeTombstoneForMemory(id) — load the memory, compute text_hash =
    sha256(normalizeSummary(summary)) (normalize = lowercase + collapse whitespace + strip punctuation,
    reusing the extractKeywords normalization spirit), store hash + optional embedding (if present) +
    summary + chat_id + created_at into memory_tombstones; isTombstoned(chatId, hash, embedding?) — return
    true if any tombstone row matches text_hash, OR (when embedding present and GOOGLE_API_KEY available)
    cosineSimilarity > TOMBSTONE_COSINE_THRESHOLD (a named const = 0.88, stricter than the 0.85 dedupe line
    per Pitfall 5); deleteMemory(id) — DELETE the row (FTS AFTER DELETE trigger keeps search in sync);
    updateMemory(id, { summary?, category? }) — UPDATE bound by ? (FTS AFTER UPDATE trigger syncs); and an
    Add mutator variant of saveStructuredMemoryAtomic stamping source='you-told-me', confirmed=1, high
    salience/importance (importance~0.9), operator agent_id, operator category, best-effort embedding.
    All SQL in db.ts, ? placeholders. In src/memory-ingest.ts insert the tombstone check at the dedupe loop
    (after embedding :214, before :234): compute the hash of result.summary and `if (isTombstoned(...)) {
    log + return false }` mirroring the existing dedupe return. In src/memory-consolidate.ts add the same
    isTombstoned check on the SYNTHESIZED summary/insight before saveConsolidationAtomic (:142) so a deleted
    fact cannot re-enter as a consolidation.
  </action>
  <verify>
    <automated>npx vitest run src/memory-ingest.test.ts src/memory-consolidate.test.ts src/db.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory-ingest.test.ts and src/memory-consolidate.test.ts GREEN (delete -> tombstone -> re-feed same/near-paraphrase turn creates no new row; consolidation does not re-save tombstoned text)
    - `grep -n "isTombstoned" src/memory-ingest.ts src/memory-consolidate.ts` shows the check in BOTH paths
    - `grep -n "TOMBSTONE_COSINE_THRESHOLD\|0.88" src/db.ts` shows the named threshold constant
    - The DELETE helper deletes the row only after a tombstone exists (writeTombstoneForMemory is a separate callable invoked first by the route in Task 2)
  </acceptance_criteria>
  <done>Tombstone hash floor + optional embedding match implemented; suppression enforced at both ingest and consolidation hook points; Add/update/delete mutators present; suppression tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Four mutation routes + Memory.tsx row actions (Add/Edit/Delete/Confirm)</name>
  <files>src/dashboard.ts, web/src/pages/Memory.tsx</files>
  <read_first>
    - .planning/phases/06-memory-surface/06-UI-SPEC.md (APPROVED UI contract — binds the row-action affordances, the Add-fact Modal, the Delete ConfirmModal, exact button copy, toast copy, and accent discipline; the analogs below show HOW the components are wired, the UI-SPEC says WHAT to render)
    - src/dashboard.contract.test.ts (the Wave 0 route contracts to make GREEN)
    - src/dashboard.ts:3587 approve (id-validate + status-guard + .changes pattern), :1603 task DELETE, :1612 task PATCH (body-parse + per-field validation), :432 requireToken, :449 DASHBOARD_MUTATIONS_ENABLED kill switch, :495 CSRF Origin allowlist (all inherited middleware — do NOT re-implement)
    - web/src/pages/Activity.tsx:330-361 (busy/failure row action state machine), :433-443 (ConfirmModal destructive flow + pushToast)
    - web/src/components/Modal.tsx (Add-fact form modal — reuse, do not rebuild), web/src/components/ConfirmModal.tsx (destructive Delete confirmation — reuse), web/src/components/Pill.tsx (row provenance/needs-review already rendered in Plan 02)
    - web/src/lib/api.ts:46 apiPost, :71 apiPatch, :97 apiDelete (all exist — use them); web/src/lib/toasts.ts pushToast; web/src/lib/vocabulary.ts term
  </read_first>
  <action>
    In src/dashboard.ts add four routes beside the existing /api/memories read routes, each calling a db.ts
    function (never inline SQL), inheriting requireToken + kill switch + CSRF automatically:
    POST /api/memory (Add, D-09) — body { summary, category }; validate category against the enum
    {your-business, your-clients, how-you-work}; clamp summary length; call the Add mutator; return
    { ok, id }. PATCH /api/memory/:id (Edit) — validate id Number.isInteger, body { summary?, category? },
    call updateMemory; return { ok }. DELETE /api/memory/:id (Delete + D-08) — validate id, call
    writeTombstoneForMemory FIRST (Pitfall 6 fail-safe order), then deleteMemory; return { ok }.
    POST /api/memory/:id/confirm (D-04) — validate id, flip confirmed=1 status-guarded so a double-click is
    a .changes===0 no-op; return { ok }.
    In web/src/pages/Memory.tsx wire the row actions adapting Activity's busy/failure state machine and
    rendering STRICTLY to the approved UI-SPEC. REUSE the in-house components — do NOT rebuild: Modal for the
    Add-fact form, ConfirmModal for Delete, the existing Pill provenance hero from Plan 02. Use
    apiPost/apiPatch/apiDelete, pushToast, and term.
    Affordance rule (UI-SPEC): confirmed rows carry exactly `Edit` and `Delete`; unconfirmed rows carry
    `Confirm`, `Edit`, `Delete`. The `Confirm` button appears ONLY on confirmed===0 rows — never rendered
    disabled-and-dead on confirmed rows.
    Add a fact (D-09): an accent-filled `Add a fact` CTA in the PageHeader action slot opens a Modal titled
    `Add a fact` with a fact-text field, a category picker labeled `Which area?` (options Your business /
    Your clients / How you like to work), the in-modal assurance line `Stored on this machine. You can edit
    or delete it anytime.`, and a primary save button `Save fact` -> apiPost('/api/memory'). On success the
    inserted fact is a confirmed, operator-authored "You told me" row.
    Edit (in place): open an inline editor or Modal with the fact text + category; `Save fact` ->
    apiPatch(`/api/memory/${id}`); on failure surface the verbatim server reason via a `Could not save` toast.
    Delete (D-08): a destructive ConfirmModal titled `Delete this fact?`, body `The assistant will forget
    this, and it will not be learned again from your work or email.`, confirmLabel `Delete`, cancelLabel
    `Cancel` -> apiDelete(`/api/memory/${id}`); success toast `Deleted. It will not come back.`, failure
    toast `Could not delete` + verbatim server reason.
    Confirm (unconfirmed only): a `Confirm` button -> apiPost(`/api/memory/${id}/confirm`); success toast
    `Confirmed. This fact can now inform what the team does on its own.`. Reload the list on success.
    Accent discipline (UI-SPEC Color): solid `--color-accent` ONLY on the `Add a fact` CTA, the `Confirm`
    primary button, and the modal-save (`Save fact`) primary + focus rings. NOT on Edit/Delete row buttons,
    provenance pills, needs-review marker, borders, or hover. Use `var(--color-*)` tokens only — no hex.
    The destructive ConfirmModal Delete button uses `--color-status-failed`, not accent.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/dashboard.contract.test.ts GREEN for Add/Edit/Delete/Confirm contracts
    - `grep -nE "/api/memory($|/:id)" src/dashboard.ts` shows POST /api/memory, PATCH+DELETE /api/memory/:id, POST /api/memory/:id/confirm
    - DELETE route calls writeTombstoneForMemory before deleteMemory (order verified in source: tombstone call precedes delete call)
    - Add/Edit/Delete/Confirm routes contain no inline SQL (all delegate to db.ts functions)
    - `npm run build` green
    - Component reuse (no rebuilds): `grep -nE "Modal|ConfirmModal" web/src/pages/Memory.tsx` shows both reused for the Add form and the Delete confirmation
    - Exact copy present (UI-SPEC): `grep -F "Add a fact" web/src/pages/Memory.tsx`, `grep -F "Save fact"`, `grep -F "Which area?"`, `grep -F "Stored on this machine. You can edit or delete it anytime."`, `grep -F "Delete this fact?"`, `grep -F "The assistant will forget this, and it will not be learned again from your work or email."`, `grep -F "Deleted. It will not come back."`, `grep -F "Confirmed. This fact can now inform what the team does on its own."`, `grep -F "Could not save"`, `grep -F "Could not delete"` all match in Memory.tsx
    - Affordance rule: a `Confirm` action is rendered only on confirmed===0 rows (guarded in source); confirmed rows show only Edit/Delete — no disabled Confirm
    - `grep -nE "apiPost|apiPatch|apiDelete" web/src/pages/Memory.tsx` shows all three mutation helpers used; pushToast used for success/failure
    - Accent discipline: no hard-coded hex in Memory.tsx (`grep -nE "#[0-9a-fA-F]{3,6}" web/src/pages/Memory.tsx` returns nothing); solid accent confined to the Add CTA / Confirm / Save-fact primary; ConfirmModal Delete uses --color-status-failed
  </acceptance_criteria>
  <done>Operator can Add (confirmed/you-told-me/high-salience via a Modal with the in-modal assurance + `Save fact`), Edit, Delete (tombstone-first via a destructive ConfirmModal with the exact tombstone-guarantee copy), and Confirm (only on unconfirmed rows) facts from /memory; all copy/toasts match the approved UI-SPEC; Modal/ConfirmModal reused; accent reserved per contract; routes inherit auth/CSRF/kill-switch; contract tests green; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> mutation routes (POST/PATCH/DELETE) | untrusted operator input (summary, category, id) crosses here |
| delete -> ingest/consolidate | a deleted fact must not be re-derivable by the engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | SQL injection via summary/category in new mutations | mitigate | All SQL in db.ts bound via ? placeholders; never interpolate summary/category (house rule, RESEARCH V5) |
| T-06-07 | Spoofing/Tampering | CSRF on new POST/PATCH/DELETE | mitigate | Inherited Origin allowlist middleware (dashboard.ts:495); no per-route code |
| T-06-08 | Tampering | mutation during incident | mitigate | Inherited DASHBOARD_MUTATIONS_ENABLED 503 kill switch (dashboard.ts:449) |
| T-06-09 | Tampering | tombstone evasion (deleted fact re-derived) | mitigate | HIGH: tombstone written BEFORE row delete (fail-safe), hash floor is deterministic + always-on, embedding secondary at 0.88; checked at BOTH ingest dedupe point and pre-consolidation save; automated suppression test proves it |
| T-06-10 | Elevation of Privilege | category accepted outside the 3-value enum | mitigate | Validate category against {your-business, your-clients, how-you-work} on Add/Edit; reject otherwise |
| T-06-11 | Tampering | confirm double-fire | mitigate | Status-guard the confirm transition; .changes===0 is an honest no-op |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero external packages) |
</threat_model>

<verification>
- `npx vitest run src/memory-ingest.test.ts src/memory-consolidate.test.ts src/dashboard.contract.test.ts` green
- `npm run build` green
- Suppression test (success criterion 3) proves a deleted fact is not re-derived by ingest OR consolidation
- UI-SPEC copy + component-reuse + accent-discipline grep checks pass on Memory.tsx
</verification>

<success_criteria>
- Operator can edit/delete/add facts in place (MEM-02, success criteria 3 + 4)
- Deleted fact provably not silently re-derived (D-08, success criterion 3) with an automated test
- Add stores a confirmed, operator-authored, high-salience fact with the local-storage assurance shown (D-09)
- Mutation routes inherit auth/CSRF/kill-switch; no bespoke auth; no inline SQL
- Row-action affordances, Add Modal, Delete ConfirmModal, button/toast copy, and accent discipline match the approved 06-UI-SPEC.md
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-03-SUMMARY.md` when done
</output>
