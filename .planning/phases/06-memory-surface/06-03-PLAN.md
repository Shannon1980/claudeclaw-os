---
phase: 06-memory-surface
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/dashboard.ts
  - src/dashboard.contract.test.ts
  - web/src/pages/Memory.tsx
  - web/src/App.tsx
  - web/src/lib/routes.ts
  - web/src/lib/vocabulary.ts
autonomous: false
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A new /memory operator page shows facts grouped by category with provenance as the hero pill (MEM-01, D-01)"
    - "Each fact shows You told me / Learned from your work / Learned from email and can be edited or deleted in place (MEM-02, D-03)"
    - "An operator can add a fact and see the on-this-machine assurance; the fact is stored confirmed/you-told-me (D-09, crit 4)"
    - "Unconfirmed facts show a Needs review marker and a Confirm action (D-04)"
    - "The developer Memories view is demoted off the daily nav but still reachable (D-02); /memory no longer redirects to /memories"
  artifacts:
    - path: "web/src/pages/Memory.tsx"
      provides: "Operator Memory surface modeled on Activity.tsx"
      min_lines: 120
    - path: "src/dashboard.ts"
      provides: "GET + POST + PATCH + DELETE + confirm /api/memory* routes"
      contains: "/api/memory"
  key_links:
    - from: "web/src/pages/Memory.tsx"
      to: "/api/memory"
      via: "apiGet grouped read + apiPost mutations"
      pattern: "/api/memory"
    - from: "src/dashboard.ts DELETE /api/memory/:id"
      to: "writeTombstoneForMemory + deleteMemory"
      via: "tombstone-first then delete (Pitfall 6)"
      pattern: "writeTombstoneForMemory"
    - from: "web/src/lib/routes.ts"
      to: "/memory RouteDef"
      via: "intelligence section nav entry; /memories demoted"
      pattern: "/memory"
---

<objective>
The visible end-to-end slice. Wire the plan-02 backend into a new operator-facing Memory page ("What I know about you") and the four mutation routes behind it. After this plan, a real operator can do something they could not before: open Memory, see facts grouped by category with provenance, add a fact (with the on-this-machine assurance), edit, delete (tombstone-backed), and confirm an inferred fact.

Covers MEM-01 (grouped view), MEM-02 (provenance + edit/delete in place), D-01 (new operator route distinct from the developer view), D-02 (demote the developer Memories view to Labs-style hidden nav), D-04 (Needs review marker + Confirm action on the surface), D-05 (email tag only if email data exists), D-07 (hide empty categories), D-09 (Add a fact). Ends with a human-verify checkpoint of the live surface.

Purpose: This is the trust surface itself — the reason the phase exists.
Output: net-new /api/memory* routes in dashboard.ts, web/src/pages/Memory.tsx, routes/App/vocabulary wiring, Labs demotion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-UI-SPEC.md
@.planning/phases/06-memory-surface/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Net-new /api/memory* mutation + grouped-read routes</name>
  <files>src/dashboard.ts, src/dashboard.contract.test.ts</files>
  <read_first>
    - src/dashboard.ts lines 2300-2323 (existing read-only memory routes + ALLOWED_CHAT_ID scope), 432 (requireToken), 449 (DASHBOARD_MUTATIONS_ENABLED kill switch), 495 (CSRF Origin allowlist), 1603-1634 (task DELETE/PATCH house style), 3587 (approve status-guard)
    - src/db.ts (plan-02 functions: getMemoriesForOperatorSurface, addOperatorFact, deleteMemory, writeTombstoneForMemory, deriveProvenance)
    - src/dashboard.contract.test.ts (plan-01 route contract assertions)
    - 06-RESEARCH.md "Net-New Mutation API Routes" table
  </read_first>
  <behavior>
    - GET /api/memory: returns operator-surface rows scoped to ALLOWED_CHAT_ID, each with a derived provenance tag via deriveProvenance, grouped/groupable by category; omits the email tag unless an email-sourced row exists (D-05).
    - POST /api/memory {summary, category}: addOperatorFact -> confirmed, source='you-told-me' (D-09); validates category against the 3-value enum, returns ok+id.
    - PATCH /api/memory/:id {summary?, category?}: updates fields (FTS trigger keeps search synced); validates id (Number.isInteger) and category enum.
    - DELETE /api/memory/:id: writeTombstoneForMemory(id) FIRST (404 if not found), THEN deleteMemory(id) (Pitfall 6 fail-safe ordering).
    - POST /api/memory/:id/confirm: sets confirmed=1, status-guarded so a double-click is a no-op (.changes check).
    - All four mutations inherit token gate + kill switch + CSRF from app middleware — no per-route auth (RESEARCH Don't Hand-Roll).
  </behavior>
  <action>
    Add the four routes in src/dashboard.ts beside the existing read routes (~:2300), mounting under /api/memory. Delegate ALL SQL to the plan-02 db.ts functions (never raw SQL in the handler). GET maps each row through deriveProvenance and applies the D-05 email-omission rule. POST/PATCH validate category against {your-business, your-clients, how-you-work} and id via Number.isInteger, returning c.json({ ok:false, error }, 400) on bad input. DELETE calls writeTombstoneForMemory then deleteMemory in that order, 404 when the memory is missing. confirm status-guards on .changes. Do NOT add bespoke auth/CSRF/kill-switch — the middleware at :432/:449/:495 covers non-GET automatically. Update src/dashboard.contract.test.ts assertions to match the implemented routes.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts</automated>
  </verify>
  <done>All four /api/memory* routes pass the contract test: id + category validation, tombstone-first delete, status-guarded confirm, GET returns provenance-tagged grouped rows honoring D-05.</done>
  <acceptance_criteria>
    - `npx vitest run src/dashboard.contract.test.ts` GREEN.
    - DELETE handler calls writeTombstoneForMemory before deleteMemory (grep ordering in the handler).
    - No raw SQL string in the new handlers (grep the route bodies — all DB access via db.ts function calls).
    - Category enum validation present on POST and PATCH.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Operator Memory page + nav wiring + Labs demotion (UI slice)</name>
  <files>web/src/pages/Memory.tsx, web/src/App.tsx, web/src/lib/routes.ts, web/src/lib/vocabulary.ts</files>
  <read_first>
    - web/src/pages/Activity.tsx (the closest analog: PageHeader/Tab, PageState, Pill, grouped useMemo, ActivityRowCard mutation pattern, ConfirmModal, toneForTag, apiGet/apiPost, pushToast)
    - web/src/components/Pill.tsx (tones: accent, medium, neutral), web/src/components/ConfirmModal.tsx, web/src/components/Modal.tsx, web/src/components/PageState.tsx
    - web/src/lib/routes.ts lines 27-46 (ROUTES array; /memories at :34 is the developer view to demote; Audit D-13 demotion comment at :38-42 is the precedent)
    - web/src/App.tsx lines 57-72 (route block; :69 `<Route path="/memory"><Redirect to="/memories" /></Route>` MUST be removed)
    - web/src/lib/vocabulary.ts (term() keys)
    - 06-UI-SPEC.md (full Copywriting Contract, Color, Typography, Spacing, Interaction Contract)
  </read_first>
  <action>
    Create web/src/pages/Memory.tsx modeled on Activity.tsx. Header: title "What I know about you", muted assurance "Stored on this machine. Edit or delete anything." (crit 4), and an accent "Add a fact" CTA in the PageHeader action slot. Load via apiGet('/api/memory') with the cancel-guard pattern; group rows by category with useMemo, rendering a category section only when it has >=1 row (D-07); category section labels use the 10px uppercase tracking-wider section-label role. Each fact row is a card (bg var(--color-elevated) border var(--color-border) rounded-md p-3, gap-4 stack, mb-8 between groups): fact text at text-[12.5px] leading-snug, provenance Pill right-aligned on the top row as the hero (You told me -> accent tone, Learned from your work / Learned from email -> neutral; email pill only when present, D-05). Unconfirmed (confirmed=0) rows carry a "Needs review" medium/amber Pill and a "Confirm" accent action (D-04); confirmed rows show "Edit"/"Delete" only — buttons that cannot act are ABSENT, never disabled. Edit opens a Modal (fact text + category picker, "Save fact"); Delete uses ConfirmModal destructive with title "Delete this fact?", body stating the tombstone guarantee, success toast "Deleted. It will not come back."; failures surface the verbatim server reason via pushToast. Confirm calls POST /api/memory/:id/confirm with success toast "Confirmed. This fact can now inform what the team does on its own." Add opens a Modal: fact text + "Which area?" category picker + assurance line + "Save fact" -> POST /api/memory. Use PageState for loading/error/empty (empty heading "Nothing here yet"). Use only var(--color-*) tokens, never hard-coded hex; accent reserved for the four UI-SPEC roles only.

    Wire nav: add `{ path: '/memory', label: 'Memory', vocabKey: 'nav.memory', section: 'intelligence', icon: <a lucide icon e.g. BookOpen> }` to routes.ts ROUTES; add the matching `<Route path="/memory"><Memory /></Route>` in App.tsx (the TRACKED file — never the untracked app.tsx, Pitfall 2); REMOVE the App.tsx:69 `/memory -> /memories` redirect (RESEARCH Open Q2). Demote the developer view (D-02): remove the `/memories` RouteDef from the visible intelligence nav list following the Audit D-13 comment precedent at routes.ts:38-42, keeping its `<Route path="/memories"><Memories /></Route>` in App.tsx for deep-link/command-palette access. Add the `nav.memory` term in vocabulary.ts.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -Eiq 'error' && echo TSFAIL || echo TSOK</automated>
  </verify>
  <done>web/src/pages/Memory.tsx renders grouped-by-category facts with provenance pills, Add/Edit/Delete/Confirm actions, the assurance line, and PageState; /memory is the operator route, /memories is demoted but reachable, App.tsx:69 redirect removed; TypeScript compiles clean.</done>
  <acceptance_criteria>
    - `cd web && npx tsc --noEmit` reports no errors in Memory.tsx / App.tsx / routes.ts.
    - `grep -n "/memory" web/src/lib/routes.ts` shows the new operator RouteDef; `grep -n 'Redirect to="/memories"' web/src/App.tsx` returns nothing (redirect removed).
    - `grep -c 'var(--color' web/src/pages/Memory.tsx` > 0 and `grep -Ec '#[0-9a-fA-F]{6}' web/src/pages/Memory.tsx` == 0 (no hard-coded hex).
    - "Add a fact", "What I know about you", "Stored on this machine. Edit or delete anything.", "Needs review", "Delete this fact?" copy strings present verbatim (UI-SPEC Copywriting Contract).
    - Edited App.tsx is the tracked file: `git ls-files web/src/App.tsx` lists it.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live Memory surface</name>
  <what-built>
    A new operator Memory page at /memory: facts grouped by category, provenance pill as the hero per row, Add a fact (with on-this-machine assurance), Edit, Delete (tombstone-backed), and Confirm on unconfirmed facts. The developer Memories view is demoted off the daily nav (still reachable). The four /api/memory* routes are live behind the token gate + kill switch + CSRF.
  </what-built>
  <how-to-verify>
    1. Start the dashboard (npm run dev or the existing dev command) and open the app; click Memory in the Intelligence nav (or visit /memory?token=...).
    2. Confirm the header reads "What I know about you" with the assurance line "Stored on this machine. Edit or delete anything."
    3. Confirm facts are grouped under Your business / Your clients / How you like to work, empty categories are not shown, and each row shows a provenance pill (You told me / Learned from your work; email pill only if email data exists).
    4. Click "Add a fact", add a fact in one category, save -> it appears with a "You told me" pill.
    5. Edit that fact's text -> it persists. Delete it via the confirm modal -> toast "Deleted. It will not come back."; confirm it does not reappear on reload.
    6. If an unconfirmed fact exists, confirm it shows the amber "Needs review" marker and a "Confirm" action; click Confirm -> toast confirms it can now inform behavior.
    7. Confirm /memories (developer Brain Graph view) is no longer in the daily sidebar but still loads via direct URL / command palette.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <files>web/src/pages/Memory.tsx, web/src/App.tsx (live surface)</files>
  <action>Operator-only verification step: no code is written. Claude has already automated the full surface (page + routes + nav) in Tasks 1-2; this task pauses for the operator to confirm the live /memory surface against the how-to-verify steps before the phase is marked done.</action>
  <verify><human-check>Operator walks the 7 how-to-verify steps and types "approved" or lists issues.</human-check></verify>
  <done>Operator types "approved" confirming the live Memory surface: grouped view, provenance pills, Add/Edit/Delete/Confirm, on-this-machine assurance, and the demoted-but-reachable developer view all behave as specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> /api/memory* | Untrusted fact summary + category cross here; validated server-side |
| dashboard route -> db.ts | All SQL delegated to db.ts functions; no SQL in handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing/Tampering | POST/PATCH/DELETE /api/memory* | mitigate | Inherited token gate (:432), CSRF Origin allowlist (:495), kill switch (:449) cover all non-GET; no bespoke auth |
| T-06-03 | Tampering | summary/category injection | mitigate | category validated against 3-value enum; id via Number.isInteger; SQL bound in db.ts (ASVS V5) |
| T-06-04 | Repudiation | deleted fact re-derived | mitigate | DELETE writes tombstone before removing the row (Pitfall 6); plan-04 wires the ingest/consolidate checks |
| T-06-06 | Information Disclosure | implying email coverage that does not exist | mitigate | D-05 honest coverage: email pill rendered only when an email-sourced row exists |
| T-06-07 | Information Disclosure | local-only assurance falsely shown | accept | Memories table is local SQLite only (RESEARCH Runtime State Inventory: no external memory store); assurance is truthful |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/dashboard.contract.test.ts` GREEN; `cd web && npx tsc --noEmit` clean.
- /memory is the operator surface; /memories demoted but reachable; App.tsx redirect removed.
- Human-verify checkpoint (Task 3) approved by the operator.
</verification>

<success_criteria>
- Operator can view (grouped + provenance), add, edit, delete, and confirm facts on the new surface (MEM-01, MEM-02, D-09).
- Provenance is the hero; empty categories hidden; email tag honest; on-this-machine assurance present.
- Developer view relocated, not deleted (D-02).
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-03-SUMMARY.md` when done.
</output>
