---
phase: 06-memory-surface
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/memory-ingest.ts
  - src/memory-consolidate.ts
  - src/memory-ingest.test.ts
  - src/memory-consolidate.test.ts
  - scripts/backfill-memory-categories.ts
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A deleted fact is not silently re-derived by ingest (tombstone checked before save) (D-08, crit 3)"
    - "A deleted fact is not re-synthesized by consolidation (tombstone checked before saveConsolidationAtomic) (D-08)"
    - "Newly ingested facts get a category via the existing Haiku extractor; unknown -> NULL (D-06, D-07)"
    - "Existing rows can be backfilled with a category by an idempotent script (D-06)"
  artifacts:
    - path: "src/memory-ingest.ts"
      provides: "Tombstone gate at the dedupe point + category from extraction prompt"
      contains: "isTombstoned"
    - path: "src/memory-consolidate.ts"
      provides: "Tombstone gate before saveConsolidationAtomic"
      contains: "isTombstoned"
    - path: "scripts/backfill-memory-categories.ts"
      provides: "Idempotent one-time category backfill of category IS NULL rows"
      min_lines: 30
  key_links:
    - from: "src/memory-ingest.ts ingestConversationTurn"
      to: "isTombstoned"
      via: "checked before saveStructuredMemoryAtomic"
      pattern: "isTombstoned"
    - from: "src/memory-consolidate.ts runConsolidation"
      to: "isTombstoned"
      via: "checked before saveConsolidationAtomic"
      pattern: "isTombstoned"
---

<objective>
The enforcement slice — replace the stubbed behaviors with the real engine hooks that make D-08 (no re-derivation) and D-06 (category populated) genuinely true end-to-end. Wire the plan-02 tombstone helpers into BOTH the ingest dedupe point and the consolidation save path so a deleted fact never returns. Extend the existing Haiku extraction prompt to also classify a fact's category on ingest. Add an idempotent backfill script for existing rows.

This completes the slice the operator surface depends on: after plan 03 the operator can delete a fact, and after this plan the engine actually honors that delete forever (not a soft hide). Categories shown on the surface get populated for new and existing facts.

Purpose: Turn the genuinely-new mechanism (tombstone) on, and make category real data rather than always-NULL.
Output: ingest + consolidate hooks, extended extraction prompt, backfill script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-02-SUMMARY.md
@.planning/phases/06-memory-surface/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tombstone gate + category classification on ingest (D-08, D-06)</name>
  <files>src/memory-ingest.ts, src/memory-ingest.test.ts</files>
  <read_first>
    - src/memory-ingest.ts lines 39 (extractViaClaude), 111 (EXTRACTION_PROMPT), 161-244 (ingestConversationTurn, embedText at :214, dedupe loop :219-232, saveStructuredMemoryAtomic at :234, importance clamp at :208)
    - src/db.ts plan-02 helpers: isTombstoned, normalizeSummary, TOMBSTONE_COSINE_THRESHOLD; saveStructuredMemoryAtomic signature (whether it now accepts category)
    - src/embeddings.ts (cosineSimilarity)
    - src/memory-ingest.test.ts (plan-01 tombstone suppression + category cases)
    - 06-RESEARCH.md "Tombstone / Suppression" hook points + "Category Column" prompt extension; Pitfall 5 (threshold) and Pitfall 6
  </read_first>
  <behavior>
    - Before saving, ingestConversationTurn computes sha256(normalizeSummary(result.summary)) and calls isTombstoned(chatId, hash, embedding); if true it returns/skips without creating a row and logs a "Skipping tombstoned memory" debug line (D-08, crit 3).
    - Re-feeding the SAME turn (or a >0.88-cosine paraphrase when embeddings present) after a delete creates NO new row.
    - The extraction prompt also returns `category` constrained to "your-business" | "your-clients" | "how-you-work" | null; the value is validated/clamped like importance and persisted on the new memory; unknown/invalid -> NULL (D-07).
  </behavior>
  <action>
    In src/memory-ingest.ts ingestConversationTurn: after embedText (:214) and before the existing cosine dedupe loop (:219), add the tombstone check using the plan-02 isTombstoned + normalizeSummary helpers (hash floor always-on; cosine secondary when embedding present), early-returning false with a logger.debug "Skipping tombstoned memory" when matched (Pitfall 6 ordering already handled by the route writing tombstone-first). Extend EXTRACTION_PROMPT (:111) so the returned JSON includes a `category` enum field; parse + validate it against {your-business, your-clients, how-you-work} (clamp like importance at :208), pass it through saveStructuredMemoryAtomic (:234) to persist on the row; null/unknown -> NULL. Reuse the existing extractViaClaude call — do NOT add a new LLM path or API key. Make tombstone tests + category-on-ingest tests pass.
  </action>
  <verify>
    <automated>npx vitest run src/memory-ingest.test.ts</automated>
  </verify>
  <done>Re-fed deleted content yields no new row (hash floor + cosine); newly ingested facts carry a validated category or NULL; no new LLM path added.</done>
  <acceptance_criteria>
    - `npx vitest run src/memory-ingest.test.ts` GREEN for tombstone suppression + category cases.
    - `grep -c isTombstoned src/memory-ingest.ts` >= 1, placed before saveStructuredMemoryAtomic.
    - EXTRACTION_PROMPT returns a category field validated against the 3-value enum; invalid -> NULL.
    - No new `@google/genai` or Anthropic client construction added (reuses extractViaClaude).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tombstone gate on the consolidation path (D-08)</name>
  <files>src/memory-consolidate.ts, src/memory-consolidate.test.ts</files>
  <read_first>
    - src/memory-consolidate.ts (runConsolidation; the save point before saveConsolidationAtomic)
    - src/db.ts plan-02 helpers: isTombstoned, normalizeSummary
    - src/memory-consolidate.test.ts (plan-01 synthesized-fact suppression case)
    - 06-RESEARCH.md "Tombstone / Suppression" second hook point
  </read_first>
  <behavior>
    - Before saveConsolidationAtomic, runConsolidation hashes the synthesized summary/insight and calls isTombstoned; a tombstoned synthesized fact is NOT saved (so a deleted fact cannot re-enter disguised as a consolidation).
  </behavior>
  <action>
    In src/memory-consolidate.ts runConsolidation, before the saveConsolidationAtomic call, compute sha256(normalizeSummary(synthesizedSummary)) and call isTombstoned(chatId, hash, embedding?) using the plan-02 helpers; skip the save (and log a debug line) when matched. Reuse the same helpers as ingest — do not duplicate the hash/cosine logic. Make the plan-01 consolidation suppression case pass.
  </action>
  <verify>
    <automated>npx vitest run src/memory-consolidate.test.ts</automated>
  </verify>
  <done>A synthesized fact matching a tombstone is not saved by consolidation; the plan-01 case is GREEN.</done>
  <acceptance_criteria>
    - `npx vitest run src/memory-consolidate.test.ts` GREEN.
    - `grep -c isTombstoned src/memory-consolidate.ts` >= 1, before saveConsolidationAtomic.
    - The same db.ts helpers are reused (no re-implemented hash/cosine in this file).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Idempotent category backfill script (D-06)</name>
  <files>scripts/backfill-memory-categories.ts</files>
  <read_first>
    - scripts/migrate.ts (how a standalone tsx script opens the store DB / process.cwd patterns)
    - src/memory-ingest.ts lines 39 (extractViaClaude) + the 429-backoff handling to copy
    - src/db.ts (how to open the store handle + parameterized UPDATE)
    - 06-RESEARCH.md "One-time backfill of existing rows"
  </read_first>
  <action>
    Create scripts/backfill-memory-categories.ts (runnable via tsx) that selects memories WHERE category IS NULL, classifies each summary via a small classify-only extractViaClaude prompt returning one of {your-business, your-clients, how-you-work, null}, and UPDATEs the row with a parameterized statement. Make it idempotent (only touches category IS NULL, safe to re-run) and rate-aware by copying the ingest 429-backoff model. Reuse extractViaClaude (no new LLM path/key). Validate each returned value against the enum; unknown -> leave NULL (D-07). Log progress counts; do not throw the whole run on a single classify failure (skip + continue).
  </action>
  <verify>
    <automated>! (npx tsc --noEmit scripts/backfill-memory-categories.ts 2>&1 | grep -qi error)</automated>
  </verify>
  <done>scripts/backfill-memory-categories.ts compiles, selects only category IS NULL rows, classifies via the existing extractor, and updates with parameterized SQL; safe to re-run.</done>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports no errors for the script.
    - The SELECT filters `category IS NULL` (idempotency) and the UPDATE binds values via `?` placeholders.
    - Returned categories validated against the 3-value enum; invalid -> NULL.
    - Reuses extractViaClaude (no new API client construction in the script).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest/consolidate engine -> memories | Machine-derived/synthesized facts gated by tombstone before persistence |
| backfill script -> Claude (Haiku) | Only fact summaries (already stored) sent for classification; no secrets/raw env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Repudiation | deleted fact re-derived (ingest) | mitigate | isTombstoned checked before saveStructuredMemoryAtomic; hash floor deterministic, cosine secondary at named 0.88 (D-08, Pitfall 5) |
| T-06-08 | Repudiation | deleted fact re-synthesized (consolidation) | mitigate | isTombstoned checked before saveConsolidationAtomic on the synthesized text (D-08) |
| T-06-09 | Information Disclosure | backfill sending sensitive content to LLM | accept | Only already-stored summaries sent to the existing Haiku-via-OAuth extractor (no new egress path); same trust level as live ingest |
| T-06-03 | Tampering | SQL injection in backfill UPDATE | mitigate | Parameterized `?` binds; category validated against enum (ASVS V5) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npm test` passes (full suite green): ingest + consolidate suppression + category-on-ingest cases all GREEN.
- `grep -c isTombstoned src/memory-ingest.ts src/memory-consolidate.ts` shows the gate in both engine paths.
- Backfill script compiles and is idempotent (filters category IS NULL).
- No new external package or LLM/API-key path introduced (reuses extractViaClaude).
</verification>

<success_criteria>
- D-08 enforced for real on both write paths: a deleted fact is not silently re-derived or re-synthesized (crit 3).
- D-06 categories populated for new ingests and backfillable for existing rows; unknown stays NULL (D-07).
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-04-SUMMARY.md` when done.
</output>
