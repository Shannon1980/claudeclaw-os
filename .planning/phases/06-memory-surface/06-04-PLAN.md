---
phase: 06-memory-surface
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/db.ts
  - src/memory-ingest.ts
  - src/memory.ts
  - src/memory-projection.ts
  - scripts/backfill-memory-categories.ts
autonomous: false
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - "An unconfirmed (machine-inferred) fact does NOT influence assistant behavior: it is absent from buildMemoryContext output and absent from renderMemoryProjection output until the operator confirms it (D-04)"
    - "Once confirmed, the fact appears in both behavior read paths"
    - "New machine-inferred facts are classified into one of the three categories (or NULL) on ingest via the existing Claude-Haiku extractor (no new key, no new package) (D-06)"
    - "Existing rows are backfilled with a category via a one-time idempotent script so the operator surface groups them meaningfully"
    - "Empty categories remain hidden on the surface (D-07)"
  artifacts:
    - path: "scripts/backfill-memory-categories.ts"
      provides: "One-time idempotent LLM category backfill for rows where category IS NULL"
      min_lines: 20
    - path: "src/db.ts"
      provides: "AND confirmed = 1 appended to the three behavior-feeding readers"
      contains: "confirmed = 1"
  key_links:
    - from: "src/memory.ts buildMemoryContext"
      to: "src/db.ts gated readers (searchMemories/getRecentHighImportanceMemories/getMemoriesWithEmbeddings)"
      via: "confirmed=1 filter at the reader layer"
      pattern: "confirmed = 1"
    - from: "src/memory-projection.ts renderMemoryProjection"
      to: "src/db.ts getRecentHighImportanceMemories"
      via: "confirmed=1 filter inherited from the gated reader"
      pattern: "getRecentHighImportanceMemories"
    - from: "src/memory-ingest.ts EXTRACTION_PROMPT"
      to: "category field"
      via: "extractViaClaude returns category enum, passed to save"
      pattern: "category"
---

<objective>
Final slice: close the trust loop. (1) The confirmed/unconfirmed behavior gate (D-04) — unconfirmed
machine-inferred facts must NOT influence assistant behavior or permission defaults until the operator
confirms them, applied once at the db.ts reader layer so BOTH behavior read paths are covered. (2) Category
classification on ingest + a one-time backfill (D-06) so the operator surface groups facts meaningfully.
Closes with the end-of-phase human-verify checkpoint.

Purpose: This is the invisible-but-critical half of the trust surface — it is the join where Memory feeds
Permissions (the reason D-04 was chosen as the larger-scope option). Applying the gate at the shared reader
layer (mirroring the existing superseded_by IS NULL clause) guarantees neither buildMemoryContext nor
renderMemoryProjection leaks an unconfirmed fact (Pitfall 3).

Output: AND confirmed=1 on the three behavior readers in db.ts, the category field folded into the existing
extraction prompt, the backfill script, and operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-surface/06-CONTEXT.md
@.planning/phases/06-memory-surface/06-RESEARCH.md
@.planning/phases/06-memory-surface/06-PATTERNS.md
@.planning/phases/06-memory-surface/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmed gate on the three behavior readers (both read paths)</name>
  <files>src/db.ts, src/memory.ts, src/memory-projection.ts</files>
  <read_first>
    - src/memory.test.ts and src/memory-projection.test.ts (the Wave 0 gate contracts to make GREEN)
    - src/db.ts:1131 getMemoriesWithEmbeddings (already carries AND superseded_by IS NULL — append AND confirmed=1), :1020 searchMemories (vector branch IN-query :1041 + FTS branch), :1150 getRecentHighImportanceMemories (NOTE: does NOT currently carry superseded_by IS NULL — add confirmed=1 here, consider the superseded clause too)
    - src/memory.ts:63 buildMemoryContext (calls all three readers), src/memory-projection.ts:29-42 renderMemoryProjection (calls getRecentHighImportanceMemories — Pitfall 3, the easily-forgotten second path)
  </read_first>
  <action>
    Append `AND confirmed = 1` to the three behavior-feeding readers in src/db.ts, exactly mirroring the
    existing `AND superseded_by IS NULL` clause shape: getMemoriesWithEmbeddings (:1131/:1136),
    searchMemories (both the vector IN-query branch :1041 AND the FTS branch), and
    getRecentHighImportanceMemories (:1150 — add confirmed=1; also add superseded_by IS NULL while in it,
    since it currently lacks it). Do NOT touch getMemoriesForOperatorSurface (Plan 02) — the operator
    surface must keep showing unconfirmed rows. Verify src/memory.ts buildMemoryContext and
    src/memory-projection.ts renderMemoryProjection route through these gated readers and need no
    per-call-site filter (the gate lives at the reader layer per Pitfall 3); make any small adjustment only
    if a path bypasses the gated readers.
  </action>
  <verify>
    <automated>npx vitest run src/memory.test.ts src/memory-projection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory.test.ts GREEN: an unconfirmed fact is absent from buildMemoryContext output; present after confirm
    - src/memory-projection.test.ts GREEN: an unconfirmed fact is absent from renderMemoryProjection output
    - `grep -cn "confirmed = 1" src/db.ts` >= 3 (all three behavior readers gated)
    - getMemoriesForOperatorSurface body does NOT contain `confirmed = 1` (operator surface still shows unconfirmed)
  </acceptance_criteria>
  <done>Unconfirmed facts excluded from BOTH behavior read paths at the shared reader layer; confirm flips them in; operator surface unaffected; gate tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Category classification on ingest + one-time backfill script</name>
  <files>src/memory-ingest.ts, scripts/backfill-memory-categories.ts</files>
  <read_first>
    - src/memory-ingest.ts:111 EXTRACTION_PROMPT, :39 extractViaClaude (Haiku via OAuth — no key, no quota wall), :205-208 the importance validate/clamp pattern to mirror for category, :234-244 saveStructuredMemoryAtomic call (pass category through)
    - src/memory-consolidate.ts (the 429 backoff model to copy for the backfill script)
    - src/db.ts:2346 reader shapes (for selecting rows WHERE category IS NULL in the backfill)
  </read_first>
  <action>
    Extend EXTRACTION_PROMPT (src/memory-ingest.ts:111) so the returned JSON also includes
    "category": "your-business" | "your-clients" | "how-you-work" | null. Validate/clamp against that enum
    exactly like the existing importance handling (:205-208); null/unknown -> store NULL (D-07: stays in
    data, hidden from the surface). Pass category through the saveStructuredMemoryAtomic call (:234-244) so
    new ingested memories carry a category and remain confirmed=0 (machine-inferred). Create
    scripts/backfill-memory-categories.ts (tsx-runnable, standalone): SELECT rows WHERE category IS NULL,
    classify each summary via the same extractViaClaude classify prompt, UPDATE the row; batch + 429-backoff
    aware (copy the ingest/consolidate backoff model); idempotent (only touches category IS NULL, safe to
    re-run). This is a data migration distinct from the schema migration; document running it after
    npm run migrate.
  </action>
  <verify>
    <automated>npx vitest run src/memory-ingest.test.ts && (npx tsc --noEmit scripts/backfill-memory-categories.ts 2>&1 | grep -vi "node_modules" | grep -qi error && { echo "TS ERROR"; exit 1; } || echo "TS OK")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "your-business|your-clients|how-you-work" src/memory-ingest.ts` shows the category enum folded into the extraction contract
    - `grep -n "category" src/memory-ingest.ts` shows category validated/clamped (mirroring importance) and passed to the save call
    - `ls scripts/backfill-memory-categories.ts` exists; it only updates rows WHERE category IS NULL (idempotent) — verified by reading the WHERE clause
    - src/memory-ingest.test.ts still GREEN (tombstone + now category on ingest)
  </acceptance_criteria>
  <done>New ingested facts are categorized via the existing extractor (no new key/package); existing rows backfillable via an idempotent script; null/unknown stays NULL and hidden (D-07).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase operator verification of the Memory surface</name>
  <files>n/a — verification checkpoint, no files modified</files>
  <read_first>
    - .planning/phases/06-memory-surface/06-UI-SPEC.md (APPROVED UI contract — the verification steps below check the live surface against it: provenance pill as the primary anchor with correct tones, amber needs-review marker, exact copy, Add-modal assurance, accent discipline, Labs demotion)
  </read_first>
  <action>
    Pause for the operator to verify the full Memory Surface end-to-end against the approved 06-UI-SPEC.md.
    Before pausing, ensure `npm test` (full suite) is green (the phase gate per 06-VALIDATION.md). Present the
    how-to-verify steps and wait for the resume signal. This is a blocking human-verify checkpoint — do not
    auto-advance.
  </action>
  <what-built>
    The full Memory Surface, built to the approved 06-UI-SPEC.md: a new /memory operator page ("What I know
    about you") showing facts grouped by category with the provenance Pill as the right-aligned row hero and
    an amber "Needs review" marker on unconfirmed facts; Add (Modal + in-modal assurance) / Edit / Delete
    (destructive ConfirmModal) / Confirm in place with contract copy + toasts; deleted facts suppressed from
    re-derivation (tombstone); unconfirmed facts excluded from assistant behavior until confirmed; the
    developer view (/memories, BrainGraph) demoted off the visible nav but still reachable.
  </what-built>
  <how-to-verify>
    1. Run `npm run migrate` against the live store, then (optionally) `npx tsx scripts/backfill-memory-categories.ts` to classify existing rows. Restart the service.
    2. Open the dashboard and visit /memory. Confirm the page title reads exactly "What I know about you" and the assurance subtitle reads exactly "Stored on this machine. Edit or delete anything."
    3. Confirm facts are grouped under the category tabs "Your business" / "Your clients" / "How you like to work"; empty categories are NOT shown (D-07). The active tab is the only solid-accent element in that area.
    4. Provenance is the hero: confirm each row shows a provenance Pill right-aligned on its top line. "You told me" reads as a soft-accent tint; "Learned from your work" (and "Learned from email", which appears only if an email-sourced fact exists) read as quiet neutral pills. None of these provenance pills use solid accent.
    5. Confirm any machine-inferred (unconfirmed) facts show an amber "Needs review" marker (not red, not accent) and carry Confirm / Edit / Delete; confirmed rows carry only Edit / Delete (no dead/disabled Confirm). Click Confirm on one; the success toast reads "Confirmed. This fact can now inform what the team does on its own." and the marker clears.
    6. Click "Add a fact" (the one filled-accent CTA in the header). Confirm the Modal title is "Add a fact", the category picker label is "Which area?", the in-modal assurance reads "Stored on this machine. You can edit or delete it anytime.", and the save button reads "Save fact". Save one; confirm it appears tagged "You told me".
    7. Edit a fact's summary; confirm the change persists after reload. Click Delete; confirm the ConfirmModal title is "Delete this fact?" with body "The assistant will forget this, and it will not be learned again from your work or email." and a red Delete button; confirm; the success toast reads "Deleted. It will not come back." and the row disappears.
    8. Confirm an empty category renders the empty state heading "Nothing here yet" via PageState (no hand-rolled empty markup).
    9. Confirm /memories (the developer BrainGraph view) is NOT in the visible nav but still loads if you navigate to it directly (e.g. via the command palette or URL) — the D-02 Labs demotion.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 9 verification steps pass on the live dashboard, matching the approved 06-UI-SPEC.md.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
  <done>Operator has signed off that the Memory Surface works end-to-end across all 9 verification steps and matches the approved 06-UI-SPEC.md (provenance pill anchor + tones, amber needs-review marker, exact copy, Add-modal assurance, accent discipline, Labs demotion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory reader -> assistant prompt/projection | unconfirmed facts must not cross into behavior-influencing reads |
| ingest extractor -> category | LLM-returned category must be constrained, not trusted raw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Elevation of Privilege | unconfirmed fact influencing a permission/behavior decision | mitigate | HIGH: D-04 gate applied at the shared db.ts reader layer (AND confirmed=1) covering BOTH buildMemoryContext and renderMemoryProjection; tested on both paths so an unconfirmed fact cannot shift autonomy defaults or assistant behavior |
| T-06-13 | Tampering | LLM-returned category outside the enum | mitigate | Validate/clamp category against {your-business, your-clients, how-you-work}; null/unknown -> NULL, never an injected/free-form bucket |
| T-06-14 | Information Disclosure | backfill script prompt content | mitigate | Backfill classifies the stored summary only via the existing scrubbed-env extractViaClaude path (no secrets/raw env in the prompt), reusing the shipped Haiku-via-OAuth path |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero external packages) |
</threat_model>

<verification>
- `npx vitest run src/memory.test.ts src/memory-projection.test.ts src/memory-ingest.test.ts` green
- `npm test` (full suite) green before /gsd-verify-work (phase gate per VALIDATION.md)
- Operator sign-off on Task 3 checkpoint (verified against the approved 06-UI-SPEC.md)
</verification>

<success_criteria>
- Unconfirmed facts excluded from both behavior read paths until confirmed (D-04); confirmed facts influence behavior
- New facts categorized on ingest; existing rows backfillable idempotently (D-06); empty categories hidden (D-07)
- Full Memory Surface verified end-to-end by the operator against the approved 06-UI-SPEC.md
</success_criteria>

<output>
Create `.planning/phases/06-memory-surface/06-04-SUMMARY.md` when done
</output>
