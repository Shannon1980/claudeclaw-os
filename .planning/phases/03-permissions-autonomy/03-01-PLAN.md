---
phase: 03-permissions-autonomy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/gate.test.ts
  - src/permissions-config.test.ts
  - src/approval-queue.test.ts
  - src/agent.test.ts
  - src/dashboard.contract.test.ts
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04]
must_haves:
  truths:
    - "Running the gate/queue/config test files fails RED with 'cannot find module' or unimplemented-assertion errors (proving the contract is pinned before implementation)."
    - "Every PERM-01..04 requirement and D-10 / gate-wiring behavior has at least one failing test pinning it."
  artifacts:
    - path: "src/gate.test.ts"
      provides: "classifyTier + resolveOutcome mode matrix + Tier 4 lock + background-queue-deny + audit-recorded RED tests"
      contains: "resolveOutcome mode matrix"
    - path: "src/permissions-config.test.ts"
      provides: "default-balanced + override round-trip RED tests"
      contains: "default balanced"
    - path: "src/approval-queue.test.ts"
      provides: "enqueue/list/approve/deny/expire + replay-once RED tests"
      contains: "approval-queue"
    - path: "src/agent.test.ts"
      provides: "gate-wiring assertion (no bypassPermissions, canUseTool present)"
      contains: "gate wired"
  key_links:
    - from: "src/gate.test.ts"
      to: "src/gate.ts (not yet created)"
      via: "import { classifyTier, resolveOutcome, makeCanUseTool }"
      pattern: "from './gate"
---

<objective>
Wave 0: author the failing test suite that pins the Phase 3 permission contract BEFORE any implementation exists. These are the RED tests of TDD — they reference modules (`gate.ts`, `permissions-config.ts`, `approval-queue.ts`) that do not exist yet and assert behaviors that later plans must satisfy.

Purpose: lock the four-tier classification, mode-resolution, Tier 4 lock, queue state machine, audit recording, and gate-wiring behaviors as executable specifications so plans 02-04 are graded against them, not against interpretation.
Output: five test files (`src/gate.test.ts`, `src/permissions-config.test.ts`, `src/approval-queue.test.ts` new; `src/agent.test.ts`, `src/dashboard.contract.test.ts` new/extended) that run RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-permissions-autonomy/03-RESEARCH.md
@.planning/phases/03-permissions-autonomy/03-VALIDATION.md
@.planning/phases/03-permissions-autonomy/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for the pure gate logic (classify + resolve + Tier 4 lock + audit) — gate.test.ts</name>
  <read_first>
    - src/db.test.ts (the `_initTestDatabase()` precedent for any DB-touching test; gate pure functions need no DB)
    - src/security.ts:87-113 (AuditAction union + audit()/setAuditCallback — mock target for the audit-recorded test)
    - src/agent.ts:122-129 (tool-name parsing the classifier mirrors), :108-120 (TOOL_LABELS built-in enumeration)
    - .planning/phases/03-permissions-autonomy/03-RESEARCH.md Patterns 2 and 3 (classifyTier patterns + resolveOutcome matrix), Validation Architecture → Phase Requirements → Test Map
  </read_first>
  <files>src/gate.test.ts</files>
  <action>
    Create src/gate.test.ts importing `{ classifyTier, resolveOutcome, makeCanUseTool, type Tier, type Mode, type GateContext }` from './gate.js' (the module does not exist yet — the import failing is the intended RED state).
    Write describe blocks pinning, with the exact `-t` test names the VALIDATION map specifies:
    - "classify": classifyTier maps money/sign/delete keyword tool names (e.g. `mcp__quickbooks__pay-invoice`, `mcp__docusign__send-for-signature`, `mcp__drive__permanent-delete`) and destructive Bash commands (`rm -rf`, `git push --force`, `drop table`) to Tier 4; read-only Bash (`ls`, `cat`, `git status`) and built-in `Read`/`Grep`/`WebSearch` to Tier 1; `Write`/`Edit` to Tier 2; external send/post MCP names to Tier 3; and any UNKNOWN/unclassified tool name to Tier 3 (the D-03 safe default — never Tier 1/2).
    - "resolveOutcome mode matrix": for each Mode (cautious/balanced/autonomous) assert the tier→(allow|ask) result matches the spec matrix — cautious auto Tier 1 only; balanced auto Tier 1+2; autonomous auto Tier 1+2+3; Tier 4 ask in all three (PERM-01).
    - "override": a per-action `always` override flips a Tier 2/3 capability to allow where the mode default was ask, and an `ask` override forces ask where the mode default was allow; with no override the mode default applies (PERM-02).
    - "tier4 locked": resolveOutcome returns 'ask' for Tier 4 in EVERY mode AND when an `always` override is present for the Tier 4 capability — the lock must ignore both mode and override (PERM-03).
    - "background queue deny": invoking the callback returned by makeCanUseTool with a background GateContext (`attended:false`, no requestInline) on a Tier 3 tool under balanced mode resolves to `{ behavior: 'deny' }` and triggers an enqueue (assert via an injected/mocked enqueue or a spy). (PERM-04)
    - "audit recorded": every gate decision path (allow, queued, inline approve/deny) calls `audit()` exactly once with `action:'permission'` and a `detail` JSON string containing the tool, tier, mode, and outcome — assert by registering a spy via `setAuditCallback` from src/security.js. Assert NO secret/env value appears in detail. (D-10)
    Use vitest `describe`/`it`/`expect`; co-locate per the `src/**/*.test.ts` glob. Do NOT create gate.ts — leave the imports dangling so the file is RED.
  </action>
  <verify>
    <automated>npx vitest run src/gate.test.ts 2>&1 | grep -qiE "cannot find module|failed|error|Cannot find" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/gate.test.ts exists and references classifyTier, resolveOutcome, makeCanUseTool from './gate.js'.
    - Test names match the VALIDATION `-t` filters: "classify", "resolveOutcome mode matrix", "override", "tier4 locked", "background queue deny", "audit recorded".
    - Running the file is RED (module not found / unimplemented) — it is NOT green.
  </acceptance_criteria>
  <done>gate.test.ts runs RED, pinning PERM-01/02/03/04 + D-10 pure-logic + audit behavior.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for config (default-balanced + override round-trip) and queue (transitions + replay-once)</name>
  <read_first>
    - src/db.ts:824 (_initTestDatabase used in beforeAll/beforeEach for DB-backed tests), :3581-3598 (getDashboardSetting/setDashboardSetting the config module wraps), :1561-1604 (routine_runs CRUD shape the queue mirrors)
    - .planning/phases/03-permissions-autonomy/03-RESEARCH.md "approval_queue table" DDL + permissions-config Code Examples; landmine L-3 (replay-once guard)
    - .planning/phases/03-permissions-autonomy/03-PATTERNS.md approval-queue.ts + permissions-config.ts sections
  </read_first>
  <files>src/permissions-config.test.ts, src/approval-queue.test.ts</files>
  <action>
    Create src/permissions-config.test.ts importing `{ getMode, setMode, getOverrides, setOverride }` from './permissions-config.js'. Use `_initTestDatabase()` from src/db.js in beforeEach. Pin, with the VALIDATION `-t` name "default balanced": getMode() returns 'balanced' when `permissions.mode` is unset (D-11). Pin override round-trip: setOverride writes and getOverrides reads back a `{ capability: 'always'|'ask' }` object; malformed stored JSON falls back to `{}` without throwing.
    Create src/approval-queue.test.ts importing `{ enqueueApproval, listPending, approve, deny, expireOlderThan }` from './approval-queue.js'. Use `_initTestDatabase()` in beforeEach. Pin the full state machine: enqueueApproval inserts a `pending` row and returns its id; listPending returns it; approve transitions pending→approved and sets `decided_at` + stores a `result`; deny transitions pending→denied; expireOlderThan flips stale pending rows to expired. Pin the replay-once guard (L-3): a SECOND approve on an already-approved id is a no-op (the underlying `UPDATE ... WHERE status='pending'` changed zero rows) and the function reports it did not act. Assert `tool_input` is stored and retrieved as the captured JSON params (D-08) and that no env/secret material is written.
    Do NOT create the source modules — leave imports dangling (RED).
  </action>
  <verify>
    <automated>npx vitest run src/permissions-config.test.ts src/approval-queue.test.ts 2>&1 | grep -qiE "cannot find module|failed|error|Cannot find" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - permissions-config.test.ts pins default-balanced (name "default balanced") + override round-trip + malformed-JSON fallback.
    - approval-queue.test.ts pins enqueue/list/approve/deny/expire + replay-once (second approve = no-op) + tool_input round-trip.
    - Both files run RED.
  </acceptance_criteria>
  <done>Config + queue contracts pinned RED (PERM-01/02/04 + L-3).</done>
</task>

<task type="auto">
  <name>Task 3: RED contract + wiring tests (dashboard routes + agent gate-wiring)</name>
  <read_first>
    - src/dashboard.contract.test.ts:10-45 (beforeAll _initTestDatabase + get/post helpers appending ?token=TOKEN), :290-303 (the routines contract block to copy the shape from)
    - src/agent.ts:179-189 (runAgent positional signature), :246-282 (the query() options block; :260-262 bypass lines)
    - .planning/phases/03-permissions-autonomy/03-VALIDATION.md Per-Task Verification Map (rows for /api/approvals, /api/permissions, gate wiring)
  </read_first>
  <files>src/agent.test.ts, src/dashboard.contract.test.ts</files>
  <action>
    Extend src/dashboard.contract.test.ts: add `describe('permissions API contract')` pinning GET /api/permissions returns `{ mode, overrides }` (200, behind the existing token gate — a tokenless request is rejected) and PUT /api/permissions with a valid mode persists and returns ok; an invalid mode (not cautious/balanced/autonomous) or invalid override value (not always/ask) returns 400. Add `describe('approvals API contract')` pinning GET /api/approvals returns `{ approvals: [...] }`; POST /api/approvals/:id/approve on a pending item returns ok and a SECOND approve of the same id does NOT replay again (status-guarded, L-3 / T-replay-twice); POST /api/approvals/:id/deny sets denied. Reuse the existing `get`/`post` helpers and `_initTestDatabase()`.
    Create (or extend) src/agent.test.ts with a describe whose test matches `-t "gate wired"`: construct the options object runAgent passes to `query()` (or invoke a small extracted options-builder if plan 03 introduces one) and assert `permissionMode !== 'bypassPermissions'` (it is 'default') AND `allowDangerouslySkipPermissions` is not true AND `canUseTool` is a function when a GateContext is provided. The simplest RED form: import a not-yet-exported `buildAgentQueryOptions`/`makeCanUseTool` and assert the options shape — leave it RED until plan 03 wires it.
    These reference routes/exports that do not exist yet — they run RED.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts src/agent.test.ts 2>&1 | grep -qiE "fail|error|cannot find|not a function|expected" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.contract.test.ts has permissions + approvals describe blocks asserting shapes, auth gate, enum validation, and replay-not-twice.
    - agent.test.ts has a "gate wired" test asserting no bypassPermissions + canUseTool present.
    - Both run RED.
  </acceptance_criteria>
  <done>Route contracts + gate-wiring assertion pinned RED (PERM-01/02/04 + gate-wiring + T-replay-twice).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → tool-call layer | The agent's requested tool call crosses into execution; the gate is the checkpoint. Tests pin that the checkpoint exists and cannot be bypassed. |
| dashboard client → /api routes | Untrusted HTTP crosses into config + approval mutations; tests pin the token gate and enum validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | gate-wiring (agent.test) | mitigate | Test asserts `permissionMode !== 'bypassPermissions'` and `canUseTool` present — a leftover bypass (P-1) fails the suite. |
| T-03-02 | Elevation of Privilege | Tier 4 lock (gate.test "tier4 locked") | mitigate | Test asserts Tier 4 = 'ask' in every mode AND with an 'always' override (PERM-03). |
| T-03-03 | Tampering | replay-once (approval-queue.test + contract) | mitigate | Test asserts a second approve is a no-op (L-3). |
| T-03-04 | Information Disclosure | audit/queue rows (gate.test, approval-queue.test) | mitigate | Tests assert no env/secret value lands in audit detail or tool_input. |
| T-03-SC | Tampering | npm installs | accept | No package installs this plan (vitest already present per RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run src/gate.test.ts src/permissions-config.test.ts src/approval-queue.test.ts src/agent.test.ts src/dashboard.contract.test.ts` runs and every new file/describe is RED (module-not-found or failing assertions), proving the contract is pinned before implementation.
- No source module under src/ named gate.ts / permissions-config.ts / approval-queue.ts is created in this plan.
</verification>

<success_criteria>
All five test files exist, reference the to-be-built modules and routes, use the exact `-t` names from VALIDATION, and run RED. PERM-01..04, D-10, the Tier 4 lock, replay-once, and gate-wiring each have a failing pinning test.
</success_criteria>

<output>
Create `.planning/phases/03-permissions-autonomy/03-01-SUMMARY.md` when done.
</output>
