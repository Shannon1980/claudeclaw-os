---
phase: 03-permissions-autonomy
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/db.ts
  - migrations/v1.2.3/create-approval-queue.ts
  - migrations/version.json
  - src/approval-queue.ts
  - src/agent.ts
  - src/scheduler.ts
  - src/bot.ts
  - src/slack-bot.ts
  - src/orchestrator.ts
  - src/routine-draft.ts
  - src/routine-runner.ts
autonomous: true
requirements: [PERM-04, PERM-01, PERM-02, PERM-03]
must_haves:
  truths:
    - "The agent SDK query() call runs with a canUseTool gate and NO bypassPermissions / allowDangerouslySkipPermissions (the gate actually fires on every run)."
    - "A background Tier 3/4 'ask' inserts a pending approval_queue row and returns deny (turn continues, model reports it queued); approve replays exactly once (status-guarded)."
    - "Scheduler (scheduled + mission) and routine-runner step delegation thread a background GateContext; routine steps carry their stored autonomy into the gate (D-06)."
    - "A missed/omitted gateCtx defaults to a safe background context (ask/queue), never silent-allow (P-5)."
    - "approval_queue exists in BOTH db.ts createSchema and a versioned migration v1.2.3 registered in version.json (dual-write, no crash-loop)."
  artifacts:
    - path: "src/approval-queue.ts"
      provides: "enqueueApproval/listPending/approve/deny/expireOlderThan + status-guarded replay"
      exports: ["enqueueApproval", "listPending", "approve", "deny", "expireOlderThan"]
    - path: "migrations/v1.2.3/create-approval-queue.ts"
      provides: "versioned approval_queue DDL"
      contains: "approval_queue"
    - path: "src/db.ts"
      provides: "approval_queue CREATE TABLE in createSchema + indices"
      contains: "approval_queue"
    - path: "src/agent.ts"
      provides: "canUseTool wiring, bypass removed, trailing gateCtx arg with safe default"
      contains: "canUseTool"
  key_links:
    - from: "src/agent.ts query()"
      to: "src/gate.ts makeCanUseTool"
      via: "canUseTool: makeCanUseTool(gateCtx)"
      pattern: "makeCanUseTool"
    - from: "src/scheduler.ts"
      to: "src/agent.ts runAgent + src/routine-runner.ts runRoutineOnce"
      via: "background GateContext with attended:false + routineAutonomy"
      pattern: "attended"
    - from: "src/gate.ts background path"
      to: "src/approval-queue.ts enqueueApproval"
      via: "enqueue on ask+background"
      pattern: "enqueueApproval"
---

<objective>
Slice B — the queue plus the dangerous wiring. Create the `approval_queue` table (dual-write: db.ts createSchema + versioned migration v1.2.3), the `approval-queue.ts` CRUD/state-machine with the replay-once guard (L-3), then perform the single most important change in the phase: replace `permissionMode:'bypassPermissions'` in `src/agent.ts` with a real `canUseTool` gate and thread a `GateContext` through every caller — scheduler (scheduled + mission), the routine-runner step seam (D-06), and the chat/bot paths — defaulting any omitted context to a safe background (ask/queue) context (P-5).

Purpose: turn the proven policy engine into an enforced boundary on every live run while failing safe-usable. Covers PERM-04 (queue) and activates PERM-01/02/03 on the real path.
Output: `approval_queue` table + migration, `src/approval-queue.ts`, and the gate wired through `agent.ts` + all `runAgent`/`runAgentWithRetry` callers + the routine seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-permissions-autonomy/03-RESEARCH.md
@.planning/phases/03-permissions-autonomy/03-PATTERNS.md
@.planning/phases/03-permissions-autonomy/03-02-SUMMARY.md
@src/gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: approval_queue table (dual-write) + approval-queue.ts CRUD with replay-once guard</name>
  <read_first>
    - src/db.ts:90-112 (routine_steps/routine_runs CREATE TABLE shape to mirror), :322-332 (audit_log block — place approval_queue near it), :824 (_initTestDatabase builds from createSchema), :1561-1604 (saveRoutineRun/getRoutineRuns CRUD shape to copy incl .slice caps)
    - migrations/v1.2.2/add-routine-tables.ts (verbatim migration structure), migrations/version.json (register v1.2.3)
    - .planning/phases/03-permissions-autonomy/03-RESEARCH.md "approval_queue table" DDL; landmine L-3 (status-guarded approve) and L-6 (migration discipline); Pitfall P-4 (migration drift)
    - src/approval-queue.test.ts (GREEN target)
  </read_first>
  <files>src/db.ts, migrations/v1.2.3/create-approval-queue.ts, migrations/version.json, src/approval-queue.ts</files>
  <action>
    Add the `approval_queue` table to src/db.ts `createSchema` using the exact DDL from RESEARCH (columns id, agent_id, chat_id, run_id, routine_id, tool_name, tool_input JSON, tier, mode_at_decision, summary, status default 'pending', decided_at, result, created_at + the two `CREATE INDEX IF NOT EXISTS` lines) so `_initTestDatabase()` builds it (P-4). Create migrations/v1.2.3/create-approval-queue.ts mirroring v1.2.2/add-routine-tables.ts (own better-sqlite3 handle from process.cwd()/store/claudeclaw.db, idempotent CREATE TABLE/INDEX IF NOT EXISTS, try/finally db.close(), exported `description` + `run()`). Register `"v1.2.3": ["create-approval-queue"]` in migrations/version.json (L-6 — skipping crash-loops the live service).
    Create src/approval-queue.ts exporting enqueueApproval (insert pending row, return id; store ONLY captured tool params, never env — L-4/V8; .slice cap long text per the saveRoutineRun precedent), listPending, approve(id, result), deny(id), expireOlderThan(seconds). approve/deny MUST use `UPDATE approval_queue SET status=?, decided_at=?, result=? WHERE id=? AND status='pending'` and report whether `.changes === 1` so a second approve / poll race cannot double-act (L-3). Validate/parse stored tool_input JSON on read; never eval it.
  </action>
  <verify>
    <automated>npx vitest run src/approval-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/approval-queue.test.ts GREEN (enqueue/list/approve/deny/expire + replay-once no-op + tool_input round-trip).
    - approval_queue is present in db.ts createSchema AND migrations/v1.2.3/ AND version.json.
    - approve/deny are status-guarded (WHERE status='pending', act only if .changes===1).
    - npm run typecheck clean.
  </acceptance_criteria>
  <done>Persisted approval queue with a state machine that cannot replay twice; table dual-written so tests and live both have it.</done>
</task>

<task type="auto">
  <name>Task 2: Wire canUseTool into agent.ts (drop bypass) + thread GateContext through all callers and the routine seam</name>
  <read_first>
    - src/agent.ts:179-189 (runAgent positional signature), :246-282 (query options; :260-262 the two bypass lines to remove), :469-505 (runAgentWithRetry forwarding), :196 (requireEnabled chokepoint stays)
    - src/scheduler.ts:256-313 (scheduled-task + routine branch runAgent/runRoutineOnce callers), :440-460 (mission caller) — the three background call sites
    - src/routine-runner.ts:83-130 (runRoutineOnce, execContext.autonomy at :96, deps.delegateToAgent step call at ~:122)
    - src/orchestrator.ts:147,233 (delegateToAgent → runAgentWithRetry — the path routine steps flow through), src/bot.ts:461,1133, src/slack-bot.ts:285, src/routine-draft.ts:160, src/message-core.ts:407 (remaining callers)
    - src/gate.ts makeCanUseTool + GateContext (plan 02); .planning/phases/03-permissions-autonomy/03-RESEARCH.md Pitfalls P-1/P-2/P-5, landmines L-1/L-2/L-4; PATTERNS agent.ts + scheduler.ts sections
    - src/agent.test.ts (the "gate wired" RED test)
  </read_first>
  <files>src/agent.ts, src/scheduler.ts, src/bot.ts, src/slack-bot.ts, src/orchestrator.ts, src/routine-draft.ts, src/routine-runner.ts</files>
  <action>
    In src/agent.ts: remove BOTH `permissionMode:'bypassPermissions'` and `allowDangerouslySkipPermissions:true` (P-1) and set `permissionMode:'default'` + `canUseTool: makeCanUseTool(gateCtx)`. Append a trailing optional `gateCtx?: GateContext` to runAgent (and forward it through runAgentWithRetry); when omitted, default to a SAFE background context `{ attended:false }` (no requestInline) so any missed caller fails to ask/queue, never silent-allow (P-5). Keep `requireEnabled('LLM_SPAWN_ENABLED')` and `getScrubbedSdkEnv` unchanged (L-4 — never copy env into queue rows). The background enqueue path of makeCanUseTool now resolves against the real src/approval-queue.ts enqueueApproval.
    In src/scheduler.ts: the scheduled-task caller (~:260) and mission caller (~:456) pass a background gateCtx `{ attended:false, runId: task.id|mission.id, agentId, chatId }`, no routineAutonomy. The routine branch (~:275-293) builds `{ attended:false, routineId: task.id, routineAutonomy: task.autonomy, agentId, chatId }` and threads it through runRoutineOnce → deps.delegateToAgent so each routine step enters the gate carrying its stored autonomy (D-06, L-1). Stay inside messageQueue.enqueue (L-4 serialization). Background ask = enqueue + immediate deny (P-2 — never block the subprocess for hours).
    In src/routine-runner.ts: thread the gateCtx (or its autonomy) from runRoutineOnce's existing execContext into the delegateToAgent call so steps carry routineAutonomy into the gate.
    In src/orchestrator.ts delegateToAgent + src/bot.ts (×2) + src/slack-bot.ts + src/routine-draft.ts: pass an appropriate gateCtx (chat/bot paths are attended where a live operator is present, but the actual requestInline resolver is wired in plan 04 — for now pass a background-safe gateCtx so nothing silent-allows; mark with a code comment that plan 04 supplies requestInline for the attended chat path). Do NOT introduce module-global gate state — context travels per call (mirrors agentRuntime).
  </action>
  <verify>
    <automated>npx vitest run src/agent.test.ts -t "gate wired" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/agent.test.ts "gate wired" GREEN: permissionMode !== 'bypassPermissions', no allowDangerouslySkipPermissions, canUseTool present.
    - Both bypass lines are gone from agent.ts.
    - All 8 runAgent/runAgentWithRetry call sites compile with a gateCtx (explicit or defaulted-safe) — including the scheduler AOS-cron injected lambda (`runAgent: (prompt, abortController) => runAgent(...)`) whose closure must capture and forward gateCtx, plus scheduler user-task + mission, bot.ts (2), slack-bot.ts, orchestrator.ts, routine-draft.ts; the routine branch passes routineAutonomy.
    - npm run typecheck clean; full `npm test` has no NEW failures vs the wave-2 baseline (inline-chat behavior is plan 04).
  </acceptance_criteria>
  <done>The gate fires on every live run; background asks queue; routine steps carry their autonomy; omitted context fails safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → SDK tool execution | The single query() call site is the chokepoint; canUseTool intercepts before any tool runs on every path (chat/scheduler/mission/routine). |
| dashboard/poll → approval_queue | Approve mutations race the scheduler poll; status-guard prevents double-replay. |
| gate → queue/audit rows | Captured tool params are persisted; must exclude env/secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-bypass-noop | Elevation of Privilege | agent.ts query options | mitigate | Both bypass lines removed; agent.test "gate wired" asserts the gate is active (P-1). |
| T-03-replay-twice | Tampering | approval-queue approve | mitigate | UPDATE ... WHERE status='pending' + .changes===1 guard (L-3). |
| T-03-missed-caller | Elevation of Privilege | runAgent gateCtx default | mitigate | Omitted gateCtx defaults to safe background (ask/queue), never silent-allow (P-5). |
| T-03-block-subprocess | Denial of Service | background gate path | mitigate | Background ask = enqueue + immediate deny; never await operator for a background run (P-2). |
| T-03-env-leak | Information Disclosure | approval_queue.tool_input | mitigate | Store only model-supplied params; keep getScrubbedSdkEnv; no env in rows (L-4/V8). |
| T-03-migration | Denial of Service | migrations/version.json | mitigate | Dual-write + register v1.2.3; run npm run migrate before restart (L-6/P-4). |
| T-03-SC | Tampering | npm installs | accept | No installs; uses installed SDK 0.2.50 + existing better-sqlite3. |
</threat_model>

<verification>
- `npx vitest run src/gate.test.ts src/approval-queue.test.ts src/agent.test.ts` GREEN.
- `npm test` + `npm run typecheck` clean (no new failures vs wave-2 baseline; the inline-chat live behavior is exercised in plan 04 + manual smoke).
- grep confirms `bypassPermissions` and `allowDangerouslySkipPermissions` are absent from src/agent.ts and `makeCanUseTool(` is present.
- migrations/version.json contains `v1.2.3`; `npm run migrate` runs without error against a copy of the store.
</verification>

<success_criteria>
PERM-04 queue persisted with replay-once; PERM-01/02/03 active on the real path because the gate is wired and bypass removed; D-06 satisfied (chat + scheduler + mission + routine steps all gated); fail-safe-usable preserved (omitted context + gate-disabled both route to ask/queue).
</success_criteria>

<output>
Create `.planning/phases/03-permissions-autonomy/03-03-SUMMARY.md` when done.
</output>
