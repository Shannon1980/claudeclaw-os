---
phase: 01-afternoon-win-point-agent-at-workspace
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/workspace-agent-setup.md
autonomous: false
requirements: [WS-04]
must_haves:
  truths:
    - "A setup doc lets the user repoint an agent at any workspace without reading source"
    - "The doc lists the agent.yaml keys, where the agent dir lives, the required restart, and the known headless caveats"
  artifacts:
    - path: "docs/workspace-agent-setup.md"
      provides: "Reproducible workspace-agent setup guide"
      min_lines: 40
      contains: "project_dir"
  key_links:
    - from: "docs/workspace-agent-setup.md"
      to: "~/.claudeclaw/agents/<id>/agent.yaml"
      via: "documents the exact keys and file location"
      pattern: "agent.yaml"
---

<objective>
Document the workspace-agent setup so the user can reproduce it or repoint an agent at ANY workspace without reading source. This is the WS-04 slice — the doc is grounded in the agent created and verified in plan 01-01, so it captures the real keys, the mandatory restart, and the verified headless caveats (hooks fire, deny list applies, secrets scrubbed, path-with-spaces) rather than guesses.

Purpose: A repeatable, source-free recipe for pointing an agent at a workspace.
Output: `docs/workspace-agent-setup.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-afternoon-win-point-agent-at-workspace/01-RESEARCH.md
@.planning/phases/01-afternoon-win-point-agent-at-workspace/01-01-SUMMARY.md
</context>

## Phase Goal

**As a** ClaudeClaw operator, **I want to** read one doc and repoint an agent at any workspace, **so that** I never have to read `agent-config.ts` to reproduce the setup.

<tasks>

<task type="auto">
  <name>Task 1: Write the workspace-agent setup guide</name>
  <read_first>
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-01-SUMMARY.md (the actual aos agent.yaml shape that was created + verified)
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-RESEARCH.md (WS-04 row; Runtime State Inventory; all six Pitfalls; Pattern 1 minimal agent.yaml)
    - .planning/codebase/CONVENTIONS.md ("Agent YAML Schema Conventions" — canonical keys)
    - docs/incident-runbook.md (existing docs/ style/tone reference so the new doc matches the repo's doc voice)
  </read_first>
  <action>
    Write `docs/workspace-agent-setup.md`. Cover, in order: (1) What this does — point a ClaudeClaw agent's cwd at any workspace repo so the SDK auto-loads that repo's CLAUDE.md/AGENTS.md and `.claude/skills` via settingSources. (2) Where the agent lives — `~/.claudeclaw/agents/<id>/` (the external CLAUDECLAW_CONFIG dir), id must match `[a-z0-9_-]`. (3) The agent.yaml keys with one-line meaning each: `name` (required), `description`, `project_dir` (the workspace absolute path — quote it if it contains spaces), optional `slack_channel` (route a channel to the agent) vs delegation-only (`@<id>:` with no token), optional `model`, optional `mcp_servers` (note: only FILTERS servers already defined in `~/.claude/settings.json`, does not define them). (4) Whether to add a CLAUDE.md — recommend NONE or a one-liner so it does not fight the workspace's own CLAUDE.md (note full persona work is a later phase). (5) The mandatory RESTART step — editing agent.yaml has no effect until the bot/agent process restarts (channel map + runtime cache are process-lifetime); give the restart command. (6) How to reach the agent — routed Slack channel or `@<id>:` delegation. (7) Known headless caveats as an explicit list, each one sentence: workspace `.claude` hooks fire under bot turns; the workspace deny list still applies even under bypassPermissions (so `curl`/`rm`/`Read(.env)` are blocked); `*_API_KEY`/`*_TOKEN` are scrubbed from the SDK env, so API/Downloads skills will not work headless yet (use text-only skills for now); paths with spaces are fine for `project_dir`/cwd but a standalone launchd service needs a no-space symlink + `/tmp` log paths; only point at a TRUSTED repo (its CLAUDE.md/skills/hooks run under the bot). (8) A short "repoint to a different workspace" recipe: change `project_dir`, restart, re-verify. Follow CLAUDE.md house style: NO em dashes anywhere, terse, plain prose. Do NOT paste full source files; reference key names and behavior only.
  </action>
  <verify>
    <automated>test -f docs/workspace-agent-setup.md && grep -q 'project_dir' docs/workspace-agent-setup.md && grep -q -i 'restart' docs/workspace-agent-setup.md && ! grep -q '—' docs/workspace-agent-setup.md && echo "doc present, covers project_dir + restart, no em dashes"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/workspace-agent-setup.md` exists and is at least 40 lines.
    - Documents: agent dir location, every agent.yaml key used, the mandatory restart, and the headless caveats (hooks fire, deny list applies, secrets scrubbed, path-with-spaces, trusted-repo-only).
    - Contains no em dash characters (grep for the em dash returns nothing).
    - A reader can repoint an agent at a new workspace from the doc alone, without opening `src/agent-config.ts`.
  </acceptance_criteria>
  <done>The setup doc exists, is grounded in the verified aos agent, and lets the user reproduce or repoint without reading source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the doc is reproducible source-free</name>
  <what-built>
    `docs/workspace-agent-setup.md` — a from-scratch recipe for pointing an agent at a workspace, grounded in the verified aos agent and the documented caveats.
  </what-built>
  <read_first>
    - docs/workspace-agent-setup.md
    - .planning/phases/01-afternoon-win-point-agent-at-workspace/01-VALIDATION.md (Manual-Only Verifications: WS-04 doc review)
  </read_first>
  <how-to-verify>
    1. Read `docs/workspace-agent-setup.md` end to end as if you had never seen the code.
    2. Confirm you could create a new workspace agent (pick id, write agent.yaml with the listed keys, restart, reach it) using ONLY the doc — no need to open `src/agent-config.ts` or any source file.
    3. Confirm the headless caveats section is present and accurate (hooks fire, deny list applies, secrets scrubbed, path-with-spaces, trusted-repo-only).
    4. Confirm there are no em dashes and the tone matches the house style.
  </how-to-verify>
  <acceptance_criteria>
    - You can describe the full repoint procedure using only the doc.
    - All five headless caveats are present and correct.
    - No em dashes; terse house style.
  </acceptance_criteria>
  <resume-signal>Type "approved" or list what the doc is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> workspace choice | The doc instructs which repos are safe to point an agent at |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | EoP / Information Disclosure | a future operator points an agent at an UNTRUSTED repo whose hooks/skills then run under the bot | mitigate | The doc explicitly states: only point at a trusted, user-owned repo; the workspace's CLAUDE.md, skills, and hooks execute under the bot. Inherited deny list + env scrub limit blast radius but do not replace the trust requirement. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (documentation only). |
</threat_model>

<verification>
- `docs/workspace-agent-setup.md` exists, >= 40 lines, contains `project_dir` and a restart step, no em dashes.
- Human review confirms source-free reproducibility and accurate caveats.
</verification>

<success_criteria>
- WS-04: the setup is documented so the user can reproduce or repoint an agent at any workspace without reading source.
</success_criteria>

<output>
Create `.planning/phases/01-afternoon-win-point-agent-at-workspace/01-02-SUMMARY.md` when done.
</output>
