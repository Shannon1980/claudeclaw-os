---
phase: 04-activity-feed
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/activity.test.ts
  - src/undo-executor.test.ts
  - src/dashboard.contract.test.ts
  - web/src/lib/format.ts
  - web/src/lib/format.test.ts
  - src/gemini.ts
autonomous: true
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "Failing unit tests exist pinning TRUST-01 tag derivation, queued-row dedup, and the phrase map"
    - "Failing unit tests exist pinning TRUST-02 undo allowlist, Tier-4 lock-before-dispatch, and never-throw"
    - "formatClock(unixSeconds) renders local clock time like '9:12am'"
    - "Summarize can obtain JSON-wrapped prose from Gemini without mutating the shared generateContent defaults"
  artifacts:
    - path: "src/activity.test.ts"
      provides: "RED tests for deriveTag, dedup, describeAction"
      contains: "deriveTag"
    - path: "src/undo-executor.test.ts"
      provides: "RED tests for undo allowlist + Tier-4 + never-throw"
      contains: "tier 4"
    - path: "web/src/lib/format.ts"
      provides: "formatClock helper"
      contains: "formatClock"
  key_links:
    - from: "src/dashboard.contract.test.ts"
      to: "GET /api/activity, POST /api/activity/:id/undo, POST /api/activity/summarize"
      via: "shape assertions (skipped/todo until Wave 1-3 mount routes)"
      pattern: "/api/activity"
---

<objective>
Wave 0 scaffolding. Close the two real codebase gaps the feed depends on (clock formatter,
JSON-prose Gemini path) and lay down the failing tests that pin TRUST-01 (tag derivation, queued-row
dedup, phrase map) and TRUST-02 (undo allowlist, Tier-4 lock, never-throw), plus contract-test
placeholders for the three new endpoints.

Purpose: Per the Nyquist rule, every later task verifies against a test that already exists. This plan
creates those tests RED and fixes the prerequisite helpers so Waves 1-3 turn them GREEN without
inventing new test infrastructure.
Output: New test files (`src/activity.test.ts`, `src/undo-executor.test.ts`), extended
`src/dashboard.contract.test.ts`, a new `formatClock` in `web/src/lib/format.ts` with a co-located
test, and a non-breaking JSON-prose helper path for Summarize in `src/gemini.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-feed/04-CONTEXT.md
@.planning/phases/04-activity-feed/04-RESEARCH.md
@.planning/phases/04-activity-feed/04-PATTERNS.md

# cwd is the worktree — run `pwd` before file ops. It MUST print:
# /Users/shannongueringer/App Repo/claudeclaw/.claude/worktrees/thirsty-chatelet-f2d10a
</context>

<tasks>

<task type="auto">
  <name>Task 1: formatClock helper + test (UI-SPEC clock gap)</name>
  <files>web/src/lib/format.ts, web/src/lib/format.test.ts</files>
  <read_first>
    - web/src/lib/format.ts (read in full — `formatRelativeTime` at line 3 is the tiny-pure-function analog; match its style)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (Pitfall 5: clock formatter does not exist; do NOT add date-fns/dayjs)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (Typography + Row anatomy: time renders "9:12am", NOT monospace)
  </read_first>
  <action>
    Add an exported pure function `formatClock(unixSeconds: number): string` to web/src/lib/format.ts,
    co-located beside `formatRelativeTime`. It converts an epoch-seconds value to local-timezone clock
    time in the form "9:12am" (lowercase am/pm, no leading zero on the hour, e.g. "9:12am", "12:05pm")
    using `Date` + `toLocaleTimeString` (or manual h12 formatting). No new dependency. Pure, no I/O.
    Then create web/src/lib/format.test.ts asserting: 9:12 local → "9:12am"; a 13:05 local time →
    "1:05pm"; midnight → "12:00am"; noon → "12:00pm". Because the root vitest glob is `src/**/*.test.ts`
    only (web has no vitest harness), this test documents intent and is verified via the web typecheck +
    build gate below; do NOT wire a new web vitest config this phase.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `web/src/lib/format.ts` exports `formatClock` (`grep -n "export function formatClock" web/src/lib/format.ts`).
    - BEHAVIOR: format.test.ts asserts "9:12am" / "1:05pm" / "12:00am" / "12:00pm" cases (lowercase meridiem, no leading-zero hour).
    - SOURCE: no import of `date-fns` or `dayjs` introduced (`grep -rn "date-fns\|dayjs" web/src/lib/format.ts` returns nothing).
    - `cd web && npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>formatClock exists, is pure, returns local "9:12am"-style strings, and web typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: JSON-prose Gemini path for Summarize (no shared-default mutation)</name>
  <files>src/gemini.ts</files>
  <read_first>
    - src/gemini.ts (read in full — `generateContent` at line 22 hardcodes `responseMimeType: 'application/json'` + `temperature: 0.1` at lines 36-37; `parseJsonResponse` at line 55)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (Pitfall 6 + Open Q4: prefer JSON-wrapped prose `{"digest":"..."}` over changing the shared helper)
  </read_first>
  <action>
    Do NOT mutate the existing `generateContent` JSON-mime/temp-0.1 defaults (other callers depend on
    them). Per RESEARCH Pitfall 6 recommendation (a), the Summarize endpoint (Wave 3) will prompt Gemini
    to return `{"digest": "..."}` and parse it with the existing `parseJsonResponse<{digest: string}>()`.
    In THIS task, only confirm the seam is usable: verify `generateContent` and `parseJsonResponse` are
    exported and add a focused unit test (in src/gemini.test.ts if it exists, else create it) that
    `parseJsonResponse<{digest:string}>('{"digest":"hi there"}')` returns `{digest:'hi there'}` and that
    `parseJsonResponse('not json')` returns null. This pins the JSON-prose contract Summarize relies on
    without touching the shared call. If src/gemini.test.ts already covers parseJsonResponse, extend it
    rather than duplicate.
  </action>
  <verify>
    <automated>npx vitest run src/gemini.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `generateContent` defaults unchanged — `grep -n "responseMimeType: 'application/json'" src/gemini.ts` still present; `temperature: 0.1` still present.
    - BEHAVIOR: a test asserts `parseJsonResponse<{digest:string}>('{"digest":"hi there"}')` equals `{digest:'hi there'}` and `parseJsonResponse('not json')` is null.
    - `npx vitest run src/gemini.test.ts` passes.
  </acceptance_criteria>
  <done>The JSON-wrapped-prose seam for Summarize is proven by test; shared generateContent defaults untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests for activity read-side (TRUST-01) and undo executor (TRUST-02) + contract stubs</name>
  <files>src/activity.test.ts, src/undo-executor.test.ts, src/dashboard.contract.test.ts</files>
  <read_first>
    - src/approval-queue.ts (lines 23-43 `ApprovalRow` + `hydrate` JSON-parse; 139-146 `listPending` query shape)
    - src/gate.ts (line 33 `Tier`, 79 `classifyTier`, 135 `summarize`, 169 `encodeDecision` — audit_log.detail is `{tool,tier,mode,outcome,queueId?}`, outcome ∈ allow|queued|approved-inline|denied-inline)
    - src/replay-executor.ts (lines 39-44 `ReplayResult {ok,message}`, 60-75 never-throws/honest-reject dispatch)
    - src/dashboard.contract.test.ts (read head — `_initTestDatabase()` at 19/34, `buildDashboardApp(undefined)` at 30, auth-gate describe at 51 — the pattern for new endpoint shape tests)
    - src/db.ts (332-369 audit_log + approval_queue schema for building fixture rows)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (Validation Architecture → Test Map: the exact `-t` names below come from it)
  </read_first>
  <behavior>
    src/activity.test.ts (imports from a NOT-YET-EXISTING src/activity.ts — these are RED):
    - `-t "deriveTag"`: queue row status 'pending' → 'needs-you'; 'approved' → 'you-approved'; 'denied' → 'skipped'; 'expired' → 'expired'; audit row outcome 'allow' with no queueId → 'ran-on-own'; outcome 'approved-inline' → 'you-approved'; unknown shape → 'ran-on-own' (never hidden, D-05).
    - `-t "dedup"`: a 'queued' audit row carrying queueId=N AND the approval_queue row id=N collapse to ONE canonical row, queue row wins (Pitfall 1). A standalone 'allow' audit row with no queueId is kept.
    - `-t "describeAction"`: a mapped tool+input (e.g. an email/label tool with a recipient count or label name) → a plain phrase containing the safe param; an UNMAPPED tool → honest "Ran {tool}" (never fabricated, never empty); a tool_input with a secret-shaped field is NOT surfaced in the phrase (ASVS V8).
    src/undo-executor.test.ts (imports from NOT-YET-EXISTING src/undo-executor.ts — RED):
    - `-t "tier 4"`: `undoAction(anyTool, 4, input)` returns `{ok:false}` with no MCP dispatch (assert via a spy/mock that the MCP spawn path is never reached — Tier-4 lock BEFORE dispatch, D-09).
    - `-t "allowlist"`: a non-allowlisted tool name returns `{ok:false}` honest message; an allowlisted family name routes to its inverse handler.
    - `-t "never throws"`: `undoAction` with a malformed/empty toolInput resolves to `{ok:false, message}` and does not reject/throw.
    src/dashboard.contract.test.ts additions (mark `.todo`/`.skip` so the suite stays green this wave; Waves 1-3 un-skip):
    - `-t "activity"`: GET /api/activity returns `{ rows: [...] }` where each row has `tag`, `phrase`, `agent_id`, `created_at`, `undoable`.
    - `-t "undo"`: POST /api/activity/:id/undo returns `{ ok, result }` and is gated by token + DASHBOARD_MUTATIONS_ENABLED.
  </behavior>
  <action>
    Create src/activity.test.ts and src/undo-executor.test.ts as RED tests against the not-yet-built
    `src/activity.ts` (exports `deriveTag`, `dedup`/`listActivity`, `describeAction`) and
    `src/undo-executor.ts` (exports `undoAction(toolName, tier, toolInput)`), using the exact `-t` names
    above (they are the per-task verify commands for Waves 1-3 and are listed in RESEARCH's Test Map).
    Build fixtures from the real audit_log/approval_queue column shapes (db.ts:332-369). Use
    `_initTestDatabase()` + direct row inserts (mirror existing contract tests) where a DB is needed; use
    pure-function calls where not. For undo Tier-4 + dispatch-isolation, mock the MCP spawn so the test
    proves no dispatch occurs on Tier 4. Extend src/dashboard.contract.test.ts with the two endpoint
    shape blocks but mark them `it.skip`/`it.todo` so `npm test` is GREEN at end of this wave (the only
    intentionally-RED files are activity.test.ts + undo-executor.test.ts, whose target modules do not
    exist yet — that is expected and is the Wave 0 signal).
  </action>
  <verify>
    <automated>npx vitest run src/activity.test.ts src/undo-executor.test.ts 2>&1 | grep -iE "Cannot find module .*(activity|undo-executor)|FAIL" | head</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `src/activity.test.ts` contains `deriveTag`, `dedup`, and `describeAction` describe/it labels (`grep -c "deriveTag\|dedup\|describeAction" src/activity.test.ts` ≥ 3).
    - SOURCE: `src/undo-executor.test.ts` contains `tier 4`, `allowlist`, `never throws` labels (`grep -c "tier 4\|allowlist\|never throws" src/undo-executor.test.ts` ≥ 3).
    - BEHAVIOR: `npx vitest run src/activity.test.ts src/undo-executor.test.ts` is RED specifically because `src/activity.ts` / `src/undo-executor.ts` do not yet exist (import-resolution failures), not because of malformed tests.
    - BEHAVIOR: `npm test` for already-existing suites stays green — the new contract-test blocks are `it.skip`/`it.todo`, not failing.
    - SOURCE: contract test has skipped/todo blocks naming `/api/activity` and `/api/activity/:id/undo`.
  </acceptance_criteria>
  <done>RED tests pin TRUST-01 and TRUST-02 behavior with the exact `-t` names Waves 1-3 will satisfy; the rest of the suite is green; contract stubs are present but skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → Hono `/api/*` | Untrusted request input crosses here; token + CSRF + mutation kill-switch already gate it app-wide. |
| stored `tool_input` (DB) → rendered phrase | Stored params flow into operator-visible text; secret-shaped fields must not surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ID1 | Information Disclosure | `describeAction` phrase map (tested here) | mitigate | Wave-0 `describeAction` test asserts a secret-shaped field is NOT surfaced in the phrase (ASVS V8). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO new packages (RESEARCH Package Legitimacy Audit: not applicable). No install surface. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit -p tsconfig.json` exits 0 (formatClock typechecks).
- `npx vitest run src/gemini.test.ts` passes (JSON-prose seam).
- `npx vitest run src/activity.test.ts src/undo-executor.test.ts` is RED only on missing target modules.
- `npm test` overall: no NEW failing tests other than the two intentionally-RED target-module imports.
</verification>

<success_criteria>
- formatClock exists and is local-tz "9:12am" style; web typecheck clean.
- JSON-wrapped-prose Summarize seam proven by test; shared generateContent defaults untouched.
- Failing tests for TRUST-01 (deriveTag/dedup/describeAction) and TRUST-02 (tier 4/allowlist/never throws) exist with the exact `-t` names used downstream.
- Contract-test stubs for the three endpoints present and skipped (suite stays green).
</success_criteria>

<output>
Create `.planning/phases/04-activity-feed/04-01-SUMMARY.md` when done.
</output>
