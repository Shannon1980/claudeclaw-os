---
phase: 04-activity-feed
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/undo-executor.ts
  - src/undo-executor.test.ts
  - src/approval-queue.ts
  - src/approval-queue.test.ts
  - src/dashboard.ts
  - src/dashboard.contract.test.ts
  - web/src/pages/Activity.tsx
autonomous: true
requirements: [TRUST-02]
must_haves:
  truths:
    - "An allowlisted reversible action (the floor family: label-remove or draft-delete) can be undone end-to-end from the feed via a real inverse operation (TRUST-02 / D-08)"
    - "A non-allowlisted action returns an honest 'no undo' and renders NO undo affordance, never a dead/no-op button (D-07)"
    - "A Tier 4 (irreversible) action is never undoable, undo-executor refuses tier>=4 before any dispatch and no Undo affordance renders (D-09)"
    - "Undo is status-guarded: a second click/retry does not double-fire the inverse (changes===1) (Tampering mitigation)"
    - "POST /api/activity/:id/undo is mutation-gated (DASHBOARD_MUTATIONS_ENABLED), 400s on a non-integer id, and returns the honest verbatim result on failure (never a generic error)"
    - "Undo never silently no-ops: it performs a real inverse or the affordance is absent (D-08/D-09)"
  artifacts:
    - path: src/undo-executor.ts
      provides: "Allowlisted inverse executor (sibling of replay-executor.ts): draft/meeting/label inverses, honest rejection, Tier 4 refused before dispatch"
      exports: ["undoAction"]
    - path: src/dashboard.ts
      provides: "POST /api/activity/:id/undo route, status-guarded, mutation-gated"
      contains: "/api/activity/:id/undo"
    - path: web/src/pages/Activity.tsx
      provides: "Conditional Undo affordance (only when undoable) + ConfirmModal + honest toast"
      contains: "/undo"
  key_links:
    - from: src/dashboard.ts
      to: src/undo-executor.ts
      via: "undoAction(tool_name, tool_input, tier) in the undo route"
      pattern: "undoAction\\("
    - from: src/undo-executor.ts
      to: src/activity.ts
      via: "reuse isUndoableFamily allowlist predicate (single source of truth)"
      pattern: "isUndoableFamily"
    - from: web/src/pages/Activity.tsx
      to: /api/activity/:id/undo
      via: "apiPost after ConfirmModal confirm"
      pattern: "/api/activity/.*/undo"
---

<objective>
Ship the Undo vertical slice, the one genuinely new write path this phase. Build src/undo-executor.ts as the structural inverse of replay-executor.ts: an allowlist of reversible tool families (drafts/meetings/labels) that maps a captured tool_input to a known safe inverse MCP call, refuses Tier 4 before any dispatch (D-09), and returns an honest "no undo" for everything else (D-07). Mount POST /api/activity/:id/undo (status-guarded, mutation-gated), and render the Undo affordance in Activity.tsx ONLY on genuinely undoable rows (never a dead button). At least ONE family works end-to-end (D-08 floor); the others ship as honest "no undo" if their inverse proves hard.

After this plan, an operator can undo a real reversible action from the feed, and irreversible/non-allowlisted/autonomous rows honestly show no undo.

Purpose: Deliver the trust-making capability, a real, safe inverse of an action, never theater.
Output: src/undo-executor.ts, the status-guarded undo write helper + route, the conditional Undo UI, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-feed/04-RESEARCH.md
@.planning/phases/04-activity-feed/04-PATTERNS.md
@.planning/phases/04-activity-feed/04-UI-SPEC.md
@.planning/phases/04-activity-feed/04-CONTEXT.md
@.planning/phases/04-activity-feed/04-01-SUMMARY.md
@.planning/phases/04-activity-feed/04-02-SUMMARY.md

# All paths resolve under the worktree root:
# /Users/shannongueringer/App Repo/claudeclaw/.claude/worktrees/friendly-euler-b767ea
# Re-check under the worktree root before claiming any file is absent (do not read the main checkout).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: confirm connected MCP inverse tool names, then build undo-executor.ts (failing-first)</name>
  <read_first>
    - src/replay-executor.ts (FULL, the structural template: ReplayResult {ok,message} at ~:39-53, the cap() + MCP_CALL_TIMEOUT_MS, the allowlist dispatch + honest-rejection + never-throws at ~:60-75, and the MCP JSON-RPC stdio handshake replayMcp at ~:125-212 with loadMcpServers() + spawn(cfg.command, cfg.args, {env:{...process.env,...cfg.env}}) + initialize -> notifications/initialized -> tools/call)
    - src/agent.ts (loadMcpServers at ~:34, the source for connected server configs; use it to enumerate which servers/tools are actually connected at execution time)
    - src/activity.ts (the exported isUndoableFamily allowlist predicate from plan 01, REUSE it; do not define a second allowlist)
    - src/gate.ts (classifyTier ~ the tier source; the mcp__server__tool naming convention, hyphenated tool, e.g. mcp__gmail__send-email)
    - src/replay-executor.test.ts (the sibling test file, mirror its allowlist + honest-rejection test shape)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (Assumption A1: exact inverse tool names mcp__gmail__delete_draft / mcp__gcal__delete_event / mcp__gmail__remove_label are [ASSUMED] and MUST be confirmed against connected servers at plan/execution time; D-08 requires only ONE family working end-to-end)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (the undo-executor.ts pattern: mirror replay, swap the tool name, derive inverse arguments from captured tool_input, refuse Tier 4 before dispatch)
  </read_first>
  <behavior>
    - Test: undoAction for an allowlisted floor family (label-remove or draft-delete) with valid tool_input dispatches the inverse and returns {ok:true, message} (mock/stub the MCP stdio layer as replay-executor.test.ts does)
    - Test: undoAction for a non-allowlisted tool returns {ok:false, message:"Undo isn't available for <tool>."}, honest, never a fabricated success
    - Test (tier 4): undoAction(anyTool, anyInput, 4) returns {ok:false} WITHOUT attempting any dispatch (refused before the allowlist), D-09
    - Test: undoAction never throws on a bad/empty tool_input, a thrown error is caught and returned as {ok:false} with an honest reason
    - Test: no em dash in any returned message
  </behavior>
  <action>
    FIRST: at execution time, enumerate the operator's actually-connected MCP servers via loadMcpServers() and (where feasible) their tools/list, to confirm the real inverse tool names. The names in RESEARCH (mcp__gmail__delete_draft, mcp__gcal__delete_event, mcp__gmail__remove_label) are [ASSUMED], use the confirmed names. Pick the cleanest, most idempotent family as the guaranteed floor (likely label-remove or draft-delete). Author src/undo-executor.test.ts failing-first mirroring src/replay-executor.test.ts (stub the MCP stdio handshake). Then create src/undo-executor.ts exporting `undoAction(toolName: string, toolInput: Record<string, unknown>, tier: number): Promise<{ok: boolean; message: string}>`. Copy the replay-executor structure verbatim: the UndoResult shape, cap(), MCP_CALL_TIMEOUT_MS, the never-throws try/catch. Order of guards: (1) refuse tier >= 4 IMMEDIATELY with "This action can't be undone." (D-09) before any allowlist or dispatch; (2) dispatch only families on the shared isUndoableFamily allowlist (imported from src/activity.ts, single source of truth) to their inverse MCP tool, DERIVING the inverse arguments from the captured forward tool_input (e.g. the created draft id, the event id, the message id + label); (3) everything else returns the honest "Undo isn't available for <tool>." For the MCP call, mirror replayMcp exactly: loadMcpServers()[server], honest failure if the server is absent ("Connect <Server> in Settings to undo this."), spawn with {...process.env, ...cfg.env}, the JSON-RPC initialize -> notifications/initialized -> tools/call sequence, bounded by MCP_CALL_TIMEOUT_MS. No eval, no shell string-building, params travel as structured JSON over stdin (T-04-undo-allowlist / injection mitigation). Families whose clean inverse cannot be confirmed this phase ship as honest "no undo" and are logged as deferred follow-ups (D-08), not faked. JSDoc the export; ESM `.js` imports; no em dashes (replace the analog's ', ' with a period).
  </action>
  <verify>
    <automated>npx vitest run src/undo-executor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/undo-executor.test.ts` passes (all behavior cases)
    - `npx vitest run src/undo-executor.test.ts -t "tier 4"` passes (Tier 4 refused before dispatch)
    - src/undo-executor.ts exports undoAction: `grep -n "export async function undoAction\|export function undoAction" src/undo-executor.ts` returns it
    - Tier 4 guard precedes dispatch: the `tier >= 4` refusal appears before any allowlist/spawn in the function body (confirm by reading; the test pins behavior)
    - Reuses the shared allowlist, no second source of truth: `grep -n "isUndoableFamily" src/undo-executor.ts` shows it imported from activity.ts (no inline duplicate allowlist array)
    - No eval / no shell interpolation: `grep -ci "eval(\|exec(\|child_process.exec\b" src/undo-executor.ts` returns 0; MCP uses spawn + JSON-RPC like replay-executor
    - No secret read from the row: `grep -ci "OAUTH\|API_KEY\|process.env\[" src/undo-executor.ts` shows env comes only from loadMcpServers cfg.env spread (mirror replay), not from the queue row
    - No em dash: `grep -cP "\x{2014}" src/undo-executor.ts` returns 0
  </acceptance_criteria>
  <done>undo-executor.ts refuses Tier 4 before dispatch, runs the floor family inverse end-to-end, honestly rejects everything else, never throws, never evals, reuses the shared allowlist; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: status-guarded undo write + POST /api/activity/:id/undo route + contract tests</name>
  <read_first>
    - src/approval-queue.ts (the approve() status-guarded UPDATE at ~:153-165, `WHERE id=? AND status=<expected>` returning info.changes===1; the getApprovalById helper added in plan 01; the `result` column)
    - src/dashboard.ts (the /api/approvals/:id/approve route at ~:3514-3543: the Number.isInteger 400 guard, the status-guarded act-once pattern, returning the verbatim honest result; the mutations kill-switch middleware at ~:367-396)
    - src/undo-executor.ts (the undoAction signature from task 1)
    - src/dashboard.contract.test.ts (the /api/approvals approve/deny describe block to mirror, incl. the replay-not-twice assertion)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (the dashboard.ts undo-route assignment + the status-guarded "act once" shared pattern; the approval-queue undo-write note: reuse the existing result column, no migration)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (A2: store the undo result in the existing approval_queue.result column, NO new migration; undo targets approval_queue rows only, since audit rows carry no tool_input)
  </read_first>
  <action>
    Add a status-guarded undo write to src/approval-queue.ts mirroring approve(): a prepared UPDATE that records the undo result in the existing `result` column WHERE id=? AND status='approved', returning info.changes===1 (this prevents a double-fire; STRIDE Tampering). Use the existing `result` column, NO new column, NO migration (A2). Add a failing-first test in approval-queue.test.ts for the status-guard (a second undo on the same row returns false). Then add `app.post('/api/activity/:id/undo', ...)` to src/dashboard.ts, copying the approve-route shape exactly: parseInt + Number.isInteger -> 400 on a bad id; getApprovalById(id) must exist, be status='approved', undoable (on the allowlist, tier<4, has tool_input), else honest rejection; call undoAction(tool_name, tool_input, tier); on success, the status-guarded write marks the row (the boolean is the source of truth for ok); return {ok, result} with the verbatim honest message on failure (never a generic error). It is a POST, so it inherits the DASHBOARD_MUTATIONS_ENABLED kill-switch automatically, do NOT add bespoke gating. Extend src/dashboard.contract.test.ts with an undo describe block: 400 on a non-integer id; mutation-gated (503 when mutations are off); an undoable approved row undoes once and a SECOND undo does not re-fire (undo-not-twice); a Tier 4 / non-allowlisted row is honestly rejected with no inverse run. No em dashes.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts -t "undo" src/approval-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard.contract.test.ts -t "undo"` passes (400 bad id, mutation-gated, undo-not-twice, Tier4/non-allowlist honest reject)
    - `npx vitest run src/approval-queue.test.ts` passes including the status-guarded undo-write test
    - Route mounted on the existing app: `grep -n "/api/activity/:id/undo" src/dashboard.ts` returns the POST
    - Integer-id guard present: `grep -n "Number.isInteger" src/dashboard.ts` covers the undo route
    - Status-guarded write, changes===1 source of truth: `grep -n "changes === 1\|info.changes" src/approval-queue.ts` shows the undo write returns the boolean
    - No new migration: `git status --porcelain migrations/ | grep -v '^#' | grep -c .` returns 0
    - No bespoke mutation gating added (inherits kill-switch): the undo handler contains no DASHBOARD_MUTATIONS_ENABLED check of its own (confirm by reading)
    - `npx vitest run src/dashboard.contract.test.ts` full file green (no regression)
  </acceptance_criteria>
  <done>POST /api/activity/:id/undo validates the id, runs the inverse via undoAction only for an approved undoable row, records the result status-guarded (no double-fire, no migration), inherits the mutations kill-switch, and returns honest verbatim results; contract + queue tests green.</done>
</task>

<task type="auto">
  <name>Task 3: conditional Undo affordance in Activity.tsx (only when undoable)</name>
  <read_first>
    - web/src/pages/Activity.tsx (the surface from plan 02, add the Undo affordance to the row anatomy)
    - web/src/components/ApprovalItem.tsx (the interaction model to mirror at ~:44-82: useState busy/failure + apiPost + pushToast success/honest-failure, and the destructive ConfirmModal usage)
    - web/src/components/ConfirmModal.tsx (the destructive prop)
    - web/src/lib/toasts.ts (pushToast)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (the Interaction States table, Undo renders ONLY when the row is undoable: approval_queue status='approved' AND on allowlist AND tier<4 AND has tool_input; absent otherwise, NO dead/greyed/no-op button; Tier 4 never; the Undo confirmation/success/failure copy in the Copywriting Contract)
  </read_first>
  <action>
    In web/src/pages/Activity.tsx, render an Undo affordance on a row ONLY when the row's `undoable` flag (computed server-side in plan 01) is true. Otherwise the Undo button is ABSENT, never a disabled or no-op button (D-07/D-08/D-09; UI-SPEC interaction table). Mirror ApprovalItem's interaction model: Undo click -> ConfirmModal (destructive) with body naming the concrete inverse ("This will delete the draft." / "This will remove the label."), confirm label "Undo" -> apiPost('/api/activity/:id/undo') -> on success pushToast "Undone." + the concrete result and update the row; on failure pushToast the honest verbatim reason (never "Something went wrong."; if a server is absent show "Connect Gmail in Settings to undo this."), and show the inline honest-failure line (the ApprovalItem font-mono failure idiom). Do NOT add Review or Summarize here (Review reuse can be folded into the human-verify scope; Summarize is plan 04). Tokens-only, weights 400/500, no em dashes in any copy.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (vite + tsc) succeeds clean
    - Undo posts to the endpoint: `grep -n "/api/activity/.*undo\|/undo" web/src/pages/Activity.tsx` returns the apiPost
    - Undo is conditional on the undoable flag (no dead button): `grep -n "undoable" web/src/pages/Activity.tsx` shows the affordance guarded by the flag
    - Confirmation before the destructive inverse: `grep -n "ConfirmModal" web/src/pages/Activity.tsx` returns its use with the destructive prop
    - Honest failure, no generic error: `grep -ci "Something went wrong" web/src/pages/Activity.tsx` returns 0
    - No em dashes: `grep -cP "\x{2014}" web/src/pages/Activity.tsx` returns 0
  </acceptance_criteria>
  <done>Activity rows render Undo only when genuinely undoable, confirm destructively, run the real inverse via the endpoint, and surface honest success/failure toasts with no dead buttons and no theater; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> POST /api/activity/:id/undo | mutating request crosses the token gate + mutations kill-switch here |
| undo-executor -> MCP server (stdio) | captured tool_input is dispatched as the inverse; allowlist + structured-JSON-only boundary |
| stored tool_input -> inverse arguments | untrusted-at-rest params derived into the inverse call; no eval, no shell building |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-undo-allowlist | Elevation of Privilege | undo-executor.ts dispatch | mitigate | Allowlist-only dispatch (shared isUndoableFamily); everything else returns an honest "no undo" (mirror replay-executor T-03-replay-exec); test pins the rejection |
| T-04-tier4 | Elevation of Privilege | undo-executor.ts tier guard | mitigate | Hard refuse tier>=4 BEFORE any allowlist/dispatch (D-09); `-t "tier 4"` test pins it; UI never renders Undo on Tier 4 rows |
| T-04-undo-doublefire | Tampering | approval-queue undo write + route | mitigate | Status-guarded UPDATE WHERE id=? AND status='approved', act only when changes===1; contract undo-not-twice test pins it |
| T-04-injection | Tampering/EoP | undo-executor MCP call | mitigate | No eval; inverse arguments derived into a structured JSON object sent over stdin (mirror replay-executor.replayMcp); no shell string-building |
| T-04-undo-infodisc | Information Disclosure | undo-executor env handling | mitigate | MCP env comes only from loadMcpServers() cfg.env spread (as live agent), never from the queue row; no secret read from stored params (L-4 / ASVS V8) |
| T-04-undo-auth | Access Control | POST /api/activity/:id/undo | mitigate | Inherits DASHBOARD_MUTATIONS_ENABLED kill-switch + token gate by mounting on `app`; contract test asserts 503 when mutations off (ASVS V4) |
| T-04-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit); slopcheck N/A. Any new dependency requires a blocking-human legitimacy checkpoint first |
</threat_model>

<verification>
- `npx vitest run src/undo-executor.test.ts` green (incl. `-t "tier 4"`)
- `npx vitest run src/dashboard.contract.test.ts -t "undo"` + `src/approval-queue.test.ts` green
- `npx vitest run src/dashboard.contract.test.ts` full file green (no regression)
- `npm run build` (vite + tsc) clean
- No new migration: `git status --porcelain migrations/`
- Manual (plan-04 end-of-phase human-verify): a real end-to-end undo of the floor family against a connected MCP server; Tier 4 + autonomous rows show no Undo
</verification>

<success_criteria>
- At least one reversible family (label-remove or draft-delete) undoes end-to-end via a real inverse (TRUST-02 / D-08)
- Non-allowlisted + Tier 4 + autonomous + inline-approved rows show NO undo, never a dead button (D-07/D-09)
- Undo is status-guarded (no double-fire) and mutation-gated, returning honest verbatim results
- Undo never silently no-ops, real inverse or absent (D-08/D-09)
- No new migration; the shared allowlist is the single source of truth
</success_criteria>

<output>
Create `.planning/phases/04-activity-feed/04-03-SUMMARY.md` when done
</output>
