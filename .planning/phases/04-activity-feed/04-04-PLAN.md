---
phase: 04-activity-feed
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/activity.ts
  - src/dashboard.ts
  - src/dashboard.contract.test.ts
  - web/src/pages/Activity.tsx
autonomous: true
requirements: [TRUST-01]
must_haves:
  truths:
    - "An operator clicks Summarize in the Activity header and gets an LLM daily digest of the day's activity (D-10)"
    - "Summarize is the ONLY LLM call on this surface — operator-invoked, never per-row (D-04)"
    - "When there is no activity (or the LLM is unavailable) Summarize shows an honest empty/error state, not a fabricated digest"
  artifacts:
    - path: "src/dashboard.ts"
      provides: "POST /api/activity/summarize endpoint"
      contains: "summarize"
    - path: "web/src/pages/Activity.tsx"
      provides: "Summarize header CTA + digest panel"
      contains: "Summarize"
  key_links:
    - from: "web/src/pages/Activity.tsx"
      to: "/api/activity/summarize"
      via: "apiPost on header CTA"
      pattern: "api/activity/summarize"
    - from: "src/dashboard.ts"
      to: "generateContent"
      via: "JSON-wrapped-prose one-shot"
      pattern: "generateContent"
---

<objective>
Summarize — the header daily-digest action (D-10). An operator-invoked LLM one-shot that turns a day's
activity rows into a short plain-language digest, reusing the existing Gemini plumbing. The one
acceptable LLM use on this surface; everything else stays deterministic.

Purpose: Completes the Activity surface's "glance at what the team did" promise with an on-demand
natural-language recap, while keeping the per-row feed fully deterministic and LLM-free (D-04).
Output: `POST /api/activity/summarize` (JSON-wrapped prose via the existing generateContent +
parseJsonResponse seam from Wave 0), a Summarize CTA + digest panel in Activity.tsx, and its contract
test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-feed/04-CONTEXT.md
@.planning/phases/04-activity-feed/04-RESEARCH.md
@.planning/phases/04-activity-feed/04-PATTERNS.md
@.planning/phases/04-activity-feed/04-UI-SPEC.md
@.planning/phases/04-activity-feed/04-03-SUMMARY.md

# cwd MUST be the worktree (`pwd` first):
# /Users/shannongueringer/App Repo/claudeclaw/.claude/worktrees/thirsty-chatelet-f2d10a
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/activity/summarize — Gemini one-shot, JSON-wrapped prose, honest empty/error</name>
  <files>src/activity.ts, src/dashboard.ts, src/dashboard.contract.test.ts</files>
  <read_first>
    - src/gemini.ts (22 generateContent — hardcodes JSON mime + temp 0.1; 29 LLM_SPAWN_ENABLED kill switch; 55 parseJsonResponse — the Wave-0-proven JSON-prose seam)
    - src/activity.ts (listActivity — reuse to fetch the day's rows + describeAction phrases to feed the prompt; one source of truth)
    - src/dashboard.ts (3514-3535 mutation route shape; 359-444 token+CSRF+DASHBOARD_MUTATIONS_ENABLED — this POST inherits all three)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (Pitfall 6 + Open Q4: prompt Gemini for `{"digest":"..."}`, parse with parseJsonResponse, do NOT change generateContent; honest empty state when no activity / GOOGLE_API_KEY unset)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (Shared Patterns: LLM one-shot for Summarize)
  </read_first>
  <action>
    Add a helper in src/activity.ts (e.g. `summarizeDay({date?})`) that gathers the day's activity rows
    via listActivity, builds a compact prompt from the deterministic `phrase` + teammate + tag of each row
    (NOT raw tool_input — reuse describeAction output so the digest never surfaces secrets), and instructs
    Gemini to return `{"digest": "<short plain-language recap>"}`. Call `generateContent(prompt)` and
    parse with `parseJsonResponse<{digest:string}>()` (the Wave-0-proven seam) — do NOT mutate
    generateContent's shared defaults. Return `{ digest }` or, when there is no activity, an honest empty
    marker (e.g. `{ digest: null, empty: true }`). Guard for `GOOGLE_API_KEY` unset / `LLM_SPAWN_ENABLED`
    off and surface an honest error rather than throwing. In src/dashboard.ts mount
    `app.post('/api/activity/summarize', async (c) => { ... return c.json(await summarizeDay(...)) })`,
    inheriting token + CSRF + mutation kill-switch (no per-route auth). Un-skip / add the `-t "summarize"`
    block in src/dashboard.contract.test.ts asserting the response shape (`{digest}` on success, honest
    empty when no rows) — mock or short-circuit the LLM call in the test so it does not require a live key.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts -t "summarize" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "api/activity/summarize" src/dashboard.ts` shows the POST route; `grep -n "generateContent\|parseJsonResponse" src/dashboard.ts` or activity.ts shows the JSON-prose seam in use.
    - SOURCE: generateContent's shared defaults remain unchanged (`grep -n "responseMimeType: 'application/json'" src/gemini.ts` still present).
    - SOURCE: the summarize prompt is built from `describeAction`/phrase output, not raw `tool_input` (no JSON.stringify(tool_input) into the prompt).
    - BEHAVIOR: contract test `-t "summarize"` green (LLM mocked/short-circuited), covering success `{digest}` and honest no-activity empty.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>POST /api/activity/summarize returns a JSON-wrapped prose digest via the existing Gemini one-shot, with honest empty/error handling, behind the existing gates.</done>
</task>

<task type="auto">
  <name>Task 2: Summarize CTA + digest panel in Activity.tsx (D-10, focal-point copy)</name>
  <files>web/src/pages/Activity.tsx</files>
  <read_first>
    - web/src/pages/Activity.tsx (the page from Plans 02/03 — add the Summarize action to PageHeader `actions`)
    - web/src/components/PageHeader.tsx (the `actions` slot for the header CTA)
    - web/src/lib/{api,toasts}.ts (apiPost; pushToast)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (Copywriting: "Summarize" CTA, "Summarizing your day…" loading, "Nothing to summarize yet today." empty; Color: Summarize is the focal point when no pending work + accent-colored top-right; no em dashes per CLAUDE.md)
  </read_first>
  <action>
    Add a "Summarize" button into the Activity PageHeader `actions` slot (accent-styled per UI-SPEC — it
    is the surface's focal CTA when no pending work exists). On click, `apiPost('/api/activity/summarize')`,
    show the loading copy "Summarizing your day…", then render the returned `digest` in a header panel /
    inline region. If the response is the honest empty marker, show "Nothing to summarize yet today." On
    error, toast honestly (no fabricated digest). The digest text is plain Inter prose (no monospace).
    Keep all user-facing copy em-dash-free (CLAUDE.md) — use the UI-SPEC strings verbatim except replace
    the ellipsis-only loading string exactly as specified ("Summarizing your day…" is fine; it contains no
    em dash).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: Activity.tsx renders a Summarize control in the PageHeader actions slot (`grep -n "Summarize" web/src/pages/Activity.tsx`).
    - SOURCE: it posts to `/api/activity/summarize` (`grep -n "api/activity/summarize" web/src/pages/Activity.tsx`).
    - SOURCE: empty/loading copy present ("Summarizing your day", "Nothing to summarize yet today.") and no fabricated-digest fallback.
    - SOURCE: no em dash `—` in Activity.tsx (`grep -c "—" web/src/pages/Activity.tsx` returns 0).
    - BEHAVIOR: `cd web && npx tsc --noEmit` exits 0 and `npx vite build` succeeds.
  </acceptance_criteria>
  <done>Operator clicks Summarize and gets a plain-language daily digest, with honest loading/empty/error states and no per-row LLM use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → `POST /api/activity/summarize` | Operator-invoked mutation (LLM call); token + CSRF + DASHBOARD_MUTATIONS_ENABLED gate it app-wide. |
| activity rows → LLM prompt | Deterministic `phrase` output feeds the prompt, NOT raw tool_input — secrets never reach the LLM. |
| LLM output → operator-visible digest | Honest empty/error path; never a fabricated digest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | data sent to the LLM for the digest | mitigate | Prompt built from `describeAction`/phrase output (already secret-filtered), not raw `tool_input`; reuses the V8 allowlist. |
| T-04-11 | Denial of Service / cost | per-row or runaway LLM calls | mitigate | Exactly ONE operator-invoked one-shot (D-04/D-10); LLM_SPAWN_ENABLED kill switch already gates generateContent. |
| T-04-12 | Repudiation | fabricated digest when no/unavailable data | mitigate | Honest empty/error state ("Nothing to summarize yet today.") — never a fabricated digest (D-05 honesty). |
| T-04-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH audit n/a). |
</threat_model>

<verification>
- `npx vitest run src/dashboard.contract.test.ts -t "summarize"` — green (LLM mocked).
- `npm run typecheck` (root) exits 0; `cd web && npx tsc --noEmit && npx vite build` succeeds.
- `npm test` full suite green.
- Manual (end-of-phase human-check): click Summarize → digest renders; with no activity → "Nothing to summarize yet today."
</verification>

<success_criteria>
- D-10 satisfied: operator-invoked daily digest via the existing Gemini one-shot, JSON-wrapped prose.
- Exactly one LLM call on the surface; per-row rendering stays deterministic (D-04).
- Honest empty/error states; no fabricated digest; no secrets in the prompt.
</success_criteria>

<output>
Create `.planning/phases/04-activity-feed/04-04-SUMMARY.md` when done.
</output>
