---
phase: 04-activity-feed
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - src/activity-summary.ts
  - src/activity-summary.test.ts
  - src/dashboard.ts
  - src/dashboard.contract.test.ts
  - web/src/pages/Activity.tsx
autonomous: false
requirements: [TRUST-01]
must_haves:
  truths:
    - "The header Summarize Today action produces an LLM plain-language daily digest of the feed, operator-invoked, not per-row (D-10)"
    - "Summarize reuses the existing Haiku-via-OAuth plumbing (extractViaClaude, scrubbed env), no new API key, no new LLM path (D-10)"
    - "Summarize is governed by the LLM_SPAWN_ENABLED kill-switch and degrades honestly ('Couldn't summarize right now. The feed below is complete.') on failure or when disabled"
    - "POST /api/activity/summarize is mutation-gated and returns text or an honest failure"
    - "The whole surface is verified live: feed unlike Audit, tags legible, undo present only when undoable, Tier 4 never, Home one-click entry reaches /activity"
  artifacts:
    - path: src/activity-summary.ts
      provides: "summarizeDay(rows): plain-text digest prompt + extractViaClaude call + honest degrade"
      exports: ["summarizeDay"]
    - path: src/dashboard.ts
      provides: "POST /api/activity/summarize route, mutation-gated, LLM_SPAWN_ENABLED-aware"
      contains: "/api/activity/summarize"
    - path: web/src/pages/Activity.tsx
      provides: "Summarize Today header action + in-progress + result/honest-failure rendering"
      contains: "Summarize"
  key_links:
    - from: src/dashboard.ts
      to: src/activity-summary.ts
      via: "summarizeDay in the summarize route"
      pattern: "summarizeDay\\("
    - from: src/activity-summary.ts
      to: src/memory-ingest.ts
      via: "extractViaClaude (Haiku/OAuth, scrubbed env)"
      pattern: "extractViaClaude"
    - from: web/src/pages/Activity.tsx
      to: /api/activity/summarize
      via: "apiPost from the Summarize Today action"
      pattern: "/api/activity/summarize"
---

<objective>
Ship the Summarize daily-digest slice (D-10) and verify the whole Activity surface live. Build src/activity-summary.ts reusing the existing Haiku-via-OAuth path (extractViaClaude, no new API key, scrubbed env, bounded timeout), mount a mutation-gated, LLM_SPAWN_ENABLED-aware POST /api/activity/summarize, and add the "Summarize Today" header action to Activity.tsx. Then a blocking human-verify checkpoint covers everything the automated suite cannot reach: the surface looks unlike Audit, tags are legible, Undo appears only on undoable rows (and never on Tier 4 / autonomous), a real floor-family undo works end-to-end, and the Home one-click entry reaches /activity.

This is the final polish slice plus the end-of-phase verification gate.

Purpose: Add the one acceptable on-demand LLM affordance and prove the phase goal holds in the running app.
Output: src/activity-summary.ts, the summarize route + UI action, contract tests, and an operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-feed/04-RESEARCH.md
@.planning/phases/04-activity-feed/04-PATTERNS.md
@.planning/phases/04-activity-feed/04-UI-SPEC.md
@.planning/phases/04-activity-feed/04-CONTEXT.md
@.planning/phases/04-activity-feed/04-02-SUMMARY.md
@.planning/phases/04-activity-feed/04-03-SUMMARY.md

# All paths resolve under the worktree root:
# /Users/shannongueringer/App Repo/claudeclaw/.claude/worktrees/friendly-euler-b767ea
# Re-check under the worktree root before claiming any file is absent (do not read the main checkout).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: summarizeDay digest module + mutation-gated POST /api/activity/summarize + contract tests</name>
  <read_first>
    - src/memory-ingest.ts (extractViaClaude at ~:39-85, the Haiku-via-OAuth one-shot: getScrubbedSdkEnv(readEnvFile(...)), query({ model:'claude-haiku-4-5-20251001', allowedTools:[], disallowedTools:['*'], maxTurns:1 }), bounded timeout, returns result text. REUSE this directly)
    - src/activity.ts (buildActivityFeed, the rows the digest summarizes)
    - src/gemini.ts (the ALTERNATIVE LLM path, do NOT use it; it needs GOOGLE_API_KEY and is 429-prone per RESEARCH)
    - src/dashboard.ts (the mutations kill-switch middleware at ~:367-396; how LLM_SPAWN_ENABLED is read elsewhere; the route-mount pattern)
    - src/dashboard.contract.test.ts (the harness + an existing POST describe block to mirror)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (the activity-summary.ts pattern + the scrubbed-env LLM shared pattern; the honest-degrade copy)
    - .planning/phases/04-activity-feed/04-RESEARCH.md (A4: extractViaClaude is the intended D-10 plumbing; the Security Domain LLM cost/abuse mitigation: LLM_SPAWN_ENABLED + operator-invoked + bounded timeout + scrubbed env)
  </read_first>
  <behavior>
    - Test: summarizeDay(rows) builds a plain-text prompt and returns the LLM text (stub/mock extractViaClaude as memory-ingest tests do)
    - Test: on extractViaClaude failure/timeout, summarizeDay returns the honest degrade string "Couldn't summarize right now. The feed below is complete." (never throws to the caller)
    - Test: the prompt assembled from rows contains no em dash and no secret/env field (only the plain phrases + teammate + time)
    - Test (contract): POST /api/activity/summarize returns text or an honest failure; when LLM_SPAWN_ENABLED is off it returns the honest disabled/degrade response and makes NO LLM call; it is mutation-gated (503 when mutations off)
  </behavior>
  <action>
    Author src/activity-summary.test.ts failing-first (mock extractViaClaude). Create src/activity-summary.ts exporting `summarizeDay(rows): Promise<string>` that assembles a plain-text prompt from the feed rows (the plain phrases + teammate + time only, NEVER raw params/env/secrets; ASVS V8) asking for a 3-4 sentence plain-language daily summary with NO em dashes, and calls extractViaClaude(prompt, 20_000) (Haiku/OAuth, scrubbed env, reuse directly, do NOT use the Gemini path). On any failure/timeout return the honest degrade "Couldn't summarize right now. The feed below is complete." (catch internally, never throw). Then add `app.post('/api/activity/summarize', ...)` to src/dashboard.ts: gate on the LLM_SPAWN_ENABLED kill-switch (if off, return the honest disabled response and make NO LLM call); build today's rows via buildActivityFeed; call summarizeDay; return the text. It is a POST so it inherits DASHBOARD_MUTATIONS_ENABLED automatically, do NOT add bespoke mutation gating. Extend src/dashboard.contract.test.ts with a summarize describe block (text-or-honest-failure; LLM_SPAWN_ENABLED off short-circuits with no LLM call; mutation-gated 503 when mutations off). No em dashes; JSDoc the export; ESM `.js` imports.
  </action>
  <verify>
    <automated>npx vitest run src/activity-summary.test.ts src/dashboard.contract.test.ts -t "summarize"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/activity-summary.test.ts` passes (digest, honest degrade, no-secret prompt)
    - `npx vitest run src/dashboard.contract.test.ts -t "summarize"` passes (text-or-honest-failure, LLM_SPAWN_ENABLED short-circuit, mutation-gated)
    - Reuses Haiku/OAuth, not Gemini: `grep -n "extractViaClaude" src/activity-summary.ts` returns the call; `grep -ci "generateContent\|GOOGLE_API_KEY\|gemini" src/activity-summary.ts` returns 0
    - Kill-switch honored: `grep -n "LLM_SPAWN_ENABLED" src/dashboard.ts` covers the summarize route and short-circuits before any LLM call
    - No secret/env in the prompt: `grep -ci "process.env\|OAUTH\|API_KEY\|tool_input" src/activity-summary.ts` returns 0 (rows pass only phrase/teammate/time)
    - Route mounted on the existing app, no bespoke mutation gate: `grep -n "/api/activity/summarize" src/dashboard.ts` returns the POST
    - No em dashes: `grep -cP "\x{2014}" src/activity-summary.ts` returns 0
    - `npx vitest run src/dashboard.contract.test.ts` full file green
  </acceptance_criteria>
  <done>summarizeDay produces an honest Haiku/OAuth digest (or honest degrade), the route is mutation-gated and LLM_SPAWN_ENABLED-aware with no LLM call when disabled, no secrets in the prompt; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Summarize Today header action in Activity.tsx</name>
  <read_first>
    - web/src/pages/Activity.tsx (the PageHeader actions slot left empty in plan 02, now render the Summarize Today button there)
    - web/src/components/ApprovalItem.tsx (the accent-filled primary button styling to mirror for Summarize, and the apiPost + busy-state interaction model)
    - web/src/components/PageHeader.tsx (the actions slot)
    - web/src/lib/toasts.ts (pushToast for honest failure)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (Summarize Today is the filled-accent header action; copy: "Summarize Today" / in-progress "Summarizing…" / result is a short plain paragraph / failure "Couldn't summarize right now. The feed below is complete." Render the result inline near the header, not as a per-row element)
  </read_first>
  <action>
    In web/src/pages/Activity.tsx, render a "Summarize Today" action in the PageHeader actions slot (filled accent, mirroring ApprovalItem's Approve button styling). On click: show "Summarizing…" busy state, apiPost('/api/activity/summarize'), then render the returned digest as a short plain-language paragraph in a quiet panel near the header (tokens-only, weight 400). On failure or a disabled kill-switch, show the honest "Couldn't summarize right now. The feed below is complete." (never a generic error). Operator-invoked only, no auto-run on mount, no per-row summarize. Tokens-only, weights 400/500, no em dashes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (vite + tsc) succeeds clean
    - Summarize posts to the endpoint: `grep -n "/api/activity/summarize" web/src/pages/Activity.tsx` returns the apiPost
    - Operator-invoked, not on mount: the summarize call is inside a click handler, not a useEffect (confirm by reading)
    - Honest failure copy, no generic error: `grep -n "The feed below is complete" web/src/pages/Activity.tsx` returns the degrade string; `grep -ci "Something went wrong" web/src/pages/Activity.tsx` returns 0
    - No em dashes: `grep -cP "\x{2014}" web/src/pages/Activity.tsx` returns 0
  </acceptance_criteria>
  <done>The Summarize Today header action runs the operator-invoked digest, renders the result or honest degrade, and never auto-runs or summarizes per row; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live human verification of the Activity surface (end-of-phase gate)</name>
  <action>Operator-only live verification gate. No code is written in this task. The executor pauses and the operator exercises the running app per the steps below, then types "approved" or reports issues for gap closure.</action>
  <what-built>
    The full Activity surface: a reverse-chronological, plain-language, teammate-attributed feed at /activity with Ran on its own / You approved / Needs you tags and honest denied/expired states; filter chips (All / Ran on its own / Needs you / per-teammate); a conditional Undo (real inverse for the floor family, absent on non-allowlisted / Tier 4 / autonomous rows); a Summarize Today daily digest; resolved nav (one Activity item to the new surface, /audit labeled Audit); and a one-click Home entry point.
  </what-built>
  <how-to-verify>
    Run the app from the live checkout (the worktree symlinks node_modules/.env/store from main; if a migration were added run `npm run migrate` first. This phase adds none, so no migrate is needed). Then:
    1. Open the sidebar: confirm exactly ONE "Activity" item, and the old technical view is now labeled "Audit". Open both and confirm /activity looks deliberately UNLIKE /audit (card/list, not a dense monospace table).
    2. From Home: click the one-click "What your team did" entry point; confirm it lands on /activity (D-03).
    3. On /activity: confirm rows are reverse-chronological, grouped by day, each with a teammate color dot + name, a plain-language phrase, and a legible tag. Confirm a "Ran on its own" autonomous row, a "You approved" row, and (if present) a "Needs you" / Skipped / Denied / Expired row each read honestly (D-05/D-06).
    4. Filters: click All / Ran on its own / Needs you / a teammate chip; confirm the feed filters correctly (D-11).
    5. Undo (TRUST-02 / D-08/D-09): perform an allowlisted floor-family action (the one confirmed working, e.g. apply a label or create a draft), find it in the feed, click Undo, confirm the destructive confirm, and confirm the REAL inverse occurred (the label is gone / the draft is deleted) with an honest success toast. Confirm a Tier 4 row and an autonomous "Ran on its own" row show NO Undo affordance at all (not a dead button).
    6. Summarize Today (D-10): click it; confirm a plain-language paragraph digest appears (or, if LLM_SPAWN_ENABLED is off or it fails, the honest "Couldn't summarize right now. The feed below is complete." with no fabrication). Confirm no em dashes anywhere in the surface copy.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any issue (e.g. a dead Undo button, a fabricated phrase, a doubled feed row, a second "Activity" nav item) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> POST /api/activity/summarize | mutating LLM-spawning request crosses the token gate + mutations kill-switch + LLM_SPAWN_ENABLED here |
| feed rows -> LLM prompt | only plain phrase/teammate/time may enter the prompt; never raw params/env/secrets |
| activity-summary -> Anthropic API | the one outbound LLM call; scrubbed env, bounded, operator-invoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-llm-dos | Denial of Service | POST /api/activity/summarize | mitigate | LLM_SPAWN_ENABLED kill-switch short-circuits with no LLM call; operator-invoked only (no per-row, no on-mount); bounded 20s timeout; inherits mutations kill-switch (RESEARCH Security Domain) |
| T-04-summarize-infodisc | Information Disclosure | summarizeDay prompt + extractViaClaude env | mitigate | Prompt carries only plain phrase/teammate/time (no raw params/env/secrets); extractViaClaude uses getScrubbedSdkEnv, no secret echo (ASVS V8); grep gate asserts no env/tool_input in the module |
| T-04-summarize-auth | Access Control | POST /api/activity/summarize | mitigate | Inherits DASHBOARD_MUTATIONS_ENABLED + token gate by mounting on `app`; contract test asserts 503 when mutations off (ASVS V4) |
| T-04-summarize-fabrication | Integrity (honesty) | Summarize result rendering | mitigate | On failure/disabled, render the honest degrade ("Couldn't summarize right now. The feed below is complete."), never a fabricated summary or generic error (D-05) |
| T-04-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit); slopcheck N/A. Any new dependency requires a blocking-human legitimacy checkpoint first |
</threat_model>

<verification>
- `npx vitest run src/activity-summary.test.ts` green
- `npx vitest run src/dashboard.contract.test.ts -t "summarize"` green
- `npm test` full suite green AND `npm run build` (vite + tsc) clean before the checkpoint
- Human-verify checkpoint approved (the manual-only items from VALIDATION: surface unlike Audit, undo present only when undoable, Tier 4 never, Home one-click entry, real end-to-end floor-family undo)
</verification>

<success_criteria>
- The Summarize Today action produces an operator-invoked Haiku/OAuth daily digest (or honest degrade), governed by LLM_SPAWN_ENABLED, with no secrets in the prompt (D-10)
- The full surface is operator-verified live: feed unlike Audit, tags legible, undo honest (real inverse or absent, Tier 4 never), Home one-click entry works
- TRUST-01 (feed + tags) and TRUST-02 (undo) are usable end to end
- `npm test` green, `npm run build` clean, no new migration, no em dashes anywhere
</success_criteria>

<output>
Create `.planning/phases/04-activity-feed/04-04-SUMMARY.md` when done
</output>
