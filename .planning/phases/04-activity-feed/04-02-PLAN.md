---
phase: 04-activity-feed
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/dashboard.ts
  - src/dashboard.contract.test.ts
  - web/src/pages/Activity.tsx
  - web/src/lib/routes.ts
  - web/src/lib/vocabulary.ts
  - web/src/App.tsx
  - web/src/pages/Home.tsx
autonomous: true
requirements: [TRUST-01]
must_haves:
  truths:
    - "Visiting /activity shows a reverse-chronological, plain-language feed of what the team did, attributed by teammate (color dot + name), grouped by day (TRUST-01 / SC1)"
    - "Each row carries a tag: Ran on its own (neutral) / You approved (green) / Needs you (amber); denied/expired surface as their own honest state (TRUST-01 / SC2, D-06)"
    - "Filter chips All / Ran on its own / Needs you / per-teammate filter the feed read-side (D-11)"
    - "GET /api/activity is token-gated (inherited from the app mount) and returns the curated rows (TRUST-01)"
    - "The sidebar Activity nav item routes to the NEW /activity surface; the old /audit route now carries its own nav.audit label (D-01/D-02)"
    - "A one-click affordance on Home navigates to /activity (D-03)"
    - "Activity looks deliberately unlike Audit, card/list visual language, no dense monospace table (D-01)"
  artifacts:
    - path: src/dashboard.ts
      provides: "GET /api/activity route mounted on the existing token-gated app"
      contains: "/api/activity"
    - path: web/src/pages/Activity.tsx
      provides: "Operator Activity surface: PageHeader + filter chips + day-grouped card rows"
      min_lines: 60
    - path: web/src/lib/routes.ts
      provides: "Re-pointed nav.activity onto /activity; /audit gets nav.audit"
      contains: "/activity"
    - path: web/src/lib/vocabulary.ts
      provides: "New nav.audit term; nav.activity/page.activity point at the new surface"
      contains: "nav.audit"
  key_links:
    - from: web/src/pages/Activity.tsx
      to: /api/activity
      via: "fetch in useEffect"
      pattern: "/api/activity"
    - from: src/dashboard.ts
      to: src/activity.ts
      via: "buildActivityFeed in the GET /api/activity handler"
      pattern: "buildActivityFeed"
    - from: web/src/App.tsx
      to: web/src/pages/Activity.tsx
      via: "wouter Route path=/activity"
      pattern: "path=\"/activity\""
---

<objective>
Ship the first end-to-end vertical slice: an operator can open /activity and SEE the feed. Mount a token-gated `GET /api/activity` over the plan-01 read model, build the new Activity.tsx surface (card/list visual, deliberately unlike Audit), wire the filter chips (D-11), resolve the nav/route/vocab collision (D-01/D-02), and add a one-click Home entry point (D-03). After this plan, the read path is proven on screen: feed renders, tags show, teammate attribution shows, filters work.

This is the MVP proof-of-path slice, UI -> API -> DB read working end to end. Undo and Summarize are absent here (later slices); their affordances are NOT rendered yet.

Purpose: Make the feed real and visible, the transparency surface the operator actually looks at.
Output: GET /api/activity, Activity.tsx, route/vocab/App wiring, the Home entry point, and extended contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-feed/04-RESEARCH.md
@.planning/phases/04-activity-feed/04-PATTERNS.md
@.planning/phases/04-activity-feed/04-UI-SPEC.md
@.planning/phases/04-activity-feed/04-CONTEXT.md
@.planning/phases/04-activity-feed/04-01-SUMMARY.md

# All paths resolve under the worktree root:
# /Users/shannongueringer/App Repo/claudeclaw/.claude/worktrees/friendly-euler-b767ea
# Re-check under the worktree root before claiming any file is absent (do not read the main checkout).
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/activity route + contract tests</name>
  <read_first>
    - src/dashboard.ts (the token gate at ~:342-356, the mutations kill-switch middleware at ~:367-396, and the /api/approvals + /api/audit block at ~:3505-3557, the exact route-mount pattern and the approvalView projector at ~:3490-3503 showing the "no raw secrets in the response" rule)
    - src/activity.ts (buildActivityFeed signature + the row shape from plan 01)
    - src/dashboard.contract.test.ts (the `buildDashboardApp(undefined)` fixture + TOKEN='test-contract-token' + `_initTestDatabase()` beforeEach at ~:16-34, and an existing /api/approvals describe block to mirror)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (the dashboard.ts MODIFY pattern: mount on the same app, inherit token + kill-switch, do NOT add bespoke auth)
  </read_first>
  <action>
    Add `app.get('/api/activity', ...)` next to the existing /api/approvals + /api/audit routes in src/dashboard.ts. Read the optional `filter` (all | autonomous | needsyou | <agent_id>) and `limit` query params (parse limit with a sane default like 100), call `buildActivityFeed({ filter, limit })` from src/activity.ts, return `c.json({ rows })`. It is a GET, so it inherits the query-token gate automatically by mounting on the existing `app`, do NOT add bespoke auth. The response carries only the curated row fields from plan 01 (no raw secrets, mirror the approvalView projector rule). Then extend src/dashboard.contract.test.ts with a new `describe` block (reuse the existing `app`/`TOKEN` fixtures): assert GET /api/activity without a token is rejected (token gate), with the token returns 200 and a `rows` array, and that a seeded queued + allowed pair returns the expected tagged/deduped shape. No em dashes in any new copy.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard.contract.test.ts -t "activity"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard.contract.test.ts -t "activity"` passes (token-gated shape + curated rows)
    - `grep -n "/api/activity" src/dashboard.ts` shows the GET route mounted on the existing `app`
    - GET handler calls the plan-01 read model: `grep -n "buildActivityFeed" src/dashboard.ts` returns the call
    - No bespoke auth added on the route: the new block contains no token-checking code (it inherits the gate), confirm by reading the handler body
    - `npx vitest run src/dashboard.contract.test.ts` full file still green (no regression to the existing approvals/permissions blocks)
  </acceptance_criteria>
  <done>GET /api/activity returns the curated, token-gated feed; contract tests green; no bespoke auth, no secret fields in the response.</done>
</task>

<task type="auto">
  <name>Task 2: Activity.tsx surface + filter chips (card/list, unlike Audit)</name>
  <read_first>
    - web/src/pages/Audit.tsx (copy ONLY the data-page shell: useState/useEffect load, PageHeader + Tab tabs, PageState loading/error/empty at ~:64-90. ANTI-PATTERN: do NOT copy the dense monospace <table> body at ~:94-127, Activity must look unlike Audit per D-01)
    - web/src/components/ApprovalItem.tsx (the card row visual to echo at ~:84-123: card on var(--color-elevated), border-[var(--color-border)] rounded-md p-3, phrase at 12.5px line-clamp-2, meta line with AgentAvatar size 16 + formatRelativeTime, Pill tag right-aligned, the font-mono failure line idiom at ~:102-104)
    - web/src/components/Pill.tsx (the StatusDot pattern at ~:44-49 for the teammate dot, and the tone prop for tags: done=green, neutral=grey, medium=amber, failed=red)
    - web/src/lib/teammate.ts (teammateColor(id) at ~:8 for the dot + per-teammate chip color)
    - web/src/lib/format.ts (formatRelativeTime for the row "when")
    - web/src/components/PageHeader.tsx (the title + subtitle + actions slot + Tab filter-chip tabs)
    - web/src/components/PageState.tsx (loading/error/empty states)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (the FULL layout contract, per-row anatomy, interaction-state table, color/typography/copywriting contracts, load-bearing)
  </read_first>
  <action>
    Create web/src/pages/Activity.tsx. Fetch GET /api/activity (the existing api helper, query-token included) in useEffect; render PageState loading/error/empty (empty copy "Nothing yet today" + the UI-SPEC body). Use PageHeader with title term('page.activity') and subtitle "What your team did" (11px, var(--color-text-muted)); leave the actions slot empty for now (Summarize lands in plan 04, do NOT render a Summarize button yet). Filter chips (D-11) in the PageHeader tabs slot via Tab: All / Ran on its own / Needs you / one chip per teammate (label = teammate name with a leading teammateColor dot; teammate set derived from the rows' agent_id). Filtering is read-side: pass the active filter to the GET (filter=all|autonomous|needsyou|<agent_id>) or filter the loaded rows client-side, either is fine, keep it read-side. Render the feed reverse-chronological, grouped by day with a quiet section-label header (TODAY / YESTERDAY / "MON, JUN 23", local timezone). Each row echoes ApprovalItem's card anatomy: 6px teammateColor dot, the plain-language phrase (12.5px/400, line-clamp-2), a meta line (AgentAvatar 16px + "Teammate · 9:12am", 11px muted), and the right-aligned Pill tag with the D-06 tone (You approved=done/green, Ran on its own=neutral/grey, Needs you=medium/amber, Denied=failed/red, Expired=neutral/faint). Render a View affordance per row (shows tool + available params/outcome honestly; for audit allow/inline rows show exactly the {tool,tier,mode,outcome} that exists and say so, D-05). Do NOT render Undo or Review yet (later plans). Tokens-only styling (var(--color-*)), weights 400/500 only, Inter, NO monospace except the inherited font-mono honest-failure line. No em dashes anywhere in copy.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (vite + tsc) succeeds clean
    - Activity.tsx fetches the feed: `grep -n "/api/activity" web/src/pages/Activity.tsx` returns the fetch
    - Card/list visual, not a monospace table: `grep -ci "font-mono\|<table" web/src/pages/Activity.tsx` returns 0 or only the single inherited ApprovalItem-style failure line (no <table>)
    - Reuses the shared primitives: `grep -n "Pill\|teammateColor\|formatRelativeTime\|AgentAvatar\|PageState\|PageHeader" web/src/pages/Activity.tsx` shows them imported/used
    - No Summarize/Undo/Review affordance rendered yet: `grep -ci "Summarize\|Undo\|/undo\|/approve" web/src/pages/Activity.tsx` returns 0
    - No em dashes in copy: `grep -cP "\x{2014}" web/src/pages/Activity.tsx` returns 0
    - Tokens-only color (no raw hex except via teammateColor): `grep -nE "#[0-9a-fA-F]{6}" web/src/pages/Activity.tsx` returns nothing (teammate color comes from teammateColor(), not literals)
  </acceptance_criteria>
  <done>Activity.tsx renders the reverse-chron, day-grouped, teammate-attributed feed with D-06 tags and D-11 filter chips, in card/list language unlike Audit; build clean; no Undo/Summarize yet.</done>
</task>

<task type="auto">
  <name>Task 3: resolve nav/route/vocab collision + wire the route + Home entry point (D-01/D-02/D-03)</name>
  <read_first>
    - web/src/lib/routes.ts (the route table; the collision is concrete at line 37: `{ path: '/audit', label: 'Audit', vocabKey: 'nav.activity', section: 'intelligence', icon: ShieldCheck }`. The `Activity` icon is already imported at line 3)
    - web/src/lib/vocabulary.ts (nav.activity at ~:56 operator:'Activity' builder:'Audit'; page.activity at ~:70, where a new nav.audit pair lands)
    - web/src/App.tsx (the wouter <Switch> block at ~:48-62, the existing <Route path="/audit"><Audit/></Route> to add a sibling beside)
    - web/src/pages/Home.tsx (the NeedsYouCard region at ~:166-177 and the existing navigate('/...') usage at ~:162, where the one-click Activity entry point lands)
    - .planning/phases/04-activity-feed/04-PATTERNS.md (the routes.ts / vocabulary.ts / App.tsx / Home.tsx MODIFY assignments)
    - .planning/phases/04-activity-feed/04-UI-SPEC.md (the Home entry-point recommendation: a quiet "What your team did" link/row beneath NeedsYou, var(--color-text-muted) with accent hover, NOT a second loud card)
  </read_first>
  <action>
    In web/src/lib/routes.ts: add a NEW route row for `/activity` (label 'Activity', vocabKey 'nav.activity', section 'intelligence', icon Activity, already imported) and re-point the existing `/audit` row's vocabKey from 'nav.activity' to a new 'nav.audit'. Keep both in the intelligence section. In web/src/lib/vocabulary.ts: add a `'nav.audit': { operator: 'Audit', builder: 'Audit' }` term (and optionally 'page.audit'); keep nav.activity/page.activity operator label "Activity" now pointing at the new surface. In web/src/App.tsx: add `<Route path="/activity"><Activity /></Route>` beside the existing `/audit` route and import Activity from './pages/Activity'. In web/src/pages/Home.tsx: add a one-click entry point beneath the NeedsYouCard region, an understated "What your team did" text+chevron link (var(--color-text-muted), accent on hover, NOT a second loud card) that calls navigate('/activity'). After this, the sidebar shows ONE "Activity" item (the new surface) and the old technical view is labeled "Audit". Tokens-only, weights 400/500, no em dashes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds clean
    - A /activity route exists and carries nav.activity: `grep -n "/activity" web/src/lib/routes.ts` shows the new row with vocabKey 'nav.activity'
    - The /audit row no longer uses nav.activity: `grep -n "/audit" web/src/lib/routes.ts` shows vocabKey 'nav.audit'; `grep -c "vocabKey: 'nav.activity'" web/src/lib/routes.ts` returns exactly 1 (only the /activity row)
    - nav.audit term exists: `grep -n "nav.audit" web/src/lib/vocabulary.ts` returns the new pair
    - The route is wired: `grep -n "path=\"/activity\"" web/src/App.tsx` returns the Route and Activity is imported
    - Home links to the feed: `grep -n "navigate('/activity')\|/activity" web/src/pages/Home.tsx` returns the entry point
    - Sidebar does not show "Activity" twice (only one route resolves to nav.activity, asserted above)
    - No em dashes added: `git diff web/src/pages/Home.tsx web/src/lib/routes.ts web/src/lib/vocabulary.ts | grep -cP "\x{2014}"` returns 0
  </acceptance_criteria>
  <done>The /activity route is the only "Activity" nav item, the old /audit is labeled "Audit", App routes /activity to Activity.tsx, and Home has a one-click entry point; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> GET /api/activity | unauthenticated read attempt crosses the token gate here |
| API response -> rendered feed | server response must carry only param-level fields, never secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-auth | Information Disclosure | GET /api/activity | mitigate | Token gate inherited by mounting on the existing `app` (dashboard.ts:342-356); contract test asserts a tokenless request is rejected (ASVS V2) |
| T-04-infodisc-resp | Information Disclosure | /api/activity response projection | mitigate | Response carries only the curated plan-01 row fields (mirror approvalView's no-raw-secrets rule); no env/token field in the JSON (ASVS V8) |
| T-04-fabrication-ui | Integrity (honesty) | Activity.tsx row rendering + View | mitigate | View shows exactly the captured {tool,tier,mode,outcome} for audit rows and says so; never implies more detail than stored; no row hidden (D-05) |
| T-04-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit); slopcheck N/A. Any new dependency requires a blocking-human legitimacy checkpoint first |
</threat_model>

<verification>
- `npx vitest run src/dashboard.contract.test.ts -t "activity"` green
- `npx vitest run src/dashboard.contract.test.ts` full file green (no regression)
- `npm run build` (vite + tsc) clean
- Manual (deferred to the plan-04 end-of-phase human-verify): /activity renders unlike /audit; sidebar shows one Activity item; Home link reaches the feed
</verification>

<success_criteria>
- Opening /activity shows the reverse-chron, day-grouped, teammate-attributed, tagged feed (TRUST-01 / SC1+SC2)
- Filter chips All / Ran on its own / Needs you / per-teammate work read-side (D-11)
- GET /api/activity is token-gated and returns curated rows with no secrets (TRUST-01)
- The nav collision is resolved: one Activity item -> the new surface; /audit labeled Audit (D-01/D-02)
- Home has a one-click entry point to /activity (D-03)
- Activity looks deliberately unlike Audit (D-01)
</success_criteria>

<output>
Create `.planning/phases/04-activity-feed/04-02-SUMMARY.md` when done
</output>
